# 子任务清单 D16-D18: Demo开发与网关

> **时间范围**: 第16-18个工作日
> **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0

---

## 本阶段目标

✅ "商品趋势问答"Demo接口开发完成  
✅ Demo前端页面开发（Streamlit）  
✅ Kong Gateway集群配置  
✅ Elasticsearch集群部署  

---

## D16: Demo后端接口

### T053: Demo API开发
```python
@router.post("/api/demo/query")
async def product_query(request: QueryRequest):
    # 1. RAG检索
    context = await rag_service.retrieve(request.query, top_k=5)
    # 2. LLM推理
    response = await llm_service.generate(context, request.query)
    return {"answer": response, "sources": context}
```
- [ ] FastAPI Demo项目搭建
- [ ] RAG检索接口
- [ ] LLM调用接口
- [ ] 接口联调测试

**验收标准**: `/api/demo/query` 可正常返回结果

---

## D17: Demo前端 + 联调

### T054: Streamlit前端
```python
import streamlit as st

st.title("AI选品系统 - 商品趋势问答")
query = st.text_input("输入您的问题:")
if query:
    result = api.query(query)
    st.markdown(result["answer"])
```
- [ ] 查询输入界面
- [ ] 结果展示界面
- [ ] 来源引用展示
- [ ] 前后端联调

**验收标准**: 端到端Demo可演示

---

## D18: Kong + ES

### T009: Kong Gateway
- [ ] 部署Kong集群（3 Pod）
- [ ] 配置路由规则（/api/* → 后端服务）
- [ ] JWT认证插件启用
- [ ] 限流配置（100 req/min）

### T024: Elasticsearch
- [ ] ES集群部署（3节点）
- [ ] 创建索引模板
- [ ] 配置IK分词器（中文）
- [ ] BM25检索验证

**验收标准**: 网关转发正常，ES可搜索

---

## 下阶段预告

**D19-D20**: M1里程碑验收准备与评审

---

**文档版本**: v1.0