# 子任务清单 D56-D60: GraphRAG知识图谱

> **时间范围**: 第56-60个工作日
> **所属阶段**: Phase 3 - ERP闭环+RAG增强
> **文档版本**: v1.0

---

## 本阶段目标

✅ Neo4j图数据库部署  
✅ 商品-类目-品牌-供应商实体关系建模  
✅ 知识抽取Pipeline（NER+RE）  
✅ 图检索API（2-hop/3-hop邻居查询）  

---

## D56: T123 Neo4j部署 + Schema设计

**图谱Schema**:
```
(:Product)-[:BELONGS_TO]->(:Category)
(:Product)-[:BRANDED_BY]->(:Brand)
(:Product)-[:SUPPLIED_BY]->(:Supplier)
(:Category)-[:RELATED_TO]->(:Category)  # 关联品类
(:Brand)-[:COMPETES_WITH]->(:Brand)    # 竞品关系
```

- [ ] Neo4j集群部署（3节点）
- [ ] 图Schema定义
- [ ] 索引创建

---

## D57: T124 实体识别 (NER)

**使用模型**: Qwen2.5-7B微调版
```python
# NER Prompt模板
NER_PROMPT = """从以下文本中提取实体：
文本: {text}
输出JSON格式:
{
  "entities": [
    {"type": "PRODUCT", "text": "户外储能电源"},
    {"type": "BRAND", "text": "EcoFlow"},
    {"type": "CATEGORY", "text": "储能设备"}
  ]
}"""
```

- [ ] NER模型训练/微调
- [ ] 四类实体识别：商品/类目/品牌/供应商
- [ ] 实体链接（消歧）

---

## D58: T125 关系抽取 (RE)

**关系类型**:
| 关系 | 说明 | 示例 |
|------|------|------|
| BELONGS_TO | 品类归属 | 户外电源 → 储能设备 |
| BRANDED_BY | 品牌关联 | 产品X → EcoFlow |
| SUPPLIED_BY | 供应关系 | 产品Y → 1688供应商Z |
| COMPETES_WITH | 竞争关系 | EcoFlow ↔ Jackery |

- [ ] RE模型开发
- [ ] 关系置信度评分
- [ ] 三元组生成

---

## D59: T126 图构建Pipeline

**自动化流程**:
```
原始文档 → NER → RE → Neo4j导入 → 图更新
```

- [ ] Pipeline编排
- [ ] 增量更新机制
- [ ] 数据一致性校验

---

## D60: T127 图检索API

```cypher
// 2-hop邻居查询
MATCH (p:Product {name: $product})-[r*1..2]-(neighbor)
RETURN neighbor, r
ORDER BY neighbor.popularity DESC
LIMIT 20
```

- [ ] Cypher查询封装
- [ ] 多跳邻居查询
- [ ] 子图匹配
- [ ] 与向量检索融合

**验收标准**: 图谱可查询，延迟<200ms

---

## 下阶段预告

**D61-D65**: Flink实时特征工程

---

**文档版本**: v1.0