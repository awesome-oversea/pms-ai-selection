# D01-02 配置与文档收口验收标准

> 来源任务清单: [D01-02配置与文档收口任务清单](../../subtasks/D01-02配置与文档收口任务清单.md)
> 所属总表: [子任务清单.md](../../../子任务清单.md)
> 验收范围: D01-D02
> 验收目标: 完成配置契约盘点、配置键名统一和部署文档同步，消除配置口径不一致问题

## 一、验收范围

本验收标准适用于配置与文档收口阶段，覆盖配置盘点、命名规范统一、示例文件修正和部署文档同步。

## 二、核心验收项

| 序号 | 验收项 | 验收标准 | 验收方法 | 结果 |
|------|------|------|------|------|
| A01 | `settings.py` 配置盘点 | 已完整列出全部 `env_prefix`、字段名、默认值和用途 | 文档审查 | ☑ |
| A02 | 环境变量来源盘点 | `.env.example`、`README.md`、`docker-compose.yml`、`k8s/*.yml` 中的环境变量全部纳入清单 | 文档审查 | ☑ |
| A03 | 配置映射表 | 已形成“现状键名 -> 目标键名 -> 使用位置 -> 兼容策略”映射表 | 文档审查 | ☑ |
| A04 | 统一命名规范 | 已明确采用统一前缀规范，且不存在模糊或重复定义 | 方案评审 | ☑ |
| A05 | 配置代码收口 | `src/config/settings.py` 中字段命名与前缀规则一致 | 代码审查 | ☑ |
| A06 | 示例文件可用 | `.env.example` 可直接作为有效模板使用，不含失效键名 | 文件校验 | ☑ |
| A07 | 部署文档同步 | README、Docker、K8s 中使用的环境变量与代码读取逻辑一致 | 文档审查 + 启动验证 | ☑ |
| A08 | 兼容与废弃说明 | 若保留兼容映射，已明确迁移说明、废弃键名和清理计划 | 文档审查 | ☑ |

## 三、交付物验收

| 序号 | 交付物 | 必要内容 | 结果 |
|------|------|------|------|
| D01 | 配置映射表 | 现状键名、目标键名、使用位置、兼容策略 | ☑ |
| D02 | 配置迁移方案 | 修改范围、影响面、兼容策略、回滚方式 | ☑ |
| D03 | `.env.example` | 与当前代码一致，可直接复制使用 | ☑ |
| D04 | 部署与启动文档 | README、Docker、K8s 口径统一 | ☑ |

## 四、通过标准

- 核心验收项 `8/8` 通过。
- 交付物验收 `4/4` 齐全。
- 不存在影响 D03-D04 执行的 P0 配置歧义问题。

## 本次自检记录

- 自检日期: 2026-04-07
- 自检方式:
  - 扫描 `.env.example`、`docker-compose.yml`、`k8s/deployment.yml`、`README.md`、安装手册中的目标键名是否已统一
  - 使用 `python` 脚本分别验证新键名与旧兼容键名均能被 `get_settings()` 正确读取
- 额外修复:
  - 修复 `Settings` 聚合类在类定义阶段提前实例化子配置的问题，改为 `default_factory`，避免环境变量读取被旧值缓存
- 已知风险:
  - `k8s/deployment.yml` 中 `${...}` 仍依赖外部渲染或 CI 注入，这一点已在迁移说明中显式标注
