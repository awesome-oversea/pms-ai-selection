# D15-16 Agent接入与概念收敛验收标准

> 来源任务清单: [D15-16Agent接入与概念收敛任务清单](../../subtasks/D15-16Agent接入与概念收敛任务清单.md)
> 所属总表: [子任务清单.md](../../../子任务清单.md)
> 验收范围: D15-D16
> 验收目标: 让各 Agent 通过 provider 工作，并收敛重复概念与双份实现

## 一、验收范围

本验收标准适用于四个核心 Agent 的 provider 化接入，以及重复概念和重复实现的收敛阶段。

## 二、核心验收项

| 序号 | 验收项 | 验收标准 | 验收方法 | 结果 |
|------|------|------|------|------|
| A01 | `DataCollectionAgent` 接入 | 已通过 provider 获取能力，不再以随机逻辑为主路径 | 代码审查 + 运行验证 | ☐ |
| A02 | `MarketInsightAgent` 接入 | 已通过 provider 获取能力，不再以随机逻辑为主路径 | 代码审查 + 运行验证 | ☐ |
| A03 | `ProductPlannerAgent` 接入 | 已通过 provider 获取能力，不再以随机逻辑为主路径 | 代码审查 + 运行验证 | ☐ |
| A04 | `CommercialAgent` 接入 | 已通过 provider 获取能力，不再以随机逻辑为主路径 | 代码审查 + 运行验证 | ☐ |
| A05 | Fallback 触发条件 | fallback 仅在 real provider 失败时触发，且有日志记录 | 日志验证 | ☐ |
| A06 | `AgentType` 统一 | 项目中关键枚举来源唯一，不存在同名不同义 | 代码审查 | ☐ |
| A07 | `HybridRetriever` 统一 | 检索主实现唯一，重复实现已清理或明确废弃 | 代码审查 | ☐ |
| A08 | 分层边界明确 | `domain` 与 `infrastructure` 的职责边界已说明 | 方案评审 | ☐ |

## 三、交付物验收

| 序号 | 交付物 | 必要内容 | 结果 |
|------|------|------|------|
| D01 | Agent 接入说明 | 四个 Agent 的 provider 使用方式与 fallback 策略 | ☐ |
| D02 | 概念收敛说明 | 统一后的枚举、检索实现和迁移路径 | ☐ |
| D03 | 重复模块清理清单 | 已清理项、保留项、废弃项 | ☐ |

## 四、通过标准

- 核心验收项 `8/8` 通过。
- 交付物验收 `3/3` 齐全。
- Agent 主链路和核心概念已完成第一轮收敛。
