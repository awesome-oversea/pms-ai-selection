# 验收标准 D4-D6: Worker节点与数据存储

> **时间范围**: 第4-6个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0

---

## 本阶段验收总览

| 维度 | 验收项数 | 通过标准 | 实际通过 |
|------|---------|---------|---------|
| 功能验收 | 24项 | ≥23项(96%) | ____/24 |
| 性能验收 | 3项 | 3/3 | ____/3 |
| **阶段结论** | | □ 通过 □ 有条件通过 □ 不通过 | |

---

## D4 验收清单：Worker节点部署

### 功能验收（5项）

| ID | 验收项 | 验证标准 | 验证命令/方法 | P | 状态 |
|----|-------|---------|--------------|---|------|
| D4-F01 | 应用Worker×3加入 | 3台ECS(8核16G)Status=Ready，标签node-role=worker-app | `kubectl get nodes -l node-role=worker-app` | P0 | ☐ |
| D4-F02 | GPU Worker×2加入 | 2台GPU ECS Status=Ready，标签node-role=worker-gpu | `kubectl get nodes -l node-role=worker-gpu` | P0 | ☐ |
| D4-F03 | NVIDIA驱动安装 | nvidia-smi正常运行，Driver Version≥525.85.12 | `nvidia-smi` 输出含GPU信息 | P0 | ☐ |
| D4-F04 | GPU显存检测 | 每张GPU的Memory-Total/Memory-Used显示正确 | `nvidia-smi --query-gpu=name,memory.total,memory.used --format=csv` | P0 | ☐ |
| D4-F05 | 总节点数确认 | kubectl get nodes 显示5个节点全部Ready | `kubectl get nodes -o wide` | P0 | ☐ |

### 节点标签验证
```bash
kubectl get nodes --show-labels | grep -E "node-role"
# 预期输出:
# node-app-1   Ready    ...   node-role=worker-app
# node-app-2   Ready    ...   node-role=worker-app
# node-app-3   Ready    ...   node-role=worker-app
# node-gpu-1   Ready    ...   node-role=worker-gpu
# node-gpu-2   Ready    ...   node-role=worker-gpu
```

---

## D5 验收清单：NVIDIA Device Plugin + PostgreSQL集群

### 功能验收（10项）

| ID | 验收项 | 验证标准 | 验证命令/方法 | P | 状态 |
|----|-------|---------|--------------|---|------|
| D5-F01 | NVIDIA Device Plugin | nvidia-device-plugin-daemonset Pod Running | `kubectl get pods -n kube-system -l app=nvidia-device-plugin-ds` | P0 | ☐ |
| D5-F02 | GPU资源可见 | Node Allocatable包含nvidia.com/gpu | `kubectl describe node <gpu-node> | grep -A5 Allocatable` | P0 | ☐ |
| D5-F03 | MIG配置(如适用) | MIG分区配置生效，nvidia-smi显示MIG设备 | `nvidia-smi -L` | P1 | ☐ |
| D5-F04 | PG主节点StatefulSet | postgresql-master-0 Pod Running，端口5432 | `kubectl get sts -n database; kubectl get svc -n database` | P0 | ☐ |
| D5-F05 | PG从节点×2 | postgresql-replica-0/1 Pod Running | `kubectl get pods -n database -l role=replica` | P0 | ☐ |
| D5-F06 | 流复制启用 | pg_stat_replication显示2个slave，state=streaming | `psql -c "SELECT client_addr, state, sync_state FROM pg_stat_replication;"` | P0 | ☐ |
| D5-F07 | 复制延迟<1s | 从库延迟<1000ms | `SELECT now() - pg_last_xact_replay_timestamp() AS replication_lag;` → <1s | P0 | ☐ |
| D5-F08 | PgBouncer连接池 | pgbouncer Pod Running，端口6432可达 | `kubectl get pods -n database -l app=pgbouncer; psql -p 6432 -c "SHOW POOLS;"` | P1 | ☐ |
| D5-F09 | 业务数据库创建 | pms/fms/kafka/flink等数据库存在 | `\l` 列出所有数据库 | P1 | ☐ |
| D5-F10 | PG连接测试 | 应用可通过PgBouncer连接PG | `psql -h pgbouncer -p 6432 -U app_user -d pms -c "SELECT 1;"` | P0 | ☐ |

---

## D6 验收清单：本地SSD存储 + Redis集群

### 功能验收（9项）

| ID | 验收项 | 验证标准 | 验证命令/方法 | P | 状态 |
|----|-------|---------|--------------|---|------|
| D6-F01 | StorageClass创建 | local-path或hostpath SC可用 | `kubectl get sc` | P0 | ☐ |
| D6-F02 | PVC创建测试 | PVC状态=Bound | `kubectl get pvc test-pvc` → Bound | P0 | ☐ |
| D6-F03 | Pod挂载读写 | 测试Pod可正常读写PVC挂载卷 | `kubectl exec test-pod -- sh -c "echo test > /data/test.txt && cat /data/test.txt"` | P0 | ☐ |
| D6-F04 | IOPS性能达标 | 随机读写IOPS>10000 | `fio --name=randrw --ioengine=libaio --iodepth=32 --rw=randrw --bs=4k --size=1G --numjobs=4 --runtime=60 --group_reporting` | P1 | ☐ |
| D6-F05 | Redis Master×3 | redis-server Pod Running ×3 | `kubectl get pods -n cache -l app=redis -l role=master` | P0 | ☐ |
| D6-F06 | Redis Slave×3 | 数据同步正常，role=slave | `kubectl get pods -n cache -l app=redis -l role=slave; redis-cli -a <pass> info replication` | P0 | ☐ |
| D6-F07 | Sentinel×3监控 | sentinel masters输出正确 | `redis-cli -p 26379 sentinel masters` | P0 | ☐ |
| D6-F08 | 故障切换<3s | 手动停Master后自动切换 | 停Master→计时→新Master选举完成<3s | P0 | ☐ |
| D6-F09 | Cluster模式(如用) | 16384槽位分配均匀，cluster info正常 | `redis-cli -a <pass> cluster info; cluster nodes` | P1 | ☐ |

### 性能基准验收（3项）

| ID | 指标 | 目标值 | 实测值 | 状态 |
|----|------|--------|-------|------|
| D6-P01 | SSD随机读IOPS | >10,000 | ______ IOPS | ☐ |
| D6-P02 | SSD随机写IOPS | >8,000 | ______ IOPS | ☐ |
| D6-P03 | Redis写入QPS | >50,000 (单实例) | ______ QPS | ☐ |

---

## D4-D6 阶段汇总验收

### 基础设施健康检查

```bash
#!/bin/bash
echo "=== D4-D6 基础设施验收 ==="
echo "--- 1. K8s Nodes ---"
kubectl get nodes -o wide
echo "--- 2. GPU资源 ---"
kubectl describe nodes -l node-role=worker-gpu | grep -A3 "nvidia.com/gpu"
echo "--- 3. PG集群 ---"
kubectl get pods -n database -o wide
echo "--- 4. Redis集群 ---"
kubectl get pods -n cache -o wide
echo "--- 4. StorageClass ---"
kubectl get sc
echo "=== 验收完成 ==="
```

### 产出物清单

| 序号 | 产出物 | 格式 | 存放位置 | 状态 |
|------|-------|------|---------|------|
| 1 | Worker节点部署方案 | Markdown | docs/deliverables/D04-D06_Worker节点与数据存储部署方案.md | ☑ |
| 2 | PG集群配置文档 | Markdown | docs/architecture/PostgreSQL集群配置文档.md | ☑ |
| 3 | Redis集群配置文档 | Markdown | docs/architecture/Redis集群配置文档.md | ☑ |
| 4 | PG K8s部署配置 | YAML | k8s/postgresql.yml | ☑ |
| 5 | Redis K8s部署配置 | YAML | k8s/redis.yml | ☑ |
| 6 | 存储性能测试报告 | Markdown | docs/phase4/Phase4_性能基准报告.md | ☑ |

### 阻塞问题记录

| 问题ID | 描述 | 影响 | 负责人 | 解决期限 |
|--------|------|------|--------|---------|

### 阶段签字

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 执行者 | _____________ | _______ | ____ |
| Tech Lead审核 | _____________ | _______ | ____ |
| **阶段结论** | □ 通过进入D7 □ 有条件通过 □ 不通过 | | |

---

**文档版本**: v1.0