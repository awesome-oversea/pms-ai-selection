# 20260419003 本地环境安装部署过程

> 文档日期：2026-04-19
>
> 适用目录：`D:\Project\fms`
>
> 目标：记录本期围绕 `B1-01 ~ B1-04 业务场景运行时验收`、`P1-06 爬虫引擎部署`、`P1-08 Kafka 本地真实运行`、`P1-09 Debezium 真连接器运行`、`P2-06 LLM定期微调真实执行（本地 CPU 基线）`，以及 `P4-01 OAuth2/SSO`、`P5-04 Redis Sentinel`、`P7-03 Flink WSL 平台栈基线` 的实际安装、启动、验收过程，沉淀后续复验与环境恢复基线。 
>
> 说明：本文件只记录本次已经实际执行或实际确认过的步骤，不写理想化流程。项目约定所有终端命令统一使用 `rtk` 前缀；`2026-04-19` 已确认本机可执行 `rtk`，但宿主命令更稳妥的方式仍是 `rtk proxy ...`。为便于直接复制，本文件保留原始命令写法。

---

## 1. 本期处理范围

- 本次主线任务：
  - `B1-01 ~ B1-04 外部数据业务场景工件补强收口`
  - `B1-06 / P1-06 爬虫平台业务化收口`
  - `C1 / P1-08 Kafka 本地真实运行`
  - `C2 / P1-09 Debezium 真连接器运行`
  - `A5-01 / P2-06 本地 CPU 周度微调真实执行`
  - `P4-01 OAuth2/SSO 本地 IdP 路线打通（Keycloak + Authlib）`
  - `P5-04 Redis Sentinel 高可用路径打通`
  - `P7-03 Flink WSL 平台栈基线`
  - `P2-03 / P2-04 WSL 可部署边界确认`
- 本期已完成的相关代码准备：
  - `src/services/business_scenario_catalog_service.py`
  - `src/agents/data_collection.py`
  - `docker-compose.local-kafka.yml`
  - `src/services/kafka_cluster_status_service.py`
  - `src/services/data_sync_service.py`
  - `src/services/local_crawl_runtime_service.py`
  - `src/services/crawl_platform_service.py`
  - `src/services/model_finetune_service.py`
  - `src/config/settings.py`
  - `src/infrastructure/redis.py`
  - `src/api/v1/endpoints/auth.py`
  - `src/main.py`
  - `docker-compose.wsl-platform.yml`
  - `docker/keycloak/pms-dev-realm.json`
  - `src/workers/crawl_scheduler_worker.py`
  - `src/workers/model_finetune_worker.py`
  - `scripts/bootstrap_business_scenario_runtime.py`
  - `scripts/bootstrap_local_crawl_platform.py`
  - `scripts/bootstrap_local_kafka_debezium.py`
  - `scripts/wsl_platform_stack_start.sh`
  - `scripts/run_local_model_finetune.py`
  - `tests/test_business_scenario_catalog.py`
  - `tests/test_data_collection_agent.py`
  - `tests/test_crawl_platform_runtime.py`
  - `tests/test_kafka_runtime.py`
  - `tests/test_local_kafka_compose.py`
  - `tests/test_governance_status_services.py`
  - `tests/test_data_sync_service.py`
  - `tests/test_model_finetune_service.py`
  - `tests/test_auth_oidc_authlib.py`
  - `tests/test_redis_sentinel_runtime.py`
  - `tests/test_local_platform_compose.py`

---

## 2. 本机环境基线

### 2.1 已确认可用的基础环境

- Windows 本机可用，项目工作目录为 `D:\Project\fms`
- WSL 可用，已确认存在并运行：
  - `Ubuntu-22.04`
  - `docker-desktop`
- Docker context 已确认指向 Linux 引擎：
  - `desktop-linux`
- 项目已有本地 PostgreSQL 容器：
  - 容器名：`pms-postgres`
  - 数据库：`pms_db`
  - 用户：`pms`
  - 密码：`pms_dev_2024`

### 2.2 已确认满足 Debezium 的 PostgreSQL 前置条件

本期已确认逻辑复制参数可用：

- `wal_level=logical`
- `max_wal_senders=10`
- `max_replication_slots=10`

这意味着 PostgreSQL 侧不再是 `P1-09` 的主要阻塞点。

---

## 3. 本期实际安装与部署步骤

### 3.1 检查 WSL 与 Docker 可用性

实际使用命令：

```powershell
rtk proxy powershell.exe -NoLogo -Command "wsl -l -v"
rtk proxy powershell.exe -NoLogo -Command "docker context ls"
```

本期目的：

- 确认 WSL 发行版处于运行状态
- 确认 Docker Desktop 使用 Linux context，而不是异常上下文

### 3.2 检查 Docker 相关残留进程

实际使用命令：

```powershell
rtk proxy powershell.exe -NoLogo -Command "Get-CimInstance Win32_Process | Where-Object Name -In 'docker.exe','python.exe' | Select-Object ProcessId,Name,CommandLine | ConvertTo-Json -Compress"
```

本期目的：

- 识别残留 `docker.exe` 客户端会话
- 避免误判为容器或服务端故障

### 3.3 校正本地 Kafka compose 定义

本期已实际修改 `docker-compose.local-kafka.yml`，关键调整如下：

- ZooKeeper 健康检查由 `ruok` 改为 `srvr`
- 增加 `ZOOKEEPER_4LW_COMMANDS_WHITELIST`
- 为 `kafka-connect` 增加： 
  - `extra_hosts: host.docker.internal:host-gateway`
  - 外部网络 `pms-network` 直连 `pms-postgres`
- 统一显式容器名：
  - `pms-local-zookeeper`
  - `pms-local-kafka`
  - `pms-local-kafka-init`
  - `pms-local-kafka-connect`
- Kafka 内部地址改为容器名互连：
  - `KAFKA_ZOOKEEPER_CONNECT: pms-local-zookeeper:2181`
  - `KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://pms-local-kafka:29092,PLAINTEXT_HOST://localhost:9092`
- Kafka 健康检查改为轻量 TCP 检查： 

```yaml
test: ["CMD-SHELL", "bash -lc 'echo > /dev/tcp/127.0.0.1/9092'"] 
``` 

- `kafka-init` 已补 Connect 内部 topic 自愈：
  - 自动确保 `pms-connect-configs` / `pms-connect-offsets` / `pms-connect-status` 为单分区
  - 自动确保 `cleanup.policy=compact`
- `DataSyncService` 默认 CDC 数据源已从 `host.docker.internal` 调整为 `pms-postgres`

### 3.4 补齐运行态探测与验收脚本

本期已实际补齐：

- `src/services/kafka_cluster_status_service.py`
  - 探测本地容器是否存在、是否运行、健康状态
  - 探测 Kafka Connect REST 是否可达
  - 探测 Debezium 插件列表、已注册连接器、运行中连接器
- `src/services/data_sync_service.py`
  - 生成真实本地 PostgreSQL 对接所需 Debezium connector 配置
- `scripts/bootstrap_local_kafka_debezium.py`
  - 分阶段拉起 `zookeeper`、`kafka`、`kafka-init`、`kafka-connect`
  - 在 `pms-postgres` 中创建源表
  - 注册 `oms` / `crm` Debezium connector
  - 发送样例更新并消费 CDC 消息
  - 输出验收产物到 `artifacts/ops/local_kafka_debezium_acceptance.json`

### 3.5 执行针对性回归

本期已实际执行并通过：

```powershell
rtk python -m py_compile src/services/kafka_cluster_status_service.py src/services/data_sync_service.py scripts/bootstrap_local_kafka_debezium.py tests/test_local_kafka_compose.py tests/test_governance_status_services.py tests/test_data_sync_service.py
rtk python -m pytest tests/test_local_kafka_compose.py tests/test_governance_status_services.py tests/test_data_sync_service.py -q
```

本期结果：

- `py_compile` 通过
- `pytest` 通过：`26 passed`

说明： 

- 代码、配置、状态面与针对性回归已经到位 
- 本期最终已完成本地 Docker 真实运行态复验与 CDC 实机验收 

### 3.6 执行本地 CPU 周度微调链路

本期已实际执行并通过：

```powershell
rtk python -m py_compile src/services/model_finetune_service.py src/workers/model_finetune_worker.py scripts/run_local_model_finetune.py tests/test_model_finetune_service.py
rtk python -m pytest tests/test_model_finetune_service.py -q
rtk python -m pytest tests/test_api_integration.py -k model_finetune -q
rtk python D:\Project\fms\scripts\run_local_model_finetune.py
```

本期结果：

- `py_compile` 通过
- `tests/test_model_finetune_service.py` 通过：`3 passed`
- `tests/test_api_integration.py -k model_finetune` 通过：`1 passed`
- `artifacts/llm/local_model_finetune_acceptance.json` 生成且 `accepted=true`

说明：

- `ModelFinetuneService` 已不再只是版本号递增模拟，而是会读取本地 `erp_local` 反馈数据与场景工件，执行 CPU `feedback-adapter` 训练并产出版本化工件
- 当前本地真实训练基线已可重复执行，不依赖 GPU

### 3.7 执行本地 real 爬虫平台链路

本期已实际执行并通过：

```powershell
rtk python -m pip install scrapy
rtk python -m playwright install chromium
rtk python -m py_compile src/infrastructure/kafka.py src/services/local_crawl_runtime_service.py src/services/crawl_platform_service.py src/workers/crawl_scheduler_worker.py scripts/bootstrap_local_crawl_platform.py tests/test_crawl_platform_runtime.py tests/test_kafka_runtime.py
rtk python -m pytest tests/test_kafka_runtime.py tests/test_crawl_platform_runtime.py tests/test_governance_status_services.py::test_crawl_platform_local_runner_publishes_records -q
rtk python -m pytest tests/test_api_integration.py -k "crawl_platform" -q
rtk python D:\Project\fms\scripts\bootstrap_local_crawl_platform.py
```

本期结果：

- `Scrapy 2.15.0` 已安装并可执行
- `Playwright Chromium` 已安装并可进行动态页面抓取
- `pytest` 通过：`4 passed`
- `tests/test_api_integration.py -k "crawl_platform"` 通过：`4 passed`
- `artifacts/ops/local_crawl_platform_acceptance.json` 生成且 `accepted=true`

说明：

- 本地 `P1-06` 已形成可重复执行的 `local-real` 验收基线，不依赖外部付费服务或 GPU。
- `src/infrastructure/kafka.py` 已补 producer 跨事件循环重建与 `start/send` 超时保护，避免爬虫平台组合回归时发布链路挂起。

### 3.8 执行业务场景运行时验收

本期已实际执行并通过：

```powershell
python -m py_compile src/agents/data_collection.py src/services/business_scenario_catalog_service.py scripts/bootstrap_business_scenario_runtime.py tests/test_business_scenario_catalog.py tests/test_data_collection_agent.py
python -m pytest tests/test_business_scenario_catalog.py tests/test_data_collection_agent.py -q
python D:\Project\fms\scripts\bootstrap_business_scenario_runtime.py
```

本期结果：

- `py_compile` 通过
- `pytest` 通过：`19 passed`
- `artifacts/ops/business_scenario_runtime_acceptance.json` 生成且 `accepted=true`
- 验收场景覆盖：Amazon / TikTok / Google Trends / 1688 共 `11` 个代表场景

说明：

- 本步骤不需要新增系统级安装，只依赖仓内脚本与既有 Python 环境。
- `DataCollectionAgent` 的场景化 mock 返回现已统一显式输出 `degraded` / `degradation_reason`，避免后续验收脚本出现字段漂移。

### 3.9 补 WSL 平台栈与 OIDC / Redis Sentinel 代码级基线

本期已实际执行并通过：

```powershell
python -m pip install authlib itsdangerous
python -m py_compile src\config\settings.py src\infrastructure\redis.py src\api\v1\endpoints\auth.py src\main.py src\core\data_masking.py src\services\channel_delivery_service.py tests\test_auth_oidc_authlib.py tests\test_redis_sentinel_runtime.py tests\test_local_platform_compose.py tests\test_api_integration.py
python -m pytest tests\test_auth_oidc_authlib.py tests\test_redis_sentinel_runtime.py tests\test_local_platform_compose.py -q
python -m pytest tests\test_api_integration.py -k oidc -q
docker compose -f D:\Project\fms\docker-compose.wsl-platform.yml config --services
```

本期结果：

- `authlib`、`itsdangerous` 已安装
- `py_compile` 通过
- `tests/test_auth_oidc_authlib.py`、`tests/test_redis_sentinel_runtime.py`、`tests/test_local_platform_compose.py` 通过：`10 passed`
- `tests/test_api_integration.py -k oidc` 通过：`2 passed`
- `docker-compose.wsl-platform.yml` 已通过 `config --services` 校验，包含：
  - `redis-master` / `redis-replica-1` / `redis-replica-2`
  - `redis-sentinel-1` / `redis-sentinel-2` / `redis-sentinel-3`
  - `keycloak-db` / `keycloak`
  - `flink-jobmanager` / `flink-taskmanager`

说明：

- OIDC 当前已具备 Authlib 授权码流程、本地 Session 会话态和 Keycloak 本地 IdP 路线
- Redis 当前已具备 Sentinel 配置模板、运行时连接逻辑与 WSL compose 基线
- Flink 当前已具备 WSL JobManager / TaskManager 基线，但尚未形成作业提交验收产物
- `P2-04 Whisper` 已明确改为 `Whisper tiny / base` 纯 CPU 路线；`P2-03` 已明确改为 `Qwen3.5-2B / TinyGPT-V` 轻量 GGUF 多模态路线

### 3.10 尝试拉起 WSL 平台栈

本期已实际尝试：

```powershell
bash D:\Project\fms\scripts\wsl_platform_stack_start.sh
docker compose -f D:\Project\fms\docker-compose.wsl-platform.yml ps
docker ps --format "{{.Names}}|{{.Status}}|{{.Ports}}"
```

本期结果：

- 未能拿到稳定的 `up/ps` 结果
- `bash` 路线出现 `Bash/Service/0x8007274c`
- `docker compose ps` / `docker ps` 返回 Docker Desktop Linux Engine API `500`

说明：

- 当前阻塞点在 Docker Desktop / Bash 运行时，不在 `docker-compose.wsl-platform.yml` 结构本身
- 因此本轮可以确认“代码、配置、测试已到位”，但不能把 `P5-04` / `P7-03` 直接记为“实机验收完成”

### 3.11 验证 WSL PostgreSQL HA 栈

本期已实际执行：

```powershell
python -m py_compile tests\test_local_postgres_ha_compose.py
python -m pytest tests\test_local_postgres_ha_compose.py -q
docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml config --services
docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml up -d pg-primary pg-standby-1 pg-standby-2 pgpool
```

本期结果：

- `py_compile` 通过
- `tests/test_local_postgres_ha_compose.py`：`4 passed`
- `config --services` 返回：`pg-primary` / `pg-standby-1` / `pg-standby-2` / `pgpool`
- 为避免与现有 `kong-database:15433` 端口冲突，已将 `pg-primary` 宿主机端口调整为 `15435`
- `up -d` 仍被 Docker Desktop Linux Engine API `500` 阻塞，报错为：
  - `unable to get image 'bitnami/postgresql-repmgr:16': request returned 500 Internal Server Error`

说明：

- 当前可以确认 `P5-03` 的 WSL + Docker 编排、脚本、测试与接入端口设计已落地
- 当前不能把 PostgreSQL 主从复制 / 自动故障切换直接记为“实机验收完成”

### 3.12 轻量 AI 基线脚本准备

本期已补：

```powershell
Get-Content D:\Project\fms\scripts\wsl_lightweight_ai_prepare.sh
where.exe ollama
```

本期结果：

- 已新增 `scripts/wsl_lightweight_ai_prepare.sh`
- 脚本会拉取默认文本模型 `qwen2.5:1.5b`，并提示 CPU 方案：
  - rerank：`bge-reranker-base`
  - speech：`Whisper tiny / base`
  - multimodal：`Qwen3.5-2B / TinyGPT-V`
- 本机已检测到 `ollama` 可执行文件：`D:\Ollama\ollama.exe`

说明：

- 该脚本当前作为轻量模型基线准备入口保留
- 真实模型拉取与 WSL 内多模态 / Whisper 运行，仍需在目标 WSL 运行时继续执行

---

## 4. 本期推荐的复验步骤

以下步骤是本次已经固化下来的下一轮复验顺序。

### 4.1 先清理残留 Docker 客户端会话

建议先检查：

```powershell
rtk proxy powershell.exe -NoLogo -Command "Get-CimInstance Win32_Process | Where-Object Name -EQ 'docker.exe' | Select-Object ProcessId,CommandLine | ConvertTo-Json -Compress"
```

若确认存在残留本轮 compose/inspect/exec 客户端，再做定向结束，避免直接动 Docker Desktop 服务。

### 4.2 确认旧容器未干扰新栈

重点关注是否仍存在旧容器：

- `pms-zk-local`
- `pms-kafka-local`

这些旧容器与当前标准容器名并存时，会干扰对真实运行态的判断。

### 4.3 按阶段重建本地 Kafka 栈

当前脚本采用分阶段方式，建议继续沿用：

```powershell
rtk docker compose -f D:\Project\fms\docker-compose.local-kafka.yml up -d --force-recreate zookeeper kafka
rtk docker compose -f D:\Project\fms\docker-compose.local-kafka.yml up -d --force-recreate kafka-init
rtk docker compose -f D:\Project\fms\docker-compose.local-kafka.yml up -d --force-recreate kafka-connect
```

### 4.4 跑真实验收脚本

```powershell
rtk python D:\Project\fms\scripts\bootstrap_local_kafka_debezium.py
```

验收通过的最低标准：

- `pms-local-zookeeper` healthy
- `pms-local-kafka` healthy
- `pms-local-kafka-init` exited `0`
- `pms-local-kafka-connect` healthy
- `oms-debezium-connector`、`crm-debezium-connector` 已注册且运行中
- `artifacts/ops/local_kafka_debezium_acceptance.json` 中：
  - `accepted=true`
  - `missing_topics=[]`
  - `cdc_messages.envelope_ready=true`

### 4.5 复验本地 CPU 微调链路

```powershell
rtk python D:\Project\fms\scripts\run_local_model_finetune.py
```

验收通过的最低标准：

- `artifacts/llm/local_model_finetune_acceptance.json` 已生成
- `accepted=true`
- `training_mode=local-real`
- `training_backend=cpu-feedback-adapter`
- `evaluation.not_regressed=true`

### 4.6 复验本地 real 爬虫平台链路

```powershell
rtk python D:\Project\fms\scripts\bootstrap_local_crawl_platform.py
```

验收通过的最低标准：

- `artifacts/ops/local_crawl_platform_acceptance.json` 已生成
- `accepted=true`
- `runtime_acceptance.scrapy.ready=true`
- `runtime_acceptance.playwright.ready=true`
- `scheduler.accepted=true`
- `scheduler.job_count=4`

### 4.7 复验本地 self-hosted 代理池链路

```powershell
rtk python D:\Project\fms\scripts\bootstrap_local_proxy_provider.py
```

本轮实测结果：

- `artifacts/ops/local_proxy_provider_acceptance.json` 已生成
- `accepted=true`
- `mode=local-self-hosted`
- `configuration_ready=true`
- `provider_runtime.probe.success_count=2`
- `provider_runtime.probe.results[0].proxy_identity=proxy-node-a`
- `provider_runtime.probe.results[1].proxy_identity=proxy-node-b`

### 4.8 复验 WSL PostgreSQL HA 栈

```powershell
rtk python -m pytest tests\test_local_postgres_ha_compose.py -q
rtk docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml config --services
rtk docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml up -d pg-primary pg-standby-1 pg-standby-2 pgpool
```

验收通过的最低标准：

- `tests/test_local_postgres_ha_compose.py` 通过
- `config --services` 能稳定返回 `pg-primary` / `pg-standby-1` / `pg-standby-2` / `pgpool`
- `docker compose ... up -d` 成功
- `docker exec pms-pg-primary-wsl repmgr cluster show` 可用
- `docker exec pms-pgpool-wsl psql -U pms -d pms_db -c "show pool_nodes;"` 返回主从节点

### 4.9 轻量 AI 基线准备

```powershell
bash D:\Project\fms\scripts\wsl_lightweight_ai_prepare.sh
```

验收通过的最低标准：

- `ollama pull qwen2.5:1.5b` 成功
- 本地默认文本模型保持为 `Qwen2.5-1.5B`
- CPU rerank / Whisper / 轻量多模态方案说明与环境变量保持一致

---

## 5. 本期产物与证据

### 5.1 代码与配置产物

- [docker-compose.local-kafka.yml](D:\Project\fms\docker-compose.local-kafka.yml)
- [src/services/kafka_cluster_status_service.py](D:\Project\fms\src\services\kafka_cluster_status_service.py)
- [src/services/data_sync_service.py](D:\Project\fms\src\services\data_sync_service.py)
- [src/services/local_crawl_runtime_service.py](D:\Project\fms\src\services\local_crawl_runtime_service.py)
- [src/services/crawl_platform_service.py](D:\Project\fms\src\services\crawl_platform_service.py)
- [src/services/local_proxy_runtime_service.py](D:\Project\fms\src\services\local_proxy_runtime_service.py)
- [src/services/proxy_provider_service.py](D:\Project\fms\src\services\proxy_provider_service.py)
- [src/services/model_finetune_service.py](D:\Project\fms\src\services\model_finetune_service.py)
- [src/workers/crawl_scheduler_worker.py](D:\Project\fms\src\workers\crawl_scheduler_worker.py)
- [src/workers/model_finetune_worker.py](D:\Project\fms\src\workers\model_finetune_worker.py)
- [scripts/bootstrap_local_crawl_platform.py](D:\Project\fms\scripts\bootstrap_local_crawl_platform.py)
- [scripts/bootstrap_local_proxy_provider.py](D:\Project\fms\scripts\bootstrap_local_proxy_provider.py)
- [scripts/bootstrap_local_kafka_debezium.py](D:\Project\fms\scripts\bootstrap_local_kafka_debezium.py)
- [scripts/run_local_model_finetune.py](D:\Project\fms\scripts\run_local_model_finetune.py)
- [docker-compose.wsl-postgres-ha.yml](D:\Project\fms\docker-compose.wsl-postgres-ha.yml)
- [scripts/wsl_postgres_ha_stack_start.sh](D:\Project\fms\scripts\wsl_postgres_ha_stack_start.sh)
- [scripts/wsl_lightweight_ai_prepare.sh](D:\Project\fms\scripts\wsl_lightweight_ai_prepare.sh)
- [tests/test_local_postgres_ha_compose.py](D:\Project\fms\tests\test_local_postgres_ha_compose.py)

### 5.2 验收与状态文档

- [2026041902下一步项目任务清单.md](D:\Project\fms\2026041902下一步项目任务清单.md)
- [2026041902下一步项目任务清单-验收标准.md](D:\Project\fms\2026041902下一步项目任务清单-验收标准.md)
- [2026041601任务分解清单.md](D:\Project\fms\2026041601任务分解清单.md)
- [2026041601任务分解清单-验收标准.md](D:\Project\fms\2026041601任务分解清单-验收标准.md)

### 5.3 当前验收产物状态 

当前 `artifacts/ops/local_kafka_debezium_acceptance.json` 已形成完整通过证据，关键结果如下：

- `accepted=true`
- `missing_topics=[]`
- `connector_runtime.all_expected_running=true`
- `cdc_messages.matched_count=3`
- `cdc_messages.envelope_ready=true`

当前 `artifacts/llm/local_model_finetune_acceptance.json` 已形成本地真实微调通过证据，关键结果如下：

- `accepted=true`
- `training_mode=local-real`
- `training_backend=cpu-feedback-adapter`
- `evaluation.validation_score=1.0`
- `evaluation.not_regressed=true`

当前 `artifacts/ops/local_crawl_platform_acceptance.json` 已形成本地 real 爬虫平台通过证据，关键结果如下：

- `accepted=true`
- `runtime_acceptance.scrapy.item_count=3`
- `runtime_acceptance.playwright.item_count=3`
- `scheduler.job_count=4`
- `scheduler.accepted=true`

当前 `artifacts/ops/local_proxy_provider_acceptance.json` 已形成本地 self-hosted 代理池通过证据，关键结果如下：

- `accepted=true`
- `mode=local-self-hosted`
- `configuration_ready=true`
- `provider_runtime.probe.success_count=2`
- `provider_runtime.probe.results[0].proxy_identity=proxy-node-a`
- `provider_runtime.probe.results[1].proxy_identity=proxy-node-b`

当前 `P5-03 PostgreSQL HA` 已形成“代码 / 配置 / 测试就绪”的证据，关键结果如下：

- `python -m py_compile tests\test_local_postgres_ha_compose.py` 通过
- `python -m pytest tests\test_local_postgres_ha_compose.py -q`：`4 passed`
- `docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml config --services` 返回 `pg-primary` / `pg-standby-1` / `pg-standby-2` / `pgpool`
- `docker compose -f D:\Project\fms\docker-compose.wsl-postgres-ha.yml up -d ...` 仍被 Docker Desktop Linux Engine API `500` 阻塞，尚未形成实机主从切换证据

当前轻量 AI 基线已形成“默认方案切换完成”的证据，关键结果如下：

- `.env.example` / `src/config/settings.py` / `src/config/__init__.py` 已切换到 `Qwen2.5-1.5B + Ollama`
- rerank 已切换到 `bge-reranker-base (纯CPU)`
- speech 已切换到 `Whisper tiny / base (纯CPU)`
- multimodal 已切换到 `Qwen3.5-2B / TinyGPT-V (GGUF)`
- `scripts/wsl_lightweight_ai_prepare.sh` 已补齐为轻量模型准备入口
- 本机已检测到 `ollama` 可执行文件：`D:\Ollama\ollama.exe`

---

## 6. 当前结论

- 本期已经把 Kafka/Debezium 本地真实运行与爬虫平台本地 real 验收所需的代码、配置、状态面、验收脚本、针对性测试全部补齐并完成实机验收。 
- PostgreSQL 逻辑复制前置条件、Kafka Connect 内部 topic、自愈逻辑与 `pms-postgres` 直连路径均已落地。 
- 本期已经把本地大模型默认方案切换为 `WSL + Ollama + 轻量模型`，并补齐 `scripts/wsl_lightweight_ai_prepare.sh` 作为基线准备入口。
- `P5-03 PostgreSQL HA` 当前已完成 Compose / 脚本 / 测试 / 端口规划落地，但 Docker 运行时仍阻塞真实拉起与故障切换验收。
- 本期最终结论为： 
- `P1-06`：`✅已完成（local-real Scrapy + Playwright + scheduler run-once）`
- `P1-07`：`✅已完成（local self-hosted 双节点代理池）`
- `P1-08`：`✅已完成`
  - `P1-09`：`✅已完成`
  - `P2-06`：`✅已完成（local-real CPU feedback-adapter）`
- 后续若需要重跑，只需复用本文件中的命令顺序与 `artifacts/ops/local_kafka_debezium_acceptance.json`、`artifacts/ops/local_crawl_platform_acceptance.json` 作为验收基线。 

### 2026-04-19 补充：本地 CI/CD 与 staging 发布记录验收

本轮继续完成 `P5-09` 本地 CI/CD 验收，已在仓内执行以下命令：

```powershell
python scripts/release_quality_gates.py security-smoke
python scripts/release_quality_gates.py smoke
python scripts/release_deploy.py --target staging --version local-ci-20260419
```

本轮产物与结果：

- `artifacts/release/latest_gate_check.json`
  - `mode=smoke`
  - `status=passed`
- `artifacts/release/latest_release.json`
  - `target=staging`
  - `status=deployed`
  - `version=local-ci-20260419`
- 本地安全门禁：`50 passed`
- 本地 smoke 门禁：`13 passed`

当前仓内 `.github/workflows/ci.yml` 已同步补齐 `security-smoke`、`docker-build`、`release-gates`、`staging-deploy` 四段链路，可作为后续远端 GitHub Actions/self-hosted runner 的标准基线。

### 2026-04-19 补充：P2-09 Neo4j / GraphRAG 本地验收尝试

本轮继续推进 `P2-09 GraphRAG升级到真实图底座`，已先完成代码与配置层的本地化准备：

- `docker-compose.wsl-local.yml` 已新增 `neo4j` 服务
- `.env.example` 已补 `NEO4J_ENABLED`、`NEO4J_URI`、账号密码、数据库、超时与 fallback 开关
- `scripts/wsl_local_stack_start.sh` 已纳入 `neo4j` 启动与 `cypher-shell` 检查建议
- `src/services/graph_rag_service.py` 已补 `neo4j.connection_verified` 与 `neo4j.runtime` 状态输出
- `scripts/bootstrap_local_graph_rag_neo4j.py` 已作为本地 GraphRAG 图底座验收脚本落地

本轮已执行：

```powershell
python -m py_compile src\services\graph_rag_service.py scripts\bootstrap_local_graph_rag_neo4j.py tests\test_graph_rag_service.py tests\test_local_graph_rag_compose.py
python -m pytest tests\test_graph_rag_service.py tests\test_local_graph_rag_compose.py -q
python -m pytest tests\test_api_integration.py -k "graph_rag_endpoints" -q
python D:\Project\fms\scripts\bootstrap_local_graph_rag_neo4j.py
```

本轮结果：

- `py_compile` 通过
- `tests/test_graph_rag_service.py tests/test_local_graph_rag_compose.py -q`：`8 passed`
- `tests/test_api_integration.py -k "graph_rag_endpoints" -q`：`1 passed`
- 已生成产物：`artifacts/ops/local_graph_rag_neo4j_acceptance.json`

当前结论：

- GraphRAG 的本地图 / Neo4j 双路径、状态面、Compose 配置、环境变量模板、验收脚本已经补齐
- 外网恢复后已完成 `docker compose ... pull neo4j`，并已安装 Python `neo4j-driver`
- 在显式移除旧容器后，`docker compose ... up -d neo4j` 已完成新容器重建
- 进一步排查发现 Neo4j 5.26.24 镜像内 `cypher-shell` 实际路径为 `/var/lib/neo4j/bin/cypher-shell`，不在默认 PATH；这导致 Compose healthcheck 与早期 bootstrap 探测出现假失败
- 已修复 `docker-compose.wsl-local.yml` 健康检查、`scripts/wsl_local_stack_start.sh` 提示命令与 `tests/test_local_graph_rag_compose.py` 断言
- `scripts/bootstrap_local_graph_rag_neo4j.py` 已从宿主机 `docker exec cypher-shell` 探测切换到宿主机 `neo4j-driver` 探测/清图，规避非 TTY `docker exec` 超时问题
- 当前 `docker ps --filter name=pms-neo4j-local` 返回 `Up ... (healthy)`，宿主机 Bolt/HTTP 可用，`GraphRAGService` 返回 `active_backend=Neo4jGraphStore` + `connection_verified=true`
- `artifacts/ops/local_graph_rag_neo4j_acceptance.json` 已更新为 `accepted=true`，`P2-09` 本机真实验收完成
### 2026-04-19 补充：P6-04 Dify HTTP runtime 本地验收

本轮继续推进 `P6-04 Dify低代码编排集成`，先完成仓内可本地闭环的 HTTP runtime 能力，不等待真实 Dify UI。

本轮处理：
- 新增 `src/services/dify_workflow_service.py`，封装真实 `POST /v1/workflows/run` 调用。
- 在 `src/services/agent_platform_service.py` 中接入真实 Dify runtime，并在失败时回退到 `dify-compatible`。
- 在 `src/apps/agent_service.py` `/status` 中暴露 `dify` 运行时状态。
- 在 `.env.example` 中补充 `DIFY_*` 本地环境变量模板。

本轮执行：

```powershell
python -m py_compile src/services/dify_workflow_service.py src/services/agent_platform_service.py src/apps/agent_service.py src/config/settings.py tests/test_dify_workflow_service.py tests/test_agent_platform_service.py tests/test_agent_service_dify_status.py
python -m pytest tests\test_dify_workflow_service.py -q
python -m pytest tests\test_agent_platform_service.py -k "test_agent_platform_service_uses_real_dify_runtime_when_available or test_agent_platform_service_falls_back_when_dify_runtime_errors" -q
python -m pytest tests\test_agent_service_dify_status.py -q
python -m pytest tests\test_api_integration.py -k "test_agent_platform_dify_framework_invoke_endpoint" -q
python -m pytest tests\test_dify_workflow_service.py tests\test_agent_platform_service.py tests\test_agent_service_dify_status.py -q
```

本轮结果：
- `py_compile` 通过
- `tests/test_dify_workflow_service.py`：`3 passed`
- `tests/test_agent_platform_service.py` Dify 子集：`2 passed`
- `tests/test_agent_service_dify_status.py`：`1 passed`
- `tests/test_api_integration.py -k test_agent_platform_dify_framework_invoke_endpoint`：`1 passed`
- 组合回归：`7 passed`

产物：
- `artifacts/ops/local_dify_runtime_acceptance.json`

当前结论：
- 仓库内已经完成 Dify HTTP runtime、fallback 和运行时状态暴露的本地代码级验收。
- 当前未做真实 Dify 容器部署、界面编排和内置 RAG pipeline 的外部环境验证。
