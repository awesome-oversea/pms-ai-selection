# 单元测试用例文档 D21-D23: Agent框架启动

> **时间范围**: 第21-23个工作日 | **所属阶段**: Phase 2 - Multi-Agent集成
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D21 | LangGraph4j与AutoGen4j集成 | 功能验证 | 10 |
| D22 | Selection Master状态机设计 | 功能验证 | 8 |
| D23 | 状态机实现与角色定义 | 功能验证 | 8 |

**总计**: 26个测试用例

---

## 二、D21 测试用例：Agent框架集成

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D21-TC01 | Agent模块导入 | `from src.agents import Agent` | 导入成功 | P0 |
| D21-TC02 | LangGraph状态机 | 创建简单状态机 | 状态机创建成功 | P0 |
| D21-TC03 | 状态节点定义 | 定义State/Node | 节点可定义 | P0 |
| D21-TC04 | 条件边定义 | 定义条件分支 | 分支可定义 | P0 |
| D21-TC05 | AutoGen对话 | 创建Agent对话 | 对话可创建 | P0 |
| D21-TC06 | Agent间通信 | 测试Agent消息传递 | 消息传递成功 | P0 |
| D21-TC07 | Agent工具调用 | 定义Agent Tool | Tool可调用 | P0 |
| D21-TC08 | Agent记忆管理 | 检查记忆模块 | 记忆可存储 | P1 |
| D21-TC09 | Agent配置管理 | 检查配置加载 | 配置可加载 | P1 |
| D21-TC10 | Agent日志记录 | 检查日志输出 | 日志完整 | P1 |

### 2.2 自动化测试脚本

```python
class TestD21_AgentFrameworkIntegration:
    """
    D21 Agent框架集成验证测试。
    
    验证:
        - Agent框架可正常导入
        - LangGraph状态机功能正常
        - AutoGen对话功能正常
    """
    
    def test_agent_module_import(self):
        """验证Agent模块可导入。"""
        from src.agents import BaseAgent
        assert BaseAgent is not None
    
    def test_agent_state_import(self):
        """验证Agent状态模块可导入。"""
        from src.agents.state import AgentState
        assert AgentState is not None
    
    def test_agent_tool_import(self):
        """验证Agent工具模块可导入。"""
        from src.agents.tools import BaseTool
        assert BaseTool is not None
    
    def test_agent_config_import(self):
        """验证Agent配置模块可导入。"""
        from src.agents.config import AgentConfig
        assert AgentConfig is not None
    
    def test_selection_master_import(self):
        """验证Selection Master可导入。"""
        from src.agents.selection_master import SelectionMaster
        assert SelectionMaster is not None
```

---

## 三、D22 测试用例：Selection Master状态机设计

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D22-TC01 | 状态机图存在 | 检查设计文档 | 图存在 | P0 |
| D22-TC02 | 初始状态定义 | 检查初始状态 | 状态已定义 | P0 |
| D22-TC03 | 终止状态定义 | 检查终止状态 | 状态已定义 | P0 |
| D22-TC04 | 状态转换规则 | 检查转换条件 | 规则完整 | P0 |
| D22-TC05 | 分支条件逻辑 | 检查条件分支 | 分支明确 | P0 |
| D22-TC06 | 循环处理逻辑 | 检查循环状态 | 循环可处理 | P1 |
| D22-TC07 | 错误状态处理 | 检查错误处理 | 处理完整 | P1 |
| D22-TC08 | 状态持久化 | 检查状态存储 | 可持久化 | P1 |

### 3.2 自动化测试脚本

```python
class TestD22_SelectionMasterStateMachine:
    """
    D22 Selection Master状态机设计验证测试。
    
    验证:
        - 状态机定义完整
        - 状态转换正确
        - 分支条件明确
    """
    
    def test_state_machine_import(self):
        """验证状态机模块可导入。"""
        from src.agents.state_machine import StateMachine
        assert StateMachine is not None
    
    def test_state_definition(self):
        """验证状态定义。"""
        from src.agents.state import AgentStateEnum
        assert AgentStateEnum is not None
    
    def test_transition_rules(self):
        """验证状态转换规则。"""
        from src.agents.state_machine import TransitionRule
        assert TransitionRule is not None
    
    def test_branch_condition_import(self):
        """验证分支条件模块可导入。"""
        from src.agents.conditions import BranchCondition
        assert BranchCondition is not None
```

---

## 四、D23 测试用例：状态机实现与角色定义

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D23-TC01 | 状态机执行 | 执行状态转换 | 转换成功 | P0 |
| D23-TC02 | 条件分支执行 | 触发条件分支 | 正确分支 | P0 |
| D23-TC03 | 循环逻辑执行 | 触发循环 | 正确循环 | P0 |
| D23-TC04 | Jaxx角色定义 | 检查角色配置 | 角色已定义 | P0 |
| D23-TC05 | Jobs角色定义 | 检查角色配置 | 角色已定义 | P0 |
| D23-TC06 | Pony角色定义 | 检查角色配置 | 角色已定义 | P0 |
| D23-TC07 | 角色Prompt模板 | 检查Prompt | 模板存在 | P1 |
| D23-TC08 | 角色工具绑定 | 检查工具绑定 | 工具已绑定 | P1 |

### 4.2 自动化测试脚本

```python
class TestD23_StateMachineImplAndRoles:
    """
    D23 状态机实现与角色定义验证测试。
    
    验证:
        - 状态机可执行
        - 角色定义完整
        - 工具绑定正确
    """
    
    def test_agent_roles_import(self):
        """验证角色定义模块可导入。"""
        from src.agents.roles import JaxxAgent, JobsAgent, PonyAgent
        assert JaxxAgent is not None
        assert JobsAgent is not None
        assert PonyAgent is not None
    
    def test_role_config(self):
        """验证角色配置。"""
        from src.agents.config import get_agent_config
        config = get_agent_config("jaxx")
        assert config is not None
    
    def test_role_prompt_template(self):
        """验证角色Prompt模板。"""
        from src.agents.prompts import get_role_prompt
        prompt = get_role_prompt("jaxx")
        assert prompt is not None
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 安装测试依赖
pip install pytest pytest-asyncio langchain langgraph

# 配置环境变量
export LLM_API_URL="http://localhost:8000"
```

### 5.2 执行命令

```bash
# 执行D21-D23全部测试
pytest tests/test_d21_d23.py -v

# 生成覆盖率报告
pytest tests/test_d21_d23.py --cov=src.agents --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| 状态机功能 | 正常 | ____ |
| 角色定义 | 4个完整 | ____ |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/agents/__init__.py` | Agent模块入口 |
| `src/agents/base.py` | Agent基类 |
| `src/agents/state.py` | Agent状态定义 |
| `src/agents/state_machine.py` | 状态机实现 |
| `src/agents/selection_master.py` | Selection Master |
| `src/agents/roles/__init__.py` | 角色定义 |
| `src/agents/tools/__init__.py` | Agent工具 |
| `src/agents/config.py` | Agent配置 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
