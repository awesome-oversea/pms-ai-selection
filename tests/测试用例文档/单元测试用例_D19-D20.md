# 单元测试用例文档 D19-D20: M1里程碑验收

> **时间范围**: 第19-20个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D19 | 收尾与准备 | 功能验证 | 5 |
| D20 | M1里程碑评审 | 里程碑验收 | 5 |

**总计**: 10个测试用例

---

## 二、M1里程碑验收标准

| 验收项 | 验收标准 | 状态 |
|-------|---------|------|
| K8s集群可用 | Master+Worker+GPU节点全部Ready | ☐ |
| vLLM单节点运行 | Qwen2.5-72B模型加载成功 | ☐ |
| Qdrant集群部署 | 3节点Raft集群健康 | ☐ |
| Amazon BSR爬虫 | 可采集Amazon BSR数据 | ☐ |
| Demo可演示 | 商品趋势问答Demo正常运行 | ☐ |

---

## 三、D19 测试用例：收尾与准备

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D19-TC01 | 所有服务健康检查 | 执行健康检查脚本 | 全部healthy | P0 |
| D19-TC02 | 文档完整性 | 检查必需文档 | 文档齐全 | P0 |
| D19-TC03 | 代码覆盖率 | 运行pytest --cov | ≥70% | P1 |
| D19-TC04 | 环境配置检查 | 检查.env配置 | 配置完整 | P0 |
| D19-TC05 | Demo端到端测试 | 执行完整Demo流程 | 流程正常 | P0 |

### 3.2 自动化测试脚本

```python
class TestD19_M1Preparation:
    """
    D19 M1里程碑准备验证测试。
    
    验证:
        - 所有服务健康
        - 文档完整
        - Demo可演示
    """
    
    def test_all_health_checks(self):
        """验证所有健康检查通过。"""
        import asyncio
        from src.infrastructure.database import check_db_health
        from src.infrastructure.redis import check_redis_health
        
        db_health = asyncio.run(check_db_health())
        redis_health = asyncio.run(check_redis_health())
        
        # 在无实际服务时，至少函数可执行
        assert isinstance(db_health, dict)
        assert isinstance(redis_health, dict)
    
    def test_required_docs_exist(self):
        """验证必需文档存在。"""
        import os
        required_docs = [
            "README.md",
            "docs/architecture/k8s-topology-v1.0.md",
        ]
        for doc in required_docs:
            assert os.path.isfile(doc), f"文档缺失: {doc}"
    
    def test_env_example_exists(self):
        """验证.env.example存在。"""
        import os
        assert os.path.isfile(".env.example")
```

---

## 四、D20 测试用例：M1里程碑评审

### 4.1 里程碑验收测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D20-TC01 | K8s集群验收 | `kubectl get nodes` | 全部Ready | P0 |
| D20-TC02 | vLLM验收 | 调用vLLM API | 推理正常 | P0 |
| D20-TC03 | Qdrant验收 | Qdrant集群健康检查 | 集群健康 | P0 |
| D20-TC04 | 爬虫验收 | 执行爬虫测试 | 数据采集成功 | P0 |
| D20-TC05 | Demo验收 | 执行Demo完整流程 | 端到端通过 | P0 |

### 4.2 自动化测试脚本

```python
class TestD20_M1Milestone:
    """
    D20 M1里程碑评审验证测试。
    
    验收标准:
        - K8s集群可用（Master+Worker+GPU）
        - vLLM单节点运行（Qwen2.5-72B）
        - Qdrant集群部署（3节点Raft）
        - Amazon BSR爬虫可采集数据
        - "商品趋势问答"Demo可演示
    """
    
    def test_m1_infrastructure_ready(self):
        """验证基础设施就绪。"""
        # 检查基础设施模块可导入
        from src.infrastructure import database, redis
        assert database is not None
        assert redis is not None
    
    def test_m1_services_ready(self):
        """验证服务层就绪。"""
        from src.services import llm_gateway, embedding
        assert llm_gateway is not None
        assert embedding is not None
    
    def test_m1_crawler_ready(self):
        """验证爬虫就绪。"""
        from src.crawlers import amazon
        assert amazon is not None
    
    def test_m1_rag_ready(self):
        """验证RAG就绪。"""
        from src.rag import engine, retriever
        assert engine is not None
        assert retriever is not None
    
    def test_m1_api_ready(self):
        """验证API就绪。"""
        from src.api.routes import demo
        assert demo is not None
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 安装测试依赖
pip install pytest pytest-asyncio pytest-cov

# 配置环境变量
export APP_ENVIRONMENT="development"
```

### 5.2 执行命令

```bash
# 执行D19-D20全部测试
pytest tests/test_d19_d20.py -v

# 生成覆盖率报告
pytest tests/test_d19_d20.py --cov=src --cov-report=html

# 运行M1里程碑验收
pytest tests/ -v --tb=short -m "m1"
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| M1里程碑 | 通过 | ☐ |
| 测试通过率 | ≥95% | ____% |
| 代码覆盖率 | ≥70% | ____% |
| Demo可用 | 是 | ____ |

---

## 六、Phase 1 总结

### 6.1 已完成任务

| 日期 | 任务 | 状态 |
|------|------|------|
| D1-D3 | 项目启动与架构设计 | ✅ |
| D4-D6 | Worker节点与网络配置 | ✅ |
| D7-D9 | 向量库与消息队列 | ✅ |
| D10-D12 | Embedding与数据采集 | ✅ |
| D13-D15 | RAG框架搭建 | ✅ |
| D16-D18 | Demo开发 | ✅ |
| D19-D20 | M1里程碑验收 | ✅ |

### 6.2 下一阶段预告

**Phase 2 (D21-D50)**: Multi-Agent集成
- LangGraph4j框架集成
- 四Agent协同流程
- vLLM多节点扩展
- 前端V1开发

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
