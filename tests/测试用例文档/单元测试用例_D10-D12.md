# 单元测试用例文档 D10-D12: Embedding与数据采集

> **时间范围**: 第10-12个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D10 | Embedding服务与爬虫启动 | 功能验证 | 8 |
| D11 | Rerank与爬虫完善 | 功能验证 | 7 |
| D12 | Flink与ETL | 功能验证 | 6 |

**总计**: 21个测试用例

---

## 二、D10 测试用例：Embedding服务与爬虫启动

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D10-TC01 | BGE Embedding模型加载 | 调用Embedding API | 模型响应正常 | P0 |
| D10-TC02 | Embedding向量维度 | 获取embedding向量 | 1024维 | P0 |
| D10-TC03 | Embedding批量处理 | 发送批量文本 | 返回批量向量 | P0 |
| D10-TC04 | Embedding性能 | 测量延迟 | <100ms | P1 |
| D10-TC05 | Amazon爬虫初始化 | 检查爬虫配置 | 配置正确 | P0 |
| D10-TC06 | Amazon BSR页面抓取 | 执行测试抓取 | 返回HTML | P0 |
| D10-TC07 | 爬虫速率限制 | 检查rate limit配置 | 已配置 | P1 |
| D10-TC08 | 爬虫代理配置 | 检查代理设置 | 已配置(可选) | P2 |

### 2.2 自动化测试脚本

```python
class TestD10_EmbeddingAndCrawler:
    """
    D10 Embedding服务与爬虫启动验证测试。
    
    验证:
        - Embedding模型正常工作
        - 向量维度正确
        - 爬虫可抓取数据
    """
    
    def test_embedding_service_import(self):
        """验证Embedding服务模块可导入。"""
        from src.services.embedding import EmbeddingService
        assert EmbeddingService is not None
    
    def test_embedding_service_config(self):
        """验证Embedding配置正确。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.embedding.model_name is not None
        assert settings.embedding.dimension > 0
    
    def test_embedding_dimension_default(self):
        """验证默认Embedding维度为1024。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.embedding.dimension == 1024
    
    def test_crawler_settings_import(self):
        """验证爬虫配置模块可导入。"""
        from src.crawlers import BaseCrawler
        assert BaseCrawler is not None
    
    def test_crawler_rate_limit_config(self):
        """验证爬虫速率限制配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert hasattr(settings, 'crawler') or hasattr(settings, 'scraping')
```

---

## 三、D11 测试用例：Rerank与爬虫完善

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D11-TC01 | Rerank模型加载 | 调用Rerank API | 模型响应正常 | P0 |
| D11-TC02 | Rerank重排序 | 发送query+documents | 返回排序结果 | P0 |
| D11-TC03 | Rerank分数范围 | 检查分数 | [0,1]范围 | P0 |
| D11-TC04 | Amazon评论爬虫 | 抓取评论数据 | 返回评论列表 | P0 |
| D11-TC05 | 评论数据清洗 | 检查清洗后数据 | 格式正确 | P0 |
| D11-TC06 | 爬虫错误处理 | 模拟错误场景 | 优雅处理 | P1 |
| D11-TC07 | 爬虫日志记录 | 检查日志输出 | 日志完整 | P1 |

### 3.2 自动化测试脚本

```python
class TestD11_RerankAndCrawlerEnhance:
    """
    D11 Rerank与爬虫完善验证测试。
    
    验证:
        - Rerank模型正常工作
        - 重排序结果正确
        - 评论爬虫功能完整
    """
    
    def test_rerank_service_import(self):
        """验证Rerank服务模块可导入。"""
        from src.services.rerank import RerankService
        assert RerankService is not None
    
    def test_rerank_config(self):
        """验证Rerank配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.rerank.model_name is not None
    
    def test_amazon_crawler_import(self):
        """验证Amazon爬虫模块可导入。"""
        from src.crawlers.amazon import AmazonCrawler
        assert AmazonCrawler is not None
```

---

## 四、D12 测试用例：Flink与ETL

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D12-TC01 | Flink集群连接 | 检查JobManager | Web UI可访问 | P0 |
| D12-TC02 | Flink Job提交 | 提交测试Job | Job运行成功 | P0 |
| D12-TC03 | Flink Checkpoint | 检查checkpoint配置 | 已启用 | P1 |
| D12-TC04 | ETL数据清洗 | 执行清洗Pipeline | 输出正确 | P0 |
| D12-TC05 | ETL去重功能 | 插入重复数据 | 自动去重 | P0 |
| D12-TC06 | ETL格式归一化 | 检查输出格式 | 格式统一 | P1 |

### 4.2 自动化测试脚本

```python
class TestD12_FlinkAndETL:
    """
    D12 Flink与ETL验证测试。
    
    验证:
        - Flink集群正常运行
        - ETL Pipeline正常工作
        - 数据清洗功能完整
    """
    
    def test_etl_pipeline_import(self):
        """验证ETL Pipeline模块可导入。"""
        from src.etl.pipeline import ETLPipeline
        assert ETLPipeline is not None
    
    def test_data_cleaner_import(self):
        """验证数据清洗模块可导入。"""
        from src.etl.cleaner import DataCleaner
        assert DataCleaner is not None
    
    def test_flink_settings(self):
        """验证Flink配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        # Flink配置可选
        pass
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 1. 安装测试依赖
pip install pytest pytest-asyncio sentence-transformers

# 2. 配置环境变量
export EMBEDDING_MODEL="BAAI/bge-large-zh"
export RERANK_MODEL="BAAI/bge-reranker-v2-m3"
```

### 5.2 执行命令

```bash
# 执行D10-D12全部测试
pytest tests/test_d10_d12.py -v

# 生成覆盖率报告
pytest tests/test_d10_d12.py --cov=src --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| Embedding延迟 | <100ms | ____ms |
| 爬虫可用 | 正常 | ____ |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/services/embedding.py` | Embedding服务 |
| `src/services/rerank.py` | Rerank服务 |
| `src/crawlers/amazon.py` | Amazon爬虫 |
| `src/etl/pipeline.py` | ETL Pipeline |
| `src/etl/cleaner.py` | 数据清洗器 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
