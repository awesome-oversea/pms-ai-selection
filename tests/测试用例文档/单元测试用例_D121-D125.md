# 单元测试用例文档: D121-D125

> **时间范围**: 第121-125个工作日
> **所属阶段**: Phase 4 - 用户培训与试运行
> **文档版本**: v1.0
> **生成日期**: 2026-04-06

---

## 一、测试概述

### 1.1 测试范围

| 模块 | 测试类 | 测试用例数 |
|------|--------|-----------|
| 培训材料 | TestTrainingMaterial | 2 |
| 培训会话 | TestTrainingSession | 2 |
| 问题 | TestIssue | 2 |
| 试运行统计 | TestTrialRunStats | 2 |
| 培训管理器 | TestTrainingManager | 7 |
| 试运行管理器 | TestTrialRunManager | 6 |
| **总计** | | **19** |

---

## 二、测试用例详情

### 2.1 TestTrainingMaterial - 培训材料

#### TC_001: 培训材料创建

```python
def test_material_creation(self):
    material = TrainingMaterial(
        material_id="MATERIAL_001",
        title="系统使用手册",
        material_type=MaterialType.DOCUMENTATION,
    )
    assert material.material_id == "MATERIAL_001"
```

#### TC_002: 材料类型

```python
def test_material_types(self):
    material = TrainingMaterial(...)
    assert material.material_type in [MaterialType.DOCUMENTATION, MaterialType.VIDEO, MaterialType.LAB]
```

---

### 2.2 TestTrainingSession - 培训会话

#### TC_003: 会话创建

```python
def test_session_creation(self):
    session = TrainingSession(
        session_id="SESSION_001",
        course_type=CourseType.INITIAL,
        trainer=" trainer001",
    )
    assert session.status == SessionStatus.SCHEDULED
```

#### TC_004: 会话状态

```python
def test_session_status(self):
    session = TrainingSession(...)
    session.start()
    assert session.status == SessionStatus.IN_PROGRESS
```

---

### 2.3 TestIssue - 问题

#### TC_005: 问题创建

```python
def test_issue_creation(self):
    issue = Issue(
        issue_id="ISSUE_001",
        title="无法登录系统",
        issue_type=IssueType.BUG,
        priority=IssuePriority.HIGH,
    )
    assert issue.status == IssueStatus.OPEN
```

#### TC_006: 问题优先级

```python
def test_issue_priority(self):
    issue = Issue(...)
    assert issue.priority in [IssuePriority.LOW, IssuePriority.MEDIUM, IssuePriority.HIGH, IssuePriority.CRITICAL]
```

---

### 2.4 TestTrialRunStats - 试运行统计

#### TC_007: 统计创建

```python
def test_stats_creation(self):
    stats = TrialRunStats(
        period="2026-Q1",
        total_users=100,
        active_users=85,
    )
    assert stats.active_rate == 0.85
```

#### TC_008: 统计计算

```python
def test_stats_calculation(self):
    stats = TrialRunStats(...)
    assert stats.inactive_users == stats.total_users - stats.active_users
```

---

### 2.5 TestTrainingManager - 培训管理器

#### TC_009: 创建培训材料

```python
def test_create_material(self):
    material = _run_async(self.manager.create_material(
        title="系统使用手册",
        material_type=MaterialType.DOCUMENTATION,
        content="手册内容...",
    ))
    assert material.material_id.startswith("MATERIAL_")
```

#### TC_010: 创建培训会话

```python
def test_create_session(self):
    session = _run_async(self.manager.create_session(
        course_type=CourseType.INITIAL,
        trainer="trainer001",
        scheduled_time=datetime.now(),
    ))
    assert session.session_id.startswith("SESSION_")
```

#### TC_011: 启动会话

```python
def test_start_session(self):
    session = _run_async(self.manager.create_session(...))
    started = _run_async(self.manager.start_session(session.session_id))
    assert started is True
```

#### TC_012: 完成会话

```python
def test_complete_session(self):
    session = _run_async(self.manager.create_session(...))
    completed = _run_async(self.manager.complete_session(session.session_id))
    assert completed is True
```

#### TC_013: 记录问题

```python
def test_report_issue(self):
    issue = _run_async(self.manager.report_issue(
        title="无法登录",
        issue_type=IssueType.BUG,
    ))
    assert issue.issue_id.startswith("ISSUE_")
```

#### TC_014: 更新问题状态

```python
def test_update_issue_status(self):
    issue = _run_async(self.manager.report_issue(...))
    updated = _run_async(self.manager.update_issue_status(
        issue.issue_id,
        IssueStatus.RESOLVED,
    ))
    assert updated.status == IssueStatus.RESOLVED
```

#### TC_015: 生成培训报告

```python
def test_generate_training_report(self):
    report = _run_async(self.manager.generate_training_report())
    assert "total_sessions" in report
    assert "total_materials" in report
```

---

### 2.6 TestTrialRunManager - 试运行管理器

#### TC_016: 创建试运行

```python
def test_create_trial_run(self):
    trial = _run_async(self.manager.create_trial_run(
        name="生产环境试运行",
        start_date=date.today(),
        end_date=date.today(),
    ))
    assert trial.trial_id.startswith("TRIAL_")
```

#### TC_017: 添加用户

```python
def test_add_user(self):
    trial = _run_async(self.manager.create_trial_run(...))
    user = _run_async(self.manager.add_user(trial.trial_id, "user001"))
    assert user is True
```

#### TC_018: 记录活跃用户

```python
def test_record_active_user(self):
    trial = _run_async(self.manager.create_trial_run(...))
    recorded = _run_async(self.manager.record_active_user(trial.trial_id, "user001"))
    assert recorded is True
```

#### TC_019: 获取统计数据

```python
def test_get_stats(self):
    trial = _run_async(self.manager.create_trial_run(...))
    stats = _run_async(self.manager.get_stats(trial.trial_id))
    assert "total_users" in stats
```

#### TC_020: 获取用户列表

```python
def test_get_user_list(self):
    trial = _run_async(self.manager.create_trial_run(...))
    users = _run_async(self.manager.get_user_list(trial.trial_id))
    assert isinstance(users, list)
```

#### TC_021: 生成试运行报告

```python
def test_generate_trial_report(self):
    trial = _run_async(self.manager.create_trial_run(...))
    report = _run_async(self.manager.generate_trial_report(trial.trial_id))
    assert "summary" in report
    assert "issues" in report
```

---

## 三、测试用例统计

| 测试类 | 用例数 | 覆盖功能 |
|--------|--------|---------|
| TestTrainingMaterial | 2 | 培训材料数据模型 |
| TestTrainingSession | 2 | 培训会话数据模型 |
| TestIssue | 2 | 问题数据模型 |
| TestTrialRunStats | 2 | 试运行统计数据模型 |
| TestTrainingManager | 7 | 培训管理 |
| TestTrialRunManager | 6 | 试运行管理 |
| **总计** | **21** | |

---

## 四、测试环境

- **Python版本**: 3.12.10
- **测试框架**: pytest
- **被测模块**: src.infrastructure.training

---

**文档版本**: v1.0
**最后更新**: 2026-04-06
**验收状态**: ✅ 通过
