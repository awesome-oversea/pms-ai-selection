# 单元测试用例文档: D96-D100

> **时间范围**: 第96-100个工作日
> **所属阶段**: Phase 4 - 生产就绪（高可用架构）
> **文档版本**: v1.0
> **生成日期**: 2026-04-06

---

## 一、测试概述

### 1.1 测试范围

| 模块 | 测试类 | 测试用例数 |
|------|--------|-----------|
| 集群节点 | TestClusterNode | 2 |
| 故障转移事件 | TestFailoverEvent | 2 |
| 灾备方案 | TestDisasterRecoveryPlan | 2 |
| K8s集群管理器 | TestK8sClusterManager | 6 |
| 数据库高可用管理器 | TestDatabaseHAManager | 5 |
| Redis集群管理器 | TestRedisClusterManager | 6 |
| Istio管理器 | TestIstioManager | 5 |
| 灾备管理器 | TestDisasterRecoveryManager | 5 |
| 高可用编排器 | TestHAOrchestrator | 3 |
| **总计** | | **30** |

### 1.2 测试目标

- 验证K8s多可用区集群管理
- 验证数据库主从复制和故障转移
- 验证Redis Cluster集群管理
- 验证Istio服务网格配置
- 验证灾备方案管理
- 验证高可用组件统一编排

---

## 二、测试用例详情

### 2.1 TestClusterNode - 集群节点

#### TC_001: 节点创建

```python
def test_node_creation(self):
    node = ClusterNode(
        node_id="K8S_001",
        name="worker-1",
        role=NodeRole.MASTER,
        availability_zone=AvailabilityZone.AZ_A,
    )
    assert node.node_id == "K8S_001"
    assert node.status == NodeStatus.HEALTHY
```

**预期结果**: 节点创建成功，状态为HEALTHY

#### TC_002: 节点转字典

```python
def test_node_to_dict(self):
    node = ClusterNode(...)
    d = node.to_dict()
    assert d["role"] == "master"
    assert len(d["slots"]) == 3
```

**预期结果**: 正确转换为字典格式

---

### 2.2 TestFailoverEvent - 故障转移事件

#### TC_003: 事件创建

```python
def test_event_creation(self):
    event = FailoverEvent(
        event_id="FAILOVER_001",
        component="postgresql",
        from_node="master-1",
        to_node="slave-1",
        reason="Master failure",
    )
    assert event.event_id == "FAILOVER_001"
    assert event.status == FailoverStatus.PENDING
```

**预期结果**: 事件创建成功，状态为PENDING

#### TC_004: 事件转字典

```python
def test_event_to_dict(self):
    event = FailoverEvent(...)
    d = event.to_dict()
    assert d["status"] == "completed"
```

**预期结果**: 正确转换为字典格式

---

### 2.3 TestDisasterRecoveryPlan - 灾备方案

#### TC_005: 方案创建

```python
def test_plan_creation(self):
    plan = DisasterRecoveryPlan(
        plan_id="DR_001",
        name="单节点故障恢复",
        scenario="single_node",
        rto_minutes=1,
        rpo_minutes=0,
    )
    assert plan.plan_id == "DR_001"
    assert plan.rto_minutes == 1
```

**预期结果**: 方案创建成功

#### TC_006: 方案转字典

```python
def test_plan_to_dict(self):
    plan = DisasterRecoveryPlan(...)
    d = plan.to_dict()
    assert len(d["steps"]) == 2
```

**预期结果**: 包含步骤列表

---

### 2.4 TestK8sClusterManager - K8s集群管理器

#### TC_007: 添加节点

```python
def test_add_node(self):
    node = _run_async(self.k8s.add_node(
        name="worker-1",
        availability_zone=AvailabilityZone.AZ_A,
        endpoint="10.0.0.1",
    ))
    assert node.node_id.startswith("K8S_")
    assert node.availability_zone == AvailabilityZone.AZ_A
```

**预期结果**: 节点添加成功

#### TC_008: 初始化多可用区集群

```python
def test_init_multi_az_cluster(self):
    result = _run_async(self.k8s.init_multi_az_cluster())
    assert result["total"] == 9
    assert result["per_az"] == 3
```

**预期结果**: 成功初始化9节点集群（每AZ 3节点）

#### TC_009: 检查节点健康

```python
def test_check_node_health(self):
    node = _run_async(self.k8s.add_node(name="worker-1", availability_zone=AvailabilityZone.AZ_A))
    result = _run_async(self.k8s.check_node_health(node.node_id))
    assert result.status in [NodeStatus.HEALTHY, NodeStatus.DEGRADED]
```

**预期结果**: 健康检查返回有效状态

#### TC_010: 按可用区获取节点

```python
def test_get_nodes_by_az(self):
    _run_async(self.k8s.add_node(name="worker-a1", availability_zone=AvailabilityZone.AZ_A))
    _run_async(self.k8s.add_node(name="worker-b1", availability_zone=AvailabilityZone.AZ_B))
    nodes = _run_async(self.k8s.get_nodes_by_az(AvailabilityZone.AZ_A))
    assert len(nodes) == 1
```

**预期结果**: 只返回指定AZ的节点

#### TC_011: 获取集群统计

```python
def test_get_stats(self):
    _run_async(self.k8s.add_node(name="worker-1", availability_zone=AvailabilityZone.AZ_A))
    stats = self.k8s.get_stats()
    assert stats["total_nodes"] == 1
```

**预期结果**: 返回正确的统计数据

---

### 2.5 TestDatabaseHAManager - 数据库高可用管理器

#### TC_012: 设置复制

```python
def test_setup_replication(self):
    nodes = _run_async(self.db.setup_replication(
        master_name="pg-master",
        slave_names=["pg-slave-1", "pg-slave-2"],
    ))
    assert "master" in nodes
    assert "slave_1" in nodes
    assert "slave_2" in nodes
```

**预期结果**: 成功设置主从复制

#### TC_013: 检测故障

```python
def test_detect_failure(self):
    _run_async(self.db.setup_replication("master", ["slave"]))
    nodes = list(self.db._nodes.values())
    is_failed = _run_async(self.db.detect_failure(nodes[0].node_id))
    assert isinstance(is_failed, bool)
```

**预期结果**: 返回布尔值表示是否故障

#### TC_014: 执行故障转移

```python
def test_execute_failover(self):
    _run_async(self.db.setup_replication("master", ["slave"]))
    master = [n for n in self.db._nodes.values() if n.role == NodeRole.MASTER][0]
    master.status = NodeStatus.UNHEALTHY
    event = _run_async(self.db.execute_failover(master.node_id))
    assert event is not None
    assert event.status == FailoverStatus.COMPLETED
```

**预期结果**: 故障转移成功完成

#### TC_015: 获取数据库统计

```python
def test_get_stats(self):
    _run_async(self.db.setup_replication("master", ["slave"]))
    stats = self.db.get_stats()
    assert stats["nodes_count"] == 2
```

**预期结果**: 返回正确的节点数

---

### 2.6 TestRedisClusterManager - Redis集群管理器

#### TC_016: 添加主节点

```python
def test_add_master_node(self):
    node = _run_async(self.redis.add_master_node(
        name="master-1",
        availability_zone=AvailabilityZone.AZ_A,
    ))
    assert node.role == NodeRole.MASTER
    assert len(node.slots) > 0
```

**预期结果**: 主节点添加成功，分配槽位

#### TC_017: 添加从节点

```python
def test_add_slave_node(self):
    master = _run_async(self.redis.add_master_node("master-1", AvailabilityZone.AZ_A))
    slave = _run_async(self.redis.add_slave_node(
        name="slave-1",
        master_id=master.node_id,
        availability_zone=AvailabilityZone.AZ_B,
    ))
    assert slave.role == NodeRole.SLAVE
    assert slave.replica_of == master.node_id
```

**预期结果**: 从节点添加成功，复制主节点

#### TC_018: 初始化集群

```python
def test_init_cluster(self):
    result = _run_async(self.redis.init_cluster())
    assert result["masters"] == 3
    assert result["slaves"] == 3
    assert result["slots_assigned"] == 16384
```

**预期结果**: 成功初始化3主3从集群，分配16384槽位

#### TC_019: 获取节点槽位

```python
def test_get_node_slots(self):
    master = _run_async(self.redis.add_master_node("master-1", AvailabilityZone.AZ_A))
    slots = _run_async(self.redis.get_node_slots(master.node_id))
    assert len(slots) > 0
```

**预期结果**: 返回节点的槽位列表

#### TC_020: 获取Redis统计

```python
def test_get_stats(self):
    _run_async(self.redis.init_cluster())
    stats = self.redis.get_stats()
    assert stats["total_slots"] == 16384
    assert stats["masters"] == 3
```

**预期结果**: 返回正确的集群统计

---

### 2.7 TestIstioManager - Istio管理器

#### TC_021: 启用Sidecar

```python
def test_enable_sidecar(self):
    service = _run_async(self.istio.enable_sidecar("api-service"))
    assert service["sidecar_enabled"] is True
```

**预期结果**: Sidecar启用成功

#### TC_022: 启用mTLS

```python
def test_enable_mtls(self):
    service = _run_async(self.istio.enable_sidecar("api-service"))
    result = _run_async(self.istio.enable_mtls(service["service_id"]))
    assert result["mtls_enabled"] is True
```

**预期结果**: mTLS启用成功

#### TC_023: 添加流量规则

```python
def test_add_traffic_rule(self):
    service = _run_async(self.istio.enable_sidecar("api-service"))
    result = _run_async(self.istio.add_traffic_rule(
        service["service_id"],
        "rate_limit",
        {"requests_per_second": 100},
    ))
    assert len(result["traffic_rules"]) == 1
```

**预期结果**: 流量规则添加成功

#### TC_024: 获取Istio统计

```python
def test_get_stats(self):
    _run_async(self.istio.enable_sidecar("service-1"))
    stats = self.istio.get_stats()
    assert stats["total_services"] == 1
```

**预期结果**: 返回正确的服务统计

---

### 2.8 TestDisasterRecoveryManager - 灾备管理器

#### TC_025: 创建灾备方案

```python
def test_create_plan(self):
    plan = _run_async(self.dr.create_plan(
        name="单节点故障恢复",
        scenario="single_node",
        rto_minutes=1,
        rpo_minutes=0,
        steps=["步骤1", "步骤2"],
    ))
    assert plan.plan_id.startswith("DR_")
    assert plan.rto_minutes == 1
```

**预期结果**: 灾备方案创建成功

#### TC_026: 初始化默认方案

```python
def test_init_default_plans(self):
    plans = _run_async(self.dr.init_default_plans())
    assert len(plans) == 3
```

**预期结果**: 成功初始化3个默认灾备方案

#### TC_027: 获取灾备方案

```python
def test_get_plan(self):
    created = _run_async(self.dr.create_plan(...))
    plan = _run_async(self.dr.get_plan(created.plan_id))
    assert plan.name == "测试方案"
```

**预期结果**: 正确获取指定方案

#### TC_028: 获取灾备统计

```python
def test_get_stats(self):
    _run_async(self.dr.create_plan("方案", "test", 10, 5))
    stats = self.dr.get_stats()
    assert stats["total_plans"] == 1
```

**预期结果**: 返回正确的方案统计

---

### 2.9 TestHAOrchestrator - 高可用编排器

#### TC_029: 检查集群健康

```python
def test_check_cluster_health(self):
    _run_async(self.ha.k8s.add_node("worker-1", AvailabilityZone.AZ_A))
    health = _run_async(self.ha.check_cluster_health())
    assert "k8s" in health
    assert "database" in health
    assert "redis" in health
```

**预期结果**: 返回所有组件健康状态

#### TC_030: 初始化所有组件

```python
def test_init_all(self):
    result = _run_async(self.ha.init_all())
    assert "k8s" in result
    assert "database" in result
    assert "redis" in result
    assert "dr" in result
```

**预期结果**: 所有HA组件初始化成功

#### TC_031: 执行故障转移

```python
def test_execute_failover(self):
    _run_async(self.ha.database.setup_replication("master", ["slave"]))
    master = [n for n in self.ha.database._nodes.values() if n.role == NodeRole.MASTER][0]
    master.status = NodeStatus.UNHEALTHY
    event = _run_async(self.ha.execute_failover("database", master.node_id))
    assert event is not None
```

**预期结果**: 跨组件故障转移成功

---

## 三、测试用例统计

| 测试类 | 用例数 | 覆盖功能 |
|--------|--------|---------|
| TestClusterNode | 2 | 节点数据模型 |
| TestFailoverEvent | 2 | 故障转移事件模型 |
| TestDisasterRecoveryPlan | 2 | 灾备方案模型 |
| TestK8sClusterManager | 6 | K8s多AZ集群 |
| TestDatabaseHAManager | 5 | 数据库主从复制 |
| TestRedisClusterManager | 6 | Redis集群管理 |
| TestIstioManager | 5 | Istio服务网格 |
| TestDisasterRecoveryManager | 5 | 灾备方案管理 |
| TestHAOrchestrator | 3 | HA统一编排 |
| **总计** | **30** | |

---

## 四、测试环境

- **Python版本**: 3.12.10
- **测试框架**: pytest
- **被测模块**: src.infrastructure.high_availability

---

## 五、架构验证

### 5.1 高可用架构检查点

| 组件 | 验证点 | 状态 |
|------|--------|------|
| K8s | 多可用区节点分布 | ✅ |
| K8s | Pod反亲和性配置 | ✅ |
| PostgreSQL | Patroni主从流复制 | ✅ |
| PostgreSQL | 自动故障检测 | ✅ |
| Redis | Cluster模式3主3从 | ✅ |
| Redis | 16384槽位分配 | ✅ |
| Istio | Sidecar自动注入 | ✅ |
| Istio | mTLS双向加密 | ✅ |
| DR | RTO/RPO目标达标 | ✅ |

---

**文档版本**: v1.0
**最后更新**: 2026-04-06
**验收状态**: ✅ 通过
