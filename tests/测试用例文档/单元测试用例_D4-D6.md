# 单元测试用例文档 D4-D6: Worker节点与网络配置

> **时间范围**: 第4-6个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D4 | Worker节点部署 | 功能验证 | 8 |
| D5 | PostgreSQL主从集群 | 功能验证 | 12 |
| D6 | Redis Sentinel集群 | 功能验证 | 10 |

**总计**: 30个测试用例

---

## 二、D4 测试用例：Worker节点部署

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D4-TC01 | 应用Worker节点Ready | `kubectl get nodes -l node-role.kubernetes.io/worker=` | 3节点状态Ready | P0 |
| D4-TC02 | GPU Worker节点Ready | `kubectl get nodes -l node-role.kubernetes.io/gpu=` | 2节点状态Ready | P0 |
| D4-TC03 | NVIDIA驱动安装 | `nvidia-smi` | GPU信息正常显示 | P0 |
| D4-TC04 | NVIDIA Device Plugin | `kubectl get ds -n kube-system nvidia-device-plugin` | DaemonSet运行中 | P0 |
| D4-TC05 | GPU可调度 | 创建测试Pod请求GPU | Pod成功调度到GPU节点 | P0 |
| D4-TC06 | MIG配置(可选) | `nvidia-smi mig -cgi` | MIG实例已创建 | P1 |
| D4-TC07 | 节点标签正确 | `kubectl get nodes --show-labels` | GPU/Worker标签正确 | P1 |
| D4-TC08 | 污点容忍配置 | 检查GPU节点污点 | nvidia.com/gpu污点存在 | P1 |

### 2.2 自动化测试脚本

```python
class TestD4_WorkerNodeDeployment:
    """
    D4 Worker节点部署验证测试。
    
    验证:
        - 应用服务Worker节点就绪
        - GPU Worker节点就绪
        - NVIDIA Device Plugin正常工作
        - GPU资源可被Pod调度
    """
    
    def test_k8s_deployment_has_namespace(self):
        """验证deployment.yml定义了pms-system命名空间。"""
        docs = self._load_yaml("deployment.yml")
        ns_doc = next(d for d in docs if d and d.get("kind") == "Namespace")
        assert ns_doc["metadata"]["name"] == "pms-system"
    
    def test_k8s_deployment_has_configmap(self):
        """验证deployment.yml包含ConfigMap环境变量配置。"""
        docs = self._load_yaml("deployment.yml")
        cm_docs = [d for d in docs if d and d.get("kind") == "ConfigMap"]
        assert len(cm_docs) >= 1
        data = cm_docs[0]["data"]
        assert "APP_ENVIRONMENT" in data
        assert "DATABASE_URL" in data
    
    def test_k8s_deployment_replicas(self):
        """验证应用Deployment配置3副本。"""
        docs = self._load_yaml("deployment.yml")
        dep = next(d for d in docs if d and d.get("kind") == "Deployment")
        assert dep["spec"]["replicas"] == 3
    
    def test_k8s_deployment_has_liveness_probe(self):
        """验证应用容器配置了liveness探针。"""
        docs = self._load_yaml("deployment.yml")
        dep = next(d for d in docs if d and d.get("kind") == "Deployment")
        container = dep["spec"]["template"]["spec"]["containers"][0]
        assert "livenessProbe" in container
        assert container["livenessProbe"]["httpGet"]["path"] == "/health"
    
    def test_k8s_deployment_has_readiness_probe(self):
        """验证应用容器配置了readiness探针。"""
        docs = self._load_yaml("deployment.yml")
        dep = next(d for d in docs if d and d.get("kind") == "Deployment")
        container = dep["spec"]["template"]["spec"]["containers"][0]
        assert "readinessProbe" in container
        assert container["readinessProbe"]["httpGet"]["path"] == "/ready"
    
    def test_k8s_deployment_has_hpa(self):
        """验证配置了HPA自动扩缩容(3-20副本)。"""
        docs = self._load_yaml("deployment.yml")
        hpa = next((d for d in docs if d and d.get("kind") == "HorizontalPodAutoscaler"), None)
        assert hpa is not None
        assert hpa["spec"]["minReplicas"] == 3
        assert hpa["spec"]["maxReplicas"] == 20
    
    def test_gpu_nvidia_device_plugin_daemonset(self):
        """验证NVIDIA Device Plugin以DaemonSet部署。"""
        docs = self._load_yaml("gpu.yml")
        ds = next((d for d in docs if d and d.get("kind") == "DaemonSet"), None)
        assert ds is not None
        tolerations = ds["spec"]["template"]["spec"]["tolerations"]
        has_gpu_taint = any(
            t.get("key") == "nvidia.com/gpu" or t.get("key", "").startswith("nvidia")
            for t in tolerations
        )
        assert has_gpu_taint
    
    def test_gpu_resource_quota(self):
        """验证GPU ResourceQuota限制配置。"""
        docs = self._load_yaml("gpu.yml")
        quota = next((d for d in docs if d and d.get("kind") == "ResourceQuota"), None)
        assert quota is not None
        hard = quota["spec"]["hard"]
        assert "requests.nvidia.com/gpu" in hard
        assert "limits.nvidia.com/gpu" in hard
```

---

## 三、D5 测试用例：PostgreSQL主从集群

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D5-TC01 | PG主节点连接 | `psql -h primary -U postgres -c "SELECT 1"` | 连接成功 | P0 |
| D5-TC02 | PG从节点连接 | `psql -h replica -U postgres -c "SELECT 1"` | 连接成功 | P0 |
| D5-TC03 | 主从复制状态 | `SELECT * FROM pg_stat_replication` | 复制正常 | P0 |
| D5-TC04 | 从节点延迟 | `SELECT extract(epoch from now() - pg_last_xact_replay_timestamp())` | <1秒 | P0 |
| D5-TC05 | 数据同步测试 | 主节点写入，从节点读取 | 数据一致 | P0 |
| D5-TC06 | 连接池PgBouncer | `psql -h pgbouncer -p 6432` | 连接成功 | P1 |
| D5-TC07 | 主节点写入 | 在主节点执行INSERT | 成功 | P0 |
| D5-TC08 | 从节点只读 | 在从节点执行INSERT | 报错(只读) | P0 |
| D5-TC09 | 故障切换测试 | 停止主节点，观察从节点提升 | 自动提升 | P1 |
| D5-TC10 | 连接数限制 | 检查max_connections | ≥200 | P1 |
| D5-TC11 | 持久化存储 | 检查PVC状态 | Bound | P0 |
| D5-TC12 | 备份配置 | 检查备份Job | 备份任务存在 | P1 |

### 3.2 自动化测试脚本

```python
class TestD5_PostgreSQLCluster:
    """
    D5 PostgreSQL主从集群验证测试。
    
    验证:
        - 数据库引擎创建与配置
        - Session工厂(同步/异步)
        - 健康检查接口
        - 主从复制状态
    """
    
    def test_base_class_exists(self):
        """验证Base声明式基类可导入。"""
        from src.infrastructure.database import Base
        from sqlalchemy.orm import DeclarativeBase
        assert issubclass(Base, DeclarativeBase)
    
    def test_get_engine_returns_same_instance(self):
        """验证get_engine()返回单例。"""
        from src.infrastructure.database import get_engine, _engine
        engine1 = get_engine()
        engine2 = get_engine()
        assert engine1 is engine2
    
    def test_async_session_factory_creation(self):
        """验证异步Session工厂创建成功。"""
        from src.infrastructure.database import get_async_session_factory
        factory = get_async_session_factory()
        assert factory is not None
        assert hasattr(factory, '__call__')
    
    def test_database_settings_default_url(self):
        """验证默认数据库URL包含postgresql+asyncpg。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert "postgresql" in settings.database.url
        assert "asyncpg" in settings.database.url
    
    def test_database_pool_size_range(self):
        """验证连接池大小在合法范围[1,100]。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert 1 <= settings.database.pool_size <= 100
        assert 0 <= settings.database.max_overflow <= 50
    
    def test_check_db_health_returns_dict(self):
        """验证check_db_health()返回字典格式结果。"""
        import asyncio
        from src.infrastructure.database import check_db_health
        result = asyncio.run(check_db_health())
        assert isinstance(result, dict)
        assert "status" in result
    
    def test_close_db_resets_globals(self):
        """验证close_db()重置全局引擎引用。"""
        import asyncio
        from src.infrastructure.database import close_db
        asyncio.run(close_db())
        from src.infrastructure.database import _engine
        assert _engine is None
    
    def test_k8s_postgresql_primary_statefulset(self):
        """验证PG主节点为StatefulSet且使用PVC。"""
        docs = self._load_yaml("postgresql.yml")
        primary = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-pg-primary"),
            None,
        )
        assert primary is not None
        assert primary["kind"] == "StatefulSet"
        assert primary["spec"]["replicas"] == 1
        assert "volumeClaimTemplates" in primary["spec"]
    
    def test_k8s_postgresql_replica_count(self):
        """验证PG从节点为2个副本。"""
        docs = self._load_yaml("postgresql.yml")
        replica = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-pg-replica"),
            None,
        )
        assert replica is not None
        assert replica["spec"]["replicas"] == 2
    
    def test_k8s_postgresql_pgbouncer_deployment(self):
        """验证PgBouncer连接池代理Deployment存在。"""
        docs = self._load_yaml("postgresql.yml")
        pgbouncer = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-pgbouncer"),
            None,
        )
        assert pgbouncer is not None
        assert pgbouncer["kind"] == "Deployment"
        container = pgbouncer["spec"]["template"]["spec"]["containers"][0]
        assert container["ports"][0]["containerPort"] == 6432
    
    def test_k8s_postgresql_services(self):
        """验证PG有primary和replica两个独立Service。"""
        docs = self._load_yaml("postgresql.yml")
        services = [d for d in docs if d and d.get("kind") == "Service"]
        svc_names = {s["metadata"]["name"] for s in services}
        assert "pms-pg-primary-svc" in svc_names
        assert "pms-pg-replica-svc" in svc_names
```

---

## 四、D6 测试用例：Redis Sentinel集群

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D6-TC01 | Redis Master连接 | `redis-cli -h master ping` | PONG | P0 |
| D6-TC02 | Redis Slave连接 | `redis-cli -h slave ping` | PONG | P0 |
| D6-TC03 | Sentinel连接 | `redis-cli -h sentinel -p 26379 ping` | PONG | P0 |
| D6-TC04 | 主从复制状态 | `redis-cli info replication` | master_link_status:up | P0 |
| D6-TC05 | Sentinel监控 | `redis-cli -p 26379 sentinel master mymaster` | master信息正常 | P0 |
| D6-TC06 | 故障切换测试 | 停止Master，观察Sentinel切换 | 自动切换 | P0 |
| D6-TC07 | 切换时间 | 记录故障切换耗时 | <30秒 | P1 |
| D6-TC08 | 缓存读写测试 | SET/GET操作 | 正常 | P0 |
| D6-TC09 | 持久化配置 | 检查appendonly | yes | P1 |
| D6-TC10 | 连接数限制 | 检查maxclients | ≥10000 | P1 |

### 4.2 自动化测试脚本

```python
class TestD6_RedisSentinelCluster:
    """
    D6 Redis Sentinel集群验证测试。
    
    验证:
        - Redis客户端创建(单节点/Cluster)
        - CacheService高级封装
        - 健康检查接口
        - 主从切换能力
    """
    
    def test_redis_client_import(self):
        """验证Redis客户端模块可正确导入。"""
        from src.infrastructure.redis import (
            get_redis_connection,
            redis_client,
            CacheService,
            check_redis_health,
        )
        assert callable(get_redis_connection)
    
    def test_redis_settings_defaults(self):
        """验证Redis配置默认值合理。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert "redis://" in settings.redis.url
        assert settings.redis.cluster_mode is False
        assert 1 <= settings.redis.max_connections <= 200
    
    def test_cache_service_initialization(self):
        """验证CacheService接受redis客户端实例。"""
        from src.infrastructure.redis import CacheService
        from unittest.mock import MagicMock
        mock_redis = MagicMock()
        service = CacheService(mock_redis)
        assert service._redis is mock_redis
    
    def test_cache_service_methods_exist(self):
        """验证CacheService提供完整的缓存操作方法集。"""
        import asyncio
        from src.infrastructure.redis import CacheService
        from unittest.mock import AsyncMock, MagicMock
        
        mock_redis = MagicMock()
        mock_redis.get = AsyncMock(return_value="value")
        mock_redis.set = AsyncMock(return_value=True)
        mock_redis.delete = AsyncMock(return_value=1)
        mock_redis.exists = AsyncMock(return_value=1)
        mock_redis.expire = AsyncMock(return_value=True)
        mock_redis.ttl = AsyncMock(return_value=300)
        mock_redis.incrby = AsyncMock(return_value=5)
        mock_redis.hget = AsyncMock(return_value="field_val")
        mock_redis.hset = AsyncMock(return_value=True)
        mock_redis.hgetall = AsyncMock(return_value={"k": "v"})
        mock_redis.lpush = AsyncMock(return_value=2)
        mock_redis.lrange = AsyncMock(return_value=["a", "b"])
        mock_redis.aclose = AsyncMock()
        
        service = CacheService(mock_redis)
        assert asyncio.run(service.get("key")) == "value"
        assert asyncio.run(service.set("key", "val")) is True
        assert asyncio.run(service.delete("key")) == 1
    
    def test_check_redis_health_returns_dict(self):
        """验证check_redis_health()返回字典格式结果。"""
        import asyncio
        from src.infrastructure.redis import check_redis_health
        result = asyncio.run(check_redis_health())
        assert isinstance(result, dict)
        assert "status" in result
    
    def test_close_redis_resets_global(self):
        """验证close_redis()重置全局客户端引用。"""
        import asyncio
        from src.infrastructure.redis import close_redis
        asyncio.run(close_redis())
        from src.infrastructure.redis import _redis_client
        assert _redis_client is None
    
    def test_k8s_redis_master_statefulset(self):
        """验证Redis Master为StatefulSet(PVC持久化)。"""
        docs = self._load_yaml("redis.yml")
        master = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-redis-master"),
            None,
        )
        assert master is not None
        assert master["kind"] == "StatefulSet"
        assert "volumeClaimTemplates" in master["spec"]
    
    def test_k8s_redis_slave_replicas(self):
        """验证Redis Slave为2个副本。"""
        docs = self._load_yaml("redis.yml")
        slave = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-redis-slave"),
            None,
        )
        assert slave is not None
        assert slave["spec"]["replicas"] == 2
    
    def test_k8s_redis_sentinel_deployment(self):
        """验证Redis Sentinel为3个副本Deployment。"""
        docs = self._load_yaml("redis.yml")
        sentinel = next(
            (d for d in docs if d and d.get("metadata", {}).get("name") == "pms-redis-sentinel"),
            None,
        )
        assert sentinel is not None
        assert sentinel["spec"]["replicas"] == 3
    
    def test_k8s_redis_sentinel_monitor_config(self):
        """验证Sentinel配置监控mymaster。"""
        docs = self._load_yaml("redis.yml")
        configmap = next(
            (d for d in docs if d and d.get("kind") == "ConfigMap"),
            None,
        )
        assert configmap is not None
        sentinel_conf = configmap["data"]["sentinel.conf"]
        assert "sentinel monitor mymaster" in sentinel_conf
        assert "down-after-milliseconds" in sentinel_conf
```

---

## 五、Docker与Docker Compose验证

### 5.1 测试用例

| 用例ID | 测试名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| DC-TC01 | Dockerfile存在 | 检查Dockerfile文件 | 文件存在 | P0 |
| DC-TC02 | 多阶段构建 | 检查FROM指令数量 | ≥2个FROM | P0 |
| DC-TC03 | 非root用户 | 检查USER指令 | appuser | P0 |
| DC-TC04 | 健康检查 | 检查HEALTHCHECK指令 | 存在 | P1 |
| DC-TC05 | docker-compose存在 | 检查docker-compose.yml | 文件存在 | P0 |
| DC-TC06 | PG服务配置 | 检查postgres-primary服务 | 存在 | P0 |
| DC-TC07 | Redis服务配置 | 检查redis-master服务 | 存在 | P0 |
| DC-TC08 | Sentinel服务配置 | 检查redis-sentinel服务 | 存在 | P0 |
| DC-TC09 | App服务配置 | 检查app服务依赖 | depends_on正确 | P0 |
| DC-TC10 | 网络配置 | 检查networks定义 | pms-network存在 | P1 |

### 5.2 自动化测试脚本

```python
class TestDockerAndCompose:
    """
    Dockerfile和docker-compose验证测试。
    
    验证:
        - Dockerfile多阶段构建
        - docker-compose服务编排
        - 网络和卷配置
    """
    
    def test_dockerfile_exists(self):
        """验证Dockerfile存在于项目根目录。"""
        import os
        assert os.path.isfile("Dockerfile")
    
    def test_dockerfile_has_multistage_build(self):
        """验证Dockerfile使用多阶段构建。"""
        with open("Dockerfile", "r", encoding="utf-8") as f:
            content = f.read()
        assert "FROM python:3.11-slim AS builder" in content
        assert "FROM python:3.11-slim AS runtime" in content
        assert "COPY --from=builder" in content
    
    def test_dockerfile_non_root_user(self):
        """验证Dockerfile运行时使用非root用户。"""
        with open("Dockerfile", "r", encoding="utf-8") as f:
            content = f.read()
        assert "USER appuser" in content
        assert "groupadd -r appuser" in content
    
    def test_dockerfile_has_healthcheck(self):
        """验证Dockerfile有HEALTHCHECK指令。"""
        with open("Dockerfile", "r", encoding="utf-8") as f:
            content = f.read()
        assert "HEALTHCHECK" in content
        assert "/health" in content
    
    def test_dockerfile_exposes_port_8000(self):
        """验证Dockerfile暴露8000端口。"""
        with open("Dockerfile", "r", encoding="utf-8") as f:
            content = f.read()
        assert "EXPOSE 8000" in content
    
    def test_docker_compose_exists(self):
        """验证docker-compose.yml存在。"""
        import os
        assert os.path.isfile("docker-compose.yml")
    
    def test_docker_compose_has_pg_service(self):
        """验证docker-compose有PostgreSQL主节点服务。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        services = compose.get("services", {})
        assert "postgres-primary" in services
        pg = services["postgres-primary"]
        assert "image" in pg
        assert "environment" in pg
        assert "healthcheck" in pg
    
    def test_docker_compose_has_redis_service(self):
        """验证docker-compose有Redis Master服务。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        services = compose.get("services", {})
        assert "redis-master" in services
        redis = services["redis-master"]
        assert "healthcheck" in redis
    
    def test_docker_compose_has_sentinel(self):
        """验证docker-compose有Redis Sentinel服务。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        services = compose.get("services", {})
        assert "redis-sentinel-1" in services
    
    def test_docker_compose_has_app_service(self):
        """验证docker-compose有FMS应用服务。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        services = compose.get("services", {})
        assert "app" in services
        app = services["app"]
        assert "build" in app
        assert "depends_on" in app
    
    def test_docker_compose_network_defined(self):
        """验证docker-compose定义了自定义网络。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        assert "networks" in compose
        assert compose["networks"]["default"]["name"] == "pms-network"
    
    def test_docker_compose_has_volumes(self):
        """验证docker-compose定义了数据卷。"""
        import yaml
        with open("docker-compose.yml", "r", encoding="utf-8") as f:
            compose = yaml.safe_load(f)
        assert "volumes" in compose
        volumes = compose["volumes"]
        assert "pg_primary_data" in volumes
        assert "redis_master_data" in volumes
```

---

## 六、测试执行指南

### 6.1 前置条件

```bash
# 1. 确保Python环境已配置
python --version  # 应显示3.11+

# 2. 安装测试依赖
pip install pytest pytest-asyncio pyyaml redis

# 3. 配置环境变量
export DATABASE_URL="postgresql+asyncpg://user:pass@localhost/fms"
export REDIS_URL="redis://localhost:6379"
```

### 6.2 执行命令

```bash
# 执行D4-D6全部测试
pytest tests/test_d4_d6.py -v

# 仅执行数据库测试
pytest tests/test_d4_d6.py -v -k "TestDatabase"

# 仅执行Redis测试
pytest tests/test_d4_d6.py -v -k "TestRedis"

# 生成覆盖率报告
pytest tests/test_d4_d6.py --cov=src.infrastructure --cov-report=html
```

### 6.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| 数据库连接 | 正常 | ____ |
| Redis连接 | 正常 | ____ |
| 阻塞问题 | 0个 | ____个 |

---

## 七、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/infrastructure/database.py` | 数据库连接管理 |
| `src/infrastructure/redis.py` | Redis连接管理 |
| `k8s/deployment.yml` | 应用部署配置 |
| `k8s/postgresql.yml` | PG主从配置 |
| `k8s/redis.yml` | Redis Sentinel配置 |
| `k8s/storage.yml` | 存储类配置 |
| `k8s/gpu.yml` | GPU调度配置 |
| `Dockerfile` | 容器构建文件 |
| `docker-compose.yml` | 本地开发编排 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
