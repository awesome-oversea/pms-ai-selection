# 单元测试用例文档: D116-D120

> **时间范围**: 第116-120个工作日
> **所属阶段**: Phase 4 - 运维工具链与监控
> **文档版本**: v1.0
> **生成日期**: 2026-04-06

---

## 一、测试概述

### 1.1 测试范围

| 模块 | 测试类 | 测试用例数 |
|------|--------|-----------|
| 脚本执行 | TestScriptExecution | 2 |
| 告警 | TestAlert | 2 |
| 检查项 | TestInspectionItem | 2 |
| 知识文章 | TestKnowledgeArticle | 2 |
| 运维脚本管理器 | TestOpsScriptManager | 6 |
| 仪表板管理器 | TestDashboardManager | 5 |
| 告警管理器 | TestAlertManager | 6 |
| 检查管理器 | TestInspectionManager | 6 |
| 知识库 | TestKnowledgeBase | 5 |
| **总计** | | **30** |

---

## 二、测试用例详情

### 2.1 TestScriptExecution - 脚本执行

#### TC_001: 脚本执行创建

```python
def test_execution_creation(self):
    execution = ScriptExecution(
        execution_id="EXEC_001",
        script_name="健康检查",
        script_type=ScriptType.HEALTH_CHECK,
    )
    assert execution.status == ScriptStatus.PENDING
```

#### TC_002: 脚本执行状态

```python
def test_execution_status(self):
    execution = ScriptExecution(...)
    execution.run()
    assert execution.status in [ScriptStatus.RUNNING, ScriptStatus.COMPLETED]
```

---

### 2.2 TestAlert - 告警

#### TC_003: 告警创建

```python
def test_alert_creation(self):
    alert = Alert(
        alert_id="ALERT_001",
        title="CPU使用率过高",
        level=AlertLevel.WARNING,
    )
    assert alert.status == AlertStatus.ACTIVE
```

#### TC_004: 告警升级

```python
def test_alert_escalation(self):
    alert = Alert(...)
    alert.escalate()
    assert alert.level == AlertLevel.CRITICAL
```

---

### 2.3 TestInspectionItem - 检查项

#### TC_005: 检查项创建

```python
def test_inspection_item_creation(self):
    item = InspectionItem(
        item_id="CHECK_001",
        name="数据库连接池",
        check_type=CheckType.DATABASE,
    )
    assert item.status == CheckStatus.PENDING
```

#### TC_006: 检查项执行

```python
def test_inspection_execution(self):
    item = InspectionItem(...)
    item.execute()
    assert item.status in [CheckStatus.PASSED, CheckStatus.FAILED]
```

---

### 2.4 TestKnowledgeArticle - 知识文章

#### TC_007: 文章创建

```python
def test_article_creation(self):
    article = KnowledgeArticle(
        article_id="KB_001",
        title="K8s故障排查指南",
        category="运维",
    )
    assert article.article_id == "KB_001"
```

#### TC_008: 文章搜索

```python
def test_article_search(self):
    article = KnowledgeArticle(...)
    results = article.search("故障排查")
    assert isinstance(results, list)
```

---

### 2.5 TestOpsScriptManager - 运维脚本管理器

#### TC_009: 创建脚本

```python
def test_create_script(self):
    script = _run_async(self.manager.create_script(
        name="健康检查",
        script_type=ScriptType.HEALTH_CHECK,
        content="#!/bin/bash\necho 'OK'",
    ))
    assert script.script_id.startswith("SCRIPT_")
```

#### TC_010: 执行脚本

```python
def test_execute_script(self):
    script = _run_async(self.manager.create_script(...))
    result = _run_async(self.manager.execute_script(script.script_id))
    assert result.exit_code == 0
```

#### TC_011: 获取执行结果

```python
def test_get_execution_result(self):
    result = _run_async(self.manager.get_execution_result("EXEC_001"))
    assert result is not None
```

#### TC_012: 列出脚本

```python
def test_list_scripts(self):
    scripts = _run_async(self.manager.list_scripts())
    assert isinstance(scripts, list)
```

#### TC_013: 更新脚本

```python
def test_update_script(self):
    script = _run_async(self.manager.create_script(...))
    updated = _run_async(self.manager.update_script(
        script.script_id,
        content="#!/bin/bash\necho 'Updated'",
    ))
    assert updated is True
```

#### TC_014: 删除脚本

```python
def test_delete_script(self):
    script = _run_async(self.manager.create_script(...))
    deleted = _run_async(self.manager.delete_script(script.script_id))
    assert deleted is True
```

---

### 2.6 TestDashboardManager - 仪表板管理器

#### TC_015: 创建仪表板

```python
def test_create_dashboard(self):
    dashboard = _run_async(self.manager.create_dashboard(
        name="系统监控",
        widgets=["cpu", "memory", "disk"],
    ))
    assert dashboard.dashboard_id.startswith("DASH_")
```

#### TC_016: 获取仪表板

```python
def test_get_dashboard(self):
    dashboard = _run_async(self.manager.get_dashboard("DASH_001"))
    assert dashboard is not None
```

#### TC_017: 更新仪表板

```python
def test_update_dashboard(self):
    dashboard = _run_async(self.manager.create_dashboard(...))
    updated = _run_async(self.manager.update_dashboard(
        dashboard.dashboard_id,
        widgets=["cpu", "memory"],
    ))
    assert updated is True
```

#### TC_018: 列出仪表板

```python
def test_list_dashboards(self):
    dashboards = _run_async(self.manager.list_dashboards())
    assert isinstance(dashboards, list)
```

#### TC_019: 删除仪表板

```python
def test_delete_dashboard(self):
    dashboard = _run_async(self.manager.create_dashboard(...))
    deleted = _run_async(self.manager.delete_dashboard(dashboard.dashboard_id))
    assert deleted is True
```

---

### 2.7 TestAlertManager - 告警管理器

#### TC_020: 创建告警规则

```python
def test_create_alert_rule(self):
    rule = _run_async(self.manager.create_alert_rule(
        name="CPU告警",
        metric="cpu_usage",
        threshold=80,
        level=AlertLevel.WARNING,
    ))
    assert rule.rule_id.startswith("RULE_")
```

#### TC_021: 触发告警

```python
def test_trigger_alert(self):
    rule = _run_async(self.manager.create_alert_rule(...))
    alert = _run_async(self.manager.trigger_alert(rule.rule_id, value=85))
    assert alert.alert_id is not None
```

#### TC_022: 获取告警

```python
def test_get_alert(self):
    alert = _run_async(self.manager.get_alert("ALERT_001"))
    assert alert is not None
```

#### TC_023: 确认告警

```python
def test_acknowledge_alert(self):
    alert = _run_async(self.manager.get_alert("ALERT_001"))
    acknowledged = _run_async(self.manager.acknowledge_alert(alert.alert_id))
    assert acknowledged is True
```

#### TC_024: 解决告警

```python
def test_resolve_alert(self):
    alert = _run_async(self.manager.get_alert("ALERT_001"))
    resolved = _run_async(self.manager.resolve_alert(alert.alert_id))
    assert resolved is True
```

#### TC_025: 列出活跃告警

```python
def test_list_active_alerts(self):
    alerts = _run_async(self.manager.list_active_alerts())
    assert isinstance(alerts, list)
```

---

### 2.8 TestInspectionManager - 检查管理器

#### TC_026: 创建检查项

```python
def test_create_inspection_item(self):
    item = _run_async(self.manager.create_inspection_item(
        name="数据库连接池",
        check_type=CheckType.DATABASE,
    ))
    assert item.item_id.startswith("CHECK_")
```

#### TC_027: 执行检查

```python
def test_execute_inspection(self):
    item = _run_async(self.manager.create_inspection_item(...))
    result = _run_async(self.manager.execute_inspection(item.item_id))
    assert result.status in [CheckStatus.PASSED, CheckStatus.FAILED]
```

#### TC_028: 获取检查结果

```python
def test_get_inspection_result(self):
    result = _run_async(self.manager.get_inspection_result("CHECK_001"))
    assert result is not None
```

#### TC_029: 列出检查项

```python
def test_list_inspection_items(self):
    items = _run_async(self.manager.list_inspection_items())
    assert isinstance(items, list)
```

#### TC_030: 执行所有检查

```python
def test_execute_all_inspections(self):
    results = _run_async(self.manager.execute_all_inspections())
    assert "total" in results
    assert "passed" in results
```

#### TC_031: 获取统计数据

```python
def test_get_stats(self):
    stats = self.manager.get_stats()
    assert stats["total_items"] >= 0
```

---

### 2.9 TestKnowledgeBase - 知识库

#### TC_032: 创建文章

```python
def test_create_article(self):
    article = _run_async(self.manager.create_article(
        title="K8s故障排查指南",
        content="故障排查步骤...",
        category="运维",
    ))
    assert article.article_id.startswith("KB_")
```

#### TC_033: 搜索文章

```python
def test_search_articles(self):
    articles = _run_async(self.manager.search_articles("故障排查"))
    assert isinstance(articles, list)
```

#### TC_034: 获取文章

```python
def test_get_article(self):
    article = _run_async(self.manager.get_article("KB_001"))
    assert article is not None
```

#### TC_035: 更新文章

```python
def test_update_article(self):
    article = _run_async(self.manager.create_article(...))
    updated = _run_async(self.manager.update_article(
        article.article_id,
        content="更新后的内容",
    ))
    assert updated is True
```

#### TC_036: 列出文章

```python
def test_list_articles(self):
    articles = _run_async(self.manager.list_articles())
    assert isinstance(articles, list)
```

---

## 三、测试用例统计

| 测试类 | 用例数 | 覆盖功能 |
|--------|--------|---------|
| TestScriptExecution | 2 | 脚本执行数据模型 |
| TestAlert | 2 | 告警数据模型 |
| TestInspectionItem | 2 | 检查项数据模型 |
| TestKnowledgeArticle | 2 | 知识文章数据模型 |
| TestOpsScriptManager | 6 | 运维脚本管理 |
| TestDashboardManager | 5 | 仪表板管理 |
| TestAlertManager | 6 | 告警管理 |
| TestInspectionManager | 6 | 检查管理 |
| TestKnowledgeBase | 5 | 知识库 |
| **总计** | **36** | |

---

## 四、测试环境

- **Python版本**: 3.12.10
- **测试框架**: pytest
- **被测模块**: src.infrastructure.ops_toolkit

---

**文档版本**: v1.0
**最后更新**: 2026-04-06
**验收状态**: ✅ 通过
