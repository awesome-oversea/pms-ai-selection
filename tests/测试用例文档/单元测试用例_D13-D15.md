# 单元测试用例文档 D13-D15: RAG框架搭建

> **时间范围**: 第13-15个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D13 | LlamaIndex集成与切片策略 | 功能验证 | 10 |
| D14 | 混合检索与Rerank集成 | 功能验证 | 8 |
| D15 | Prompt优化与性能测试 | 性能验证 | 6 |

**总计**: 24个测试用例

---

## 二、D13 测试用例：LlamaIndex集成与切片策略

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D13-TC01 | RAG框架导入 | `from src.rag import RAGEngine` | 导入成功 | P0 |
| D13-TC02 | 文档加载器 | 加载测试文档 | 成功加载 | P0 |
| D13-TC03 | 文档切片器 | 执行文档切片 | 返回chunks | P0 |
| D13-TC04 | 切片大小配置 | 检查chunk_size | 512-1024 tokens | P0 |
| D13-TC05 | 切片重叠配置 | 检查chunk_overlap | 50-100 tokens | P1 |
| D13-TC06 | 向量索引创建 | 创建测试索引 | 索引创建成功 | P0 |
| D13-TC07 | 向量索引持久化 | 保存索引到磁盘 | 文件存在 | P1 |
| D13-TC08 | 多种切片策略 | 测试不同切片方式 | 均可用 | P1 |
| D13-TC09 | 切片元数据保留 | 检查元数据 | 元数据完整 | P1 |
| D13-TC10 | 切片性能 | 测量切片速度 | <1s/100KB | P2 |

### 2.2 自动化测试脚本

```python
class TestD13_RAGFrameworkChunking:
    """
    D13 RAG框架与切片策略验证测试。
    
    验证:
        - RAG框架可正常导入
        - 文档切片功能正常
        - 向量索引创建正常
    """
    
    def test_rag_engine_import(self):
        """验证RAG引擎模块可导入。"""
        from src.rag.engine import RAGEngine
        assert RAGEngine is not None
    
    def test_document_chunker_import(self):
        """验证文档切片器可导入。"""
        from src.rag.chunker import DocumentChunker
        assert DocumentChunker is not None
    
    def test_chunker_settings(self):
        """验证切片器配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert hasattr(settings, 'rag') or hasattr(settings, 'chunker')
    
    def test_chunk_size_range(self):
        """验证切片大小在合理范围。"""
        from src.config.settings import get_settings
        settings = get_settings()
        if hasattr(settings, 'rag'):
            assert 128 <= settings.rag.chunk_size <= 2048
    
    def test_vector_store_import(self):
        """验证向量存储模块可导入。"""
        from src.rag.vector_store import VectorStore
        assert VectorStore is not None
```

---

## 三、D14 测试用例：混合检索与Rerank集成

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D14-TC01 | 向量检索 | 执行向量搜索 | 返回相似文档 | P0 |
| D14-TC02 | 关键词检索 | 执行BM25搜索 | 返回匹配文档 | P0 |
| D14-TC03 | 混合检索 | 执行混合搜索 | 返回融合结果 | P0 |
| D14-TC04 | Rerank集成 | 对结果重排序 | 分数更新 | P0 |
| D14-TC05 | 检索Top-K配置 | 检查top_k设置 | 默认10 | P1 |
| D14-TC06 | 检索分数阈值 | 检查score_threshold | 默认0.5 | P1 |
| D14-TC07 | 检索性能 | 测量检索延迟 | <500ms | P1 |
| D14-TC08 | 检索缓存 | 检查缓存功能 | 缓存命中 | P2 |

### 3.2 自动化测试脚本

```python
class TestD14_HybridRetrievalRerank:
    """
    D14 混合检索与Rerank验证测试。
    
    验证:
        - 向量检索功能正常
        - 关键词检索功能正常
        - Rerank重排序功能正常
    """
    
    def test_hybrid_retriever_import(self):
        """验证混合检索器可导入。"""
        from src.rag.retriever import HybridRetriever
        assert HybridRetriever is not None
    
    def test_bm25_retriever_import(self):
        """验证BM25检索器可导入。"""
        from src.rag.retriever import BM25Retriever
        assert BM25Retriever is not None
    
    def test_rerank_integration(self):
        """验证Rerank集成到RAG流程。"""
        from src.rag.pipeline import RAGPipeline
        assert RAGPipeline is not None
```

---

## 四、D15 测试用例：Prompt优化与性能测试

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D15-TC01 | Prompt模板加载 | 加载Prompt模板 | 模板列表非空 | P0 |
| D15-TC02 | Prompt模板渲染 | 渲染测试Prompt | 正确替换变量 | P0 |
| D15-TC03 | 多场景Prompt | 检查Prompt覆盖场景 | ≥5个场景 | P0 |
| D15-TC04 | vLLM推理延迟P99 | 测量延迟 | <3秒 | P0 |
| D15-TC05 | vLLM吞吐量 | 测量QPS | >10 QPS | P1 |
| D15-TC06 | Prompt长度限制 | 检查max_tokens | 配置合理 | P1 |

### 4.2 自动化测试脚本

```python
class TestD15_PromptOptimizationPerf:
    """
    D15 Prompt优化与性能验证测试。
    
    验证:
        - Prompt模板管理功能正常
        - vLLM推理性能达标
    """
    
    def test_prompt_template_manager_import(self):
        """验证Prompt模板管理器可导入。"""
        from src.services.prompt_template import PromptTemplateManager
        assert PromptTemplateManager is not None
    
    def test_prompt_template_config(self):
        """验证Prompt模板配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.llm.max_tokens > 0
        assert 0.0 <= settings.llm.temperature <= 2.0
    
    def test_llm_gateway_import(self):
        """验证LLM Gateway可导入。"""
        from src.services.llm_gateway import LLMGateway
        assert LLMGateway is not None
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 安装测试依赖
pip install pytest pytest-asyncio llama-index llama-index-vector-stores-qdrant

# 配置环境变量
export QDRANT_URL="http://localhost:6333"
export LLM_API_URL="http://localhost:8000"
```

### 5.2 执行命令

```bash
# 执行D13-D15全部测试
pytest tests/test_d13_d15.py -v

# 生成覆盖率报告
pytest tests/test_d13_d15.py --cov=src.rag --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| 检索延迟 | <500ms | ____ms |
| 推理延迟P99 | <3秒 | ____秒 |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/rag/engine.py` | RAG引擎 |
| `src/rag/chunker.py` | 文档切片器 |
| `src/rag/retriever.py` | 混合检索器 |
| `src/rag/vector_store.py` | 向量存储 |
| `src/rag/pipeline.py` | RAG Pipeline |
| `src/services/prompt_template.py` | Prompt模板管理 |
| `src/services/llm_gateway.py` | LLM网关 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
