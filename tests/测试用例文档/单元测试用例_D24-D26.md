# 单元测试用例文档 D24-D26: 并行执行与前端启动

> **时间范围**: 第24-26个工作日 | **所属阶段**: Phase 2 - Multi-Agent集成
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D24 | Ray Actor并行执行与前端初始化 | 功能验证 | 8 |
| D25 | Vue框架与Human-in-loop | 功能验证 | 7 |
| D26 | 断点调试与对话协议 | 功能验证 | 6 |

**总计**: 21个测试用例

---

## 二、D24 测试用例：并行执行与前端初始化

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D24-TC01 | Ray初始化 | 检查Ray集群 | Ray可用 | P0 |
| D24-TC02 | Actor创建 | 创建Ray Actor | Actor创建成功 | P0 |
| D24-TC03 | 并行任务执行 | 执行并行任务 | 任务并行执行 | P0 |
| D24-TC04 | 任务结果收集 | 收集并行结果 | 结果正确 | P0 |
| D24-TC05 | Next.js项目结构 | 检查项目结构 | App Router结构 | P0 |
| D24-TC06 | 前端依赖安装 | `npm install` | 安装成功 | P0 |
| D24-TC07 | 前端启动 | `npm run dev` | 启动成功 | P0 |
| D24-TC08 | 前端页面渲染 | 访问localhost:3000 | 页面渲染 | P1 |

### 2.2 自动化测试脚本

```python
class TestD24_ParallelExecutionFrontendInit:
    """
    D24 并行执行与前端初始化验证测试。
    
    验证:
        - Ray Actor并行执行正常
        - Next.js项目初始化成功
    """
    
    def test_parallel_executor_import(self):
        """验证并行执行器模块可导入。"""
        from src.agents.parallel import ParallelExecutor
        assert ParallelExecutor is not None
    
    def test_ray_config(self):
        """验证Ray配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        # Ray配置可选
        pass
    
    def test_web_directory_exists(self):
        """验证web目录存在。"""
        import os
        assert os.path.isdir("web")
```

---

## 三、D25 测试用例：Vue框架与Human-in-loop

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D25-TC01 | Vue后台框架 | 检查Vue项目结构 | 结构正确 | P0 |
| D25-TC02 | ElementUI Plus | 检查UI组件库 | 已安装 | P0 |
| D25-TC03 | 后台布局组件 | 检查布局组件 | 组件存在 | P0 |
| D25-TC04 | Human-in-loop接口 | 调用人机交互API | 接口可用 | P0 |
| D25-TC05 | 人工审批流程 | 模拟审批请求 | 流程正确 | P0 |
| D25-TC06 | 审批状态更新 | 更新审批状态 | 状态更新成功 | P1 |
| D25-TC07 | 审批历史记录 | 检查历史记录 | 记录完整 | P1 |

### 3.2 自动化测试脚本

```python
class TestD25_VueFrameworkHumanInLoop:
    """
    D25 Vue框架与Human-in-loop验证测试。
    
    验证:
        - Vue后台框架正常
        - Human-in-loop功能完整
    """
    
    def test_human_in_loop_import(self):
        """验证Human-in-loop模块可导入。"""
        from src.agents.human_in_loop import HumanInTheLoop
        assert HumanInTheLoop is not None
    
    def test_approval_workflow_import(self):
        """验证审批工作流模块可导入。"""
        from src.agents.approval import ApprovalWorkflow
        assert ApprovalWorkflow is not None
    
    def test_web_html_exists(self):
        """验证前端HTML文件存在。"""
        import os
        assert os.path.isfile("web/index.html")
```

---

## 四、D26 测试用例：断点调试与对话协议

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D26-TC01 | 调试面板存在 | 检查调试页面 | 页面存在 | P0 |
| D26-TC02 | Agent状态查看 | 查看Agent状态 | 状态可查看 | P0 |
| D26-TC03 | 断点设置 | 设置断点 | 断点可设置 | P0 |
| D26-TC04 | 单步执行 | 执行单步操作 | 执行成功 | P1 |
| D26-TC05 | 对话协议定义 | 检查消息格式 | 格式已定义 | P0 |
| D26-TC06 | 消息序列化 | 序列化消息 | 序列化正确 | P1 |

### 4.2 自动化测试脚本

```python
class TestD26_DebugAndProtocol:
    """
    D26 断点调试与对话协议验证测试。
    
    验证:
        - 调试面板功能正常
        - 对话协议定义完整
    """
    
    def test_debug_panel_import(self):
        """验证调试面板模块可导入。"""
        from src.agents.debug import DebugPanel
        assert DebugPanel is not None
    
    def test_message_protocol_import(self):
        """验证消息协议模块可导入。"""
        from src.agents.protocol import MessageProtocol
        assert MessageProtocol is not None
    
    def test_message_format(self):
        """验证消息格式定义。"""
        from src.agents.protocol import AgentMessage
        assert AgentMessage is not None
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 安装测试依赖
pip install pytest pytest-asyncio ray

# 前端依赖
cd web && npm install
```

### 5.2 执行命令

```bash
# 执行D24-D26全部测试
pytest tests/test_d24_d26.py -v

# 生成覆盖率报告
pytest tests/test_d24_d26.py --cov=src.agents --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| 并行执行 | 正常 | ____ |
| Human-in-loop | 可用 | ____ |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/agents/parallel.py` | 并行执行器 |
| `src/agents/human_in_loop.py` | Human-in-loop |
| `src/agents/approval.py` | 审批工作流 |
| `src/agents/debug.py` | 调试面板 |
| `src/agents/protocol.py` | 对话协议 |
| `web/index.html` | 前端页面 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
