# 单元测试用例文档 D7-D9: 向量库与消息队列

> **时间范围**: 第7-9个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D7 | Qdrant与Kafka部署 | 功能验证 | 10 |
| D8 | Qdrant集群与vLLM启动 | 功能验证 | 8 |
| D9 | vLLM配置优化 | 性能验证 | 6 |

**总计**: 24个测试用例

---

## 二、D7 测试用例：Qdrant与Kafka部署

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D7-TC01 | Qdrant节点1连接 | `curl http://qdrant-1:6333/collections` | 返回JSON | P0 |
| D7-TC02 | Kafka Broker连接 | `kafka-broker-api-versions.sh --bootstrap-server kafka:9092` | 返回API列表 | P0 |
| D7-TC03 | Kafka Topic创建 | `kafka-topics.sh --create --topic test` | 创建成功 | P0 |
| D7-TC04 | Kafka生产消息 | 发送测试消息 | 成功 | P0 |
| D7-TC05 | Kafka消费消息 | 消费测试消息 | 成功接收 | P0 |
| D7-TC06 | Qdrant Collection创建 | 创建测试Collection | 成功 | P0 |
| D7-TC07 | Qdrant向量写入 | 插入测试向量 | 成功 | P0 |
| D7-TC08 | Qdrant向量搜索 | 执行向量搜索 | 返回结果 | P0 |
| D7-TC09 | Kafka分区配置 | 检查分区数 | ≥3 | P1 |
| D7-TC10 | Kafka副本配置 | 检查副本因子 | ≥2 | P1 |

### 2.2 自动化测试脚本

```python
class TestD7_QdrantKafkaDeployment:
    """
    D7 Qdrant与Kafka部署验证测试。
    
    验证:
        - Qdrant API可访问
        - Kafka集群正常运行
        - Topic创建和消息收发
    """
    
    def test_qdrant_client_import(self):
        """验证Qdrant客户端模块可导入。"""
        from src.infrastructure.vector_db import QdrantClient
        assert QdrantClient is not None
    
    def test_qdrant_client_settings(self):
        """验证Qdrant配置默认值合理。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.qdrant.url is not None
        assert settings.qdrant.collection_name is not None
    
    def test_kafka_settings_defaults(self):
        """验证Kafka配置默认值合理。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.kafka.bootstrap_servers is not None
        assert len(settings.kafka.topics) > 0
    
    def test_kafka_producer_import(self):
        """验证Kafka生产者模块可导入。"""
        from src.infrastructure.messaging import KafkaProducer
        assert KafkaProducer is not None
    
    def test_kafka_consumer_import(self):
        """验证Kafka消费者模块可导入。"""
        from src.infrastructure.messaging import KafkaConsumer
        assert KafkaConsumer is not None
```

---

## 三、D8 测试用例：Qdrant集群与vLLM启动

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D8-TC01 | Qdrant集群状态 | 检查Raft集群 | 3节点健康 | P0 |
| D8-TC02 | Qdrant Leader选举 | 查询当前Leader | Leader已选出 | P0 |
| D8-TC03 | Qdrant数据同步 | 写入数据，检查所有节点 | 数据一致 | P0 |
| D8-TC04 | vLLM容器启动 | `docker ps | grep vllm` | 容器运行中 | P0 |
| D8-TC05 | vLLM API连接 | `curl http://vllm:8000/v1/models` | 返回模型列表 | P0 |
| D8-TC06 | vLLM模型加载 | 检查模型状态 | Qwen2.5-72B已加载 | P0 |
| D8-TC07 | vLLM推理测试 | 发送简单请求 | 返回正确响应 | P0 |
| D8-TC08 | vLLM内存使用 | 检查GPU内存占用 | <80GB | P1 |

### 3.2 自动化测试脚本

```python
class TestD8_QdrantClustervLLMStart:
    """
    D8 Qdrant集群与vLLM启动验证测试。
    
    验证:
        - Qdrant Raft集群正常
        - vLLM推理服务启动
        - 基本推理功能
    """
    
    def test_qdrant_cluster_mode_config(self):
        """验证Qdrant集群模式配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        # 集群模式需要多个节点URL
        if hasattr(settings.qdrant, 'cluster_nodes'):
            assert settings.qdrant.cluster_nodes is not None or settings.qdrant.url is not None
    
    def test_llm_settings_model_name(self):
        """验证LLM模型名称配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.llm.primary_model is not None
    
    def test_llm_settings_max_tokens(self):
        """验证LLM max_tokens配置合理。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert 100 <= settings.llm.max_tokens <= 32000
    
    def test_llm_settings_temperature(self):
        """验证LLM temperature配置合理。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert 0.0 <= settings.llm.temperature <= 2.0
```

---

## 四、D9 测试用例：vLLM配置优化

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D9-TC01 | 张量并行TP=4 | 检查vLLM启动参数 | TP=4生效 | P0 |
| D9-TC02 | 流水线并行PP=2 | 检查vLLM启动参数 | PP=2生效 | P0 |
| D9-TC03 | Prefix Caching | 检查缓存命中率 | >50% | P1 |
| D9-TC04 | 动态Batching | 检查batch配置 | 已启用 | P0 |
| D9-TC05 | 推理延迟P99 | 测量延迟 | <3秒 | P0 |
| D9-TC06 | 吞吐量测试 | 测量QPS | >10 QPS | P1 |

### 4.2 自动化测试脚本

```python
class TestD9_vLLMConfigOptimization:
    """
    D9 vLLM配置优化验证测试。
    
    验证:
        - 张量并行配置
        - 流水线并行配置
        - Prefix Caching
        - 推理性能
    """
    
    def test_llm_gateway_import(self):
        """验证LLM Gateway模块可导入。"""
        from src.services.llm_gateway import LLMGateway
        assert LLMGateway is not None
    
    def test_llm_gateway_settings(self):
        """验证LLM Gateway配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        assert settings.llm.gateway_url is not None or settings.llm.primary_model is not None
    
    def test_prompt_template_manager_import(self):
        """验证Prompt模板管理器可导入。"""
        from src.services.prompt_template import PromptTemplateManager
        assert PromptTemplateManager is not None
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 1. 确保Python环境已配置
python --version  # 应显示3.11+

# 2. 安装测试依赖
pip install pytest pytest-asyncio qdrant-client kafka-python

# 3. 配置环境变量
export QDRANT_URL="http://localhost:6333"
export KAFKA_BOOTSTRAP_SERVERS="localhost:9092"
export LLM_API_URL="http://localhost:8000"
```

### 5.2 执行命令

```bash
# 执行D7-D9全部测试
pytest tests/test_d7_d9.py -v

# 仅执行Qdrant测试
pytest tests/test_d7_d9.py -v -k "Qdrant"

# 仅执行vLLM测试
pytest tests/test_d7_d9.py -v -k "vLLM"

# 生成覆盖率报告
pytest tests/test_d7_d9.py --cov=src --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| Qdrant集群 | 3节点健康 | ____ |
| vLLM推理延迟P99 | <3秒 | ____秒 |
| 阻塞问题 | 0个 | ____个 |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/infrastructure/vector_db.py` | 向量数据库连接 |
| `src/infrastructure/messaging.py` | Kafka消息队列 |
| `src/services/llm_gateway.py` | LLM网关服务 |
| `src/services/prompt_template.py` | Prompt模板管理 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
