# 单元测试用例文档 D16-D18: Demo开发

> **时间范围**: 第16-18个工作日 | **所属阶段**: Phase 1 - 基础搭建
> **文档版本**: v1.0 | **最后更新**: 2026-04-06

---

## 一、测试范围概述

| 任务编号 | 测试范围 | 测试类型 | 测试用例数 |
|---------|---------|---------|----------|
| D16 | Demo接口开发 | 功能验证 | 8 |
| D17 | Demo前端开发 | 功能验证 | 6 |
| D18 | 网关与ES准备 | 功能验证 | 6 |

**总计**: 20个测试用例

---

## 二、D16 测试用例：Demo接口开发

### 2.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D16-TC01 | API端点存在 | `GET /api/demo/query` | 返回200 | P0 |
| D16-TC02 | 查询参数验证 | 发送空query | 返回400错误 | P0 |
| D16-TC03 | 查询响应格式 | 发送有效查询 | 返回JSON响应 | P0 |
| D16-TC04 | 响应包含答案 | 检查响应字段 | 包含answer字段 | P0 |
| D16-TC05 | 响应包含来源 | 检查响应字段 | 包含sources字段 | P1 |
| D16-TC06 | API响应时间 | 测量响应时间 | <5秒 | P0 |
| D16-TC07 | 错误处理 | 发送异常请求 | 返回友好错误 | P1 |
| D16-TC08 | API文档 | 访问/docs | Swagger UI可用 | P1 |

### 2.2 自动化测试脚本

```python
class TestD16_DemoAPIDevelopment:
    """
    D16 Demo接口开发验证测试。
    
    验证:
        - Demo API端点可访问
        - 查询功能正常
        - 响应格式正确
    """
    
    def test_api_router_import(self):
        """验证API路由模块可导入。"""
        from src.api.routes import demo
        assert demo is not None
    
    def test_fastapi_app_import(self):
        """验证FastAPI应用可导入。"""
        from src.main import app
        assert app is not None
    
    def test_api_response_model(self):
        """验证API响应模型定义。"""
        from src.api.schemas import DemoQueryResponse
        assert DemoQueryResponse is not None
```

---

## 三、D17 测试用例：Demo前端开发

### 3.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D17-TC01 | 前端页面加载 | 访问首页 | 页面正常显示 | P0 |
| D17-TC02 | 查询输入框 | 检查输入框存在 | 输入框可用 | P0 |
| D17-TC03 | 查询提交 | 提交查询请求 | 触发API调用 | P0 |
| D17-TC04 | 结果显示 | 检查结果区域 | 显示答案 | P0 |
| D17-TC05 | 加载状态 | 提交查询时 | 显示loading | P1 |
| D17-TC06 | 错误显示 | API错误时 | 显示错误提示 | P1 |

### 3.2 自动化测试脚本

```python
class TestD17_DemoFrontendDevelopment:
    """
    D17 Demo前端开发验证测试。
    
    验证:
        - 前端页面文件存在
        - 静态资源配置正确
    """
    
    def test_web_index_exists(self):
        """验证前端index.html存在。"""
        import os
        assert os.path.isfile("web/index.html")
    
    def test_web_js_exists(self):
        """验证前端JS文件存在。"""
        import os
        assert os.path.isfile("web/app.js") or os.path.isfile("web/static/app.js")
    
    def test_web_css_exists(self):
        """验证前端CSS文件存在。"""
        import os
        assert os.path.isfile("web/styles.css") or os.path.isfile("web/static/styles.css")
```

---

## 四、D18 测试用例：网关与ES准备

### 4.1 功能验证测试

| 用例ID | 测试名称 | 测试命令/步骤 | 预期结果 | 优先级 |
|--------|---------|--------------|---------|--------|
| D18-TC01 | Kong Gateway连接 | `curl http://kong:8001/status` | 返回运行状态 | P0 |
| D18-TC02 | Kong路由配置 | 检查Service/Route | 配置正确 | P0 |
| D18-TC03 | Kong限流配置 | 检查Rate Limiting插件 | 已配置 | P1 |
| D18-TC04 | ES集群连接 | `curl http://es:9200/_cluster/health` | status=green | P0 |
| D18-TC05 | ES索引创建 | 创建测试索引 | 索引创建成功 | P0 |
| D18-TC06 | ES文档写入 | 写入测试文档 | 文档写入成功 | P0 |

### 4.2 自动化测试脚本

```python
class TestD18_GatewayAndES:
    """
    D18 网关与ES验证测试。
    
    验证:
        - Kong Gateway配置正确
        - Elasticsearch集群正常
    """
    
    def test_elasticsearch_client_import(self):
        """验证ES客户端模块可导入。"""
        from src.infrastructure.elasticsearch import ESClient
        assert ESClient is not None
    
    def test_kong_settings(self):
        """验证Kong配置。"""
        from src.config.settings import get_settings
        settings = get_settings()
        # Kong配置可选
        pass
```

---

## 五、测试执行指南

### 5.1 前置条件

```bash
# 安装测试依赖
pip install pytest pytest-asyncio httpx elasticsearch

# 配置环境变量
export API_BASE_URL="http://localhost:8000"
export ES_URL="http://localhost:9200"
```

### 5.2 执行命令

```bash
# 执行D16-D18全部测试
pytest tests/test_d16_d18.py -v

# 生成覆盖率报告
pytest tests/test_d16_d18.py --cov=src.api --cov-report=html
```

### 5.3 验收通过标准

| 指标 | 标准 | 实际 |
|------|------|------|
| 测试通过率 | ≥95% | ____% |
| API响应时间 | <5秒 | ____秒 |
| Demo可用 | 是 | ____ |

---

## 六、相关文件索引

| 文件路径 | 说明 |
|---------|------|
| `src/api/routes/demo.py` | Demo API路由 |
| `src/api/schemas/__init__.py` | API数据模型 |
| `src/main.py` | FastAPI应用入口 |
| `web/index.html` | 前端页面 |
| `web/app.js` | 前端JS逻辑 |
| `src/infrastructure/elasticsearch.py` | ES客户端 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**文档状态**: 正式版
