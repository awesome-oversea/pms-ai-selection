# 单元测试用例文档: D106-D110

> **时间范围**: 第106-110个工作日
> **所属阶段**: Phase 4 - 安全加固与备份恢复
> **文档版本**: v1.0
> **生成日期**: 2026-04-06

---

## 一、测试概述

### 1.1 测试范围

| 模块 | 测试类 | 测试用例数 |
|------|--------|-----------|
| 安全检查 | TestSecurityCheck | 2 |
| 审计日志 | TestAuditLog | 2 |
| 备份任务 | TestBackupJob | 2 |
| 恢复测试 | TestRecoveryTest | 2 |
| 事件 | TestIncident | 2 |
| 安全管理器 | TestSecurityManager | 7 |
| 审计日志器 | TestAuditLogger | 5 |
| 备份管理器 | TestBackupManager | 7 |
| 恢复测试器 | TestRecoveryTester | 4 |
| 事件管理器 | TestIncidentManager | 6 |
| **总计** | | **31** |

---

## 二、测试用例详情

### 2.1 TestSecurityCheck - 安全检查

#### TC_001: 安全检查创建

```python
def test_check_creation(self):
    check = SecurityCheck(
        check_id="SEC_001",
        name="SQL注入检测",
        layer=SecurityLayer.API,
    )
    assert check.check_id == "SEC_001"
    assert check.status == SecurityStatus.PENDING
```

#### TC_002: 安全检查转字典

```python
def test_check_to_dict(self):
    check = SecurityCheck(...)
    d = check.to_dict()
    assert d["layer"] == "api"
```

---

### 2.2 TestAuditLog - 审计日志

#### TC_003: 审计日志创建

```python
def test_audit_log_creation(self):
    log = AuditLog(
        log_id="AUDIT_001",
        user="admin",
        action="DELETE",
        resource="products",
        level=LogLevel.WARNING,
    )
    assert log.level == LogLevel.WARNING
```

#### TC_004: 审计日志转字典

```python
def test_audit_log_to_dict(self):
    log = AuditLog(...)
    d = log.to_dict()
    assert "timestamp" in d
```

---

### 2.3 TestBackupJob - 备份任务

#### TC_005: 备份任务创建

```python
def test_backup_job_creation(self):
    job = BackupJob(
        job_id="BACKUP_001",
        name="每日全量备份",
        backup_type=BackupType.FULL,
    )
    assert job.status == BackupStatus.PENDING
```

#### TC_006: 备份任务执行

```python
def test_backup_job_execution(self):
    job = BackupJob(...)
    job.execute()
    assert job.status in [BackupStatus.RUNNING, BackupStatus.COMPLETED]
```

---

### 2.4 TestRecoveryTest - 恢复测试

#### TC_007: 恢复测试创建

```python
def test_recovery_test_creation(self):
    test = RecoveryTest(
        test_id="RECOVERY_001",
        name="RPO恢复测试",
        backup_job_id="BACKUP_001",
    )
    assert test.test_id.startswith("RECOVERY_")
```

#### TC_008: 恢复测试执行

```python
def test_recovery_test_execution(self):
    test = RecoveryTest(...)
    result = test.execute()
    assert isinstance(result, bool)
```

---

### 2.5 TestIncident - 事件

#### TC_009: 事件创建

```python
def test_incident_creation(self):
    incident = Incident(
        incident_id="INC_001",
        title="数据泄露告警",
        severity=IncidentSeverity.CRITICAL,
    )
    assert incident.status == IncidentStatus.OPEN
```

#### TC_010: 事件升级

```python
def test_incident_escalation(self):
    incident = Incident(...)
    incident.escalate()
    assert incident.severity == IncidentSeverity.CRITICAL
```

---

### 2.6 TestSecurityManager - 安全管理器

#### TC_011: 执行安全检查

```python
def test_run_security_check(self):
    check = _run_async(self.manager.run_security_check(
        name="SQL注入检测",
        layer=SecurityLayer.API,
    ))
    assert check.check_id.startswith("SEC_")
```

#### TC_012: 扫描所有层

```python
def test_scan_all_layers(self):
    results = _run_async(self.manager.scan_all_layers())
    assert len(results) > 0
```

#### TC_013: 获取检查结果

```python
def test_get_check_result(self):
    check = _run_async(self.manager.run_security_check(...))
    result = _run_async(self.manager.get_check_result(check.check_id))
    assert result is not None
```

#### TC_014: 生成安全报告

```python
def test_generate_security_report(self):
    report = _run_async(self.manager.generate_security_report())
    assert "scan_time" in report
    assert "total_checks" in report
```

#### TC_015: 获取安全评分

```python
def test_get_security_score(self):
    score = _run_async(self.manager.get_security_score())
    assert 0 <= score <= 100
```

#### TC_016: 获取未修复问题

```python
def test_get_unresolved_issues(self):
    issues = _run_async(self.manager.get_unresolved_issues())
    assert isinstance(issues, list)
```

#### TC_017: 获取统计数据

```python
def test_get_stats(self):
    stats = self.manager.get_stats()
    assert stats["total_checks"] >= 0
```

---

### 2.7 TestAuditLogger - 审计日志器

#### TC_018: 记录日志

```python
def test_log_action(self):
    log = _run_async(self.logger.log_action(
        user="admin",
        action="DELETE",
        resource="products",
    ))
    assert log.log_id.startswith("AUDIT_")
```

#### TC_019: 查询日志

```python
def test_query_logs(self):
    logs = _run_async(self.logger.query_logs(
        user="admin",
        start_time=datetime.now(),
        end_time=datetime.now(),
    ))
    assert isinstance(logs, list)
```

#### TC_020: 获取关键操作日志

```python
def test_get_critical_actions(self):
    logs = _run_async(self.logger.get_critical_actions())
    assert isinstance(logs, list)
```

#### TC_021: 导出审计报告

```python
def test_export_audit_report(self):
    report = _run_async(self.logger.export_audit_report(
        start_time=datetime.now(),
        end_time=datetime.now(),
    ))
    assert "total_actions" in report
```

#### TC_022: 获取日志统计

```python
def test_get_stats(self):
    stats = self.logger.get_stats()
    assert stats["total_logs"] >= 0
```

---

### 2.8 TestBackupManager - 备份管理器

#### TC_023: 创建备份任务

```python
def test_create_backup_job(self):
    job = _run_async(self.manager.create_backup_job(
        name="每日全量备份",
        backup_type=BackupType.FULL,
    ))
    assert job.job_id.startswith("BACKUP_")
```

#### TC_024: 执行备份

```python
def test_execute_backup(self):
    job = _run_async(self.manager.create_backup_job(...))
    result = _run_async(self.manager.execute_backup(job.job_id))
    assert result.status in [BackupStatus.RUNNING, BackupStatus.COMPLETED]
```

#### TC_025: 列出备份任务

```python
def test_list_backup_jobs(self):
    jobs = _run_async(self.manager.list_backup_jobs())
    assert isinstance(jobs, list)
```

#### TC_026: 获取备份状态

```python
def test_get_backup_status(self):
    status = _run_async(self.manager.get_backup_status("BACKUP_001"))
    assert status is not None
```

#### TC_027: 验证备份

```python
def test_verify_backup(self):
    job = _run_async(self.manager.create_backup_job(...))
    verified = _run_async(self.manager.verify_backup(job.job_id))
    assert isinstance(verified, bool)
```

#### TC_028: 删除备份

```python
def test_delete_backup(self):
    job = _run_async(self.manager.create_backup_job(...))
    deleted = _run_async(self.manager.delete_backup(job.job_id))
    assert deleted is True
```

#### TC_029: 获取备份统计

```python
def test_get_stats(self):
    stats = self.manager.get_stats()
    assert stats["total_backups"] >= 0
```

---

### 2.9 TestRecoveryTester - 恢复测试器

#### TC_030: 创建恢复测试

```python
def test_create_recovery_test(self):
    test = _run_async(self.tester.create_recovery_test(
        name="RPO恢复测试",
        backup_job_id="BACKUP_001",
    ))
    assert test.test_id.startswith("RECOVERY_")
```

#### TC_031: 执行恢复测试

```python
def test_execute_recovery_test(self):
    test = _run_async(self.tester.create_recovery_test(...))
    result = _run_async(self.tester.execute_recovery_test(test.test_id))
    assert result is not None
```

#### TC_032: 获取测试结果

```python
def test_get_test_result(self):
    result = _run_async(self.tester.get_test_result("RECOVERY_001"))
    assert result is not None
```

#### TC_033: 列出恢复测试

```python
def test_list_recovery_tests(self):
    tests = _run_async(self.tester.list_recovery_tests())
    assert isinstance(tests, list)
```

---

### 2.10 TestIncidentManager - 事件管理器

#### TC_034: 上报事件

```python
def test_report_incident(self):
    incident = _run_async(self.manager.report_incident(
        title="数据泄露告警",
        severity=IncidentSeverity.CRITICAL,
        description="检测到异常数据访问",
    ))
    assert incident.incident_id.startswith("INC_")
```

#### TC_035: 更新事件状态

```python
def test_update_incident_status(self):
    incident = _run_async(self.manager.report_incident(...))
    updated = _run_async(self.manager.update_incident_status(
        incident.incident_id,
        IncidentStatus.IN_PROGRESS,
    ))
    assert updated.status == IncidentStatus.IN_PROGRESS
```

#### TC_036: 获取事件详情

```python
def test_get_incident(self):
    incident = _run_async(self.manager.report_incident(...))
    detail = _run_async(self.manager.get_incident(incident.incident_id))
    assert detail.title == incident.title
```

#### TC_037: 列出未解决事件

```python
def test_list_open_incidents(self):
    incidents = _run_async(self.manager.list_open_incidents())
    assert isinstance(incidents, list)
```

#### TC_038: 获取严重事件

```python
def test_get_critical_incidents(self):
    incidents = _run_async(self.manager.get_critical_incidents())
    assert isinstance(incidents, list)
```

#### TC_039: 解决事件

```python
def test_resolve_incident(self):
    incident = _run_async(self.manager.report_incident(...))
    resolved = _run_async(self.manager.resolve_incident(
        incident.incident_id,
        resolution="已修复",
    ))
    assert resolved.status == IncidentStatus.RESOLVED
```

---

## 三、测试用例统计

| 测试类 | 用例数 | 覆盖功能 |
|--------|--------|---------|
| TestSecurityCheck | 2 | 安全检查数据模型 |
| TestAuditLog | 2 | 审计日志数据模型 |
| TestBackupJob | 2 | 备份任务数据模型 |
| TestRecoveryTest | 2 | 恢复测试数据模型 |
| TestIncident | 2 | 事件数据模型 |
| TestSecurityManager | 7 | 安全管理 |
| TestAuditLogger | 5 | 审计日志 |
| TestBackupManager | 7 | 备份管理 |
| TestRecoveryTester | 4 | 恢复测试 |
| TestIncidentManager | 6 | 事件管理 |
| **总计** | **39** | |

---

## 四、测试环境

- **Python版本**: 3.12.10
- **测试框架**: pytest
- **被测模块**: src.infrastructure.security_backup

---

**文档版本**: v1.0
**最后更新**: 2026-04-06
**验收状态**: ✅ 通过
