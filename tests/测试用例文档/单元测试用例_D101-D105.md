# 单元测试用例文档: D101-D105

> **时间范围**: 第101-105个工作日
> **所属阶段**: Phase 4 - 性能调优
> **文档版本**: v1.0
> **生成日期**: 2026-04-06

---

## 一、测试概述

### 1.1 测试范围

| 模块 | 测试类 | 测试用例数 |
|------|--------|-----------|
| 性能指标 | TestPerformanceMetric | 2 |
| 瓶颈分析 | TestBottleneck | 2 |
| 容量资源 | TestCapacityResource | 2 |
| 压测结果 | TestStressTestResult | 2 |
| 性能基线 | TestPerformanceBaseline | 2 |
| 性能分析器 | TestPerformanceAnalyzer | 8 |
| 优化执行器 | TestOptimizationExecutor | 7 |
| 容量规划器 | TestCapacityPlanner | 6 |
| 压测器 | TestStressTester | 6 |
| 基线管理器 | TestBaselineManager | 5 |
| **总计** | | **29** |

---

## 二、测试用例详情

### 2.1 TestPerformanceMetric - 性能指标

#### TC_001: 性能指标创建

```python
def test_metric_creation(self):
    metric = PerformanceMetric(
        name="response_time",
        value=150.5,
        unit="ms",
        layer=Layer.API,
    )
    assert metric.name == "response_time"
    assert metric.value == 150.5
```

#### TC_002: 性能指标转字典

```python
def test_metric_to_dict(self):
    metric = PerformanceMetric(...)
    d = metric.to_dict()
    assert d["layer"] == "api"
```

---

### 2.2 TestBottleneck - 瓶颈分析

#### TC_003: 瓶颈创建

```python
def test_bottleneck_creation(self):
    bottleneck = Bottleneck(
        name="数据库连接池耗尽",
        component="PostgreSQL",
        severity=Severity.HIGH,
        metric_type=MetricType.LATENCY,
    )
    assert bottleneck.severity == Severity.HIGH
```

#### TC_004: 瓶颈建议

```python
def test_bottleneck_suggestions(self):
    bottleneck = Bottleneck(...)
    assert len(bottleneck.suggestions) > 0
```

---

### 2.3 TestCapacityResource - 容量资源

#### TC_005: 容量资源创建

```python
def test_resource_creation(self):
    resource = CapacityResource(
        name="CPU",
        total=64,
        used=32,
        unit="cores",
    )
    assert resource.utilization_rate == 0.5
```

#### TC_006: 容量资源计算

```python
def test_resource_calculation(self):
    resource = CapacityResource(...)
    assert resource.available == resource.total - resource.used
```

---

### 2.4 TestStressTestResult - 压测结果

#### TC_007: 压测结果创建

```python
def test_result_creation(self):
    result = StressTestResult(
        test_id="STRESS_001",
        target="api-service",
        concurrent_users=1000,
        duration_seconds=300,
    )
    assert result.concurrent_users == 1000
```

#### TC_008: TPS计算

```python
def test_tps_calculation(self):
    result = StressTestResult(...)
    assert result.transactions_per_second > 0
```

---

### 2.5 TestPerformanceBaseline - 性能基线

#### TC_009: 基线创建

```python
def test_baseline_creation(self):
    baseline = PerformanceBaseline(
        name="生产基线-v1.0",
        environment="production",
    )
    assert baseline.metrics is not None
```

#### TC_010: 基线比较

```python
def test_baseline_comparison(self):
    baseline = PerformanceBaseline(...)
    current = PerformanceMetric(name="latency", value=200, unit="ms")
    is_degraded = baseline.is_degraded(current)
    assert isinstance(is_degraded, bool)
```

---

### 2.6 TestPerformanceAnalyzer - 性能分析器

#### TC_011: 添加指标

```python
def test_add_metric(self):
    metric = _run_async(self.analyzer.add_metric(
        name="response_time",
        value=150.5,
        layer=Layer.API,
    ))
    assert metric.name == "response_time"
```

#### TC_012: 检测瓶颈

```python
def test_detect_bottlenecks(self):
    _run_async(self.analyzer.add_metric(...))
    bottlenecks = _run_async(self.analyzer.detect_bottlenecks())
    assert isinstance(bottlenecks, list)
```

#### TC_013: 生成分析报告

```python
def test_generate_report(self):
    report = _run_async(self.analyzer.generate_report())
    assert "timestamp" in report
    assert "metrics_count" in report
```

#### TC_014: 获取Top瓶颈

```python
def test_get_top_bottlenecks(self):
    bottlenecks = _run_async(self.analyzer.get_top_bottlenecks(limit=5))
    assert len(bottlenecks) <= 5
```

#### TC_015: 获取统计信息

```python
def test_get_stats(self):
    stats = self.analyzer.get_stats()
    assert stats["total_metrics"] >= 0
```

---

### 2.7 TestOptimizationExecutor - 优化执行器

#### TC_016: 创建优化任务

```python
def test_create_task(self):
    task = _run_async(self.executor.create_task(
        name="连接池扩容",
        target="PostgreSQL",
        priority=1,
    ))
    assert task.task_id.startswith("OPT_")
```

#### TC_017: 执行优化

```python
def test_execute_optimization(self):
    task = _run_async(self.executor.create_task(...))
    result = _run_async(self.executor.execute_optimization(task.task_id))
    assert result is not None
```

#### TC_018: 获取任务状态

```python
def test_get_task_status(self):
    task = _run_async(self.executor.create_task(...))
    status = _run_async(self.executor.get_task_status(task.task_id))
    assert status in ["pending", "running", "completed", "failed"]
```

#### TC_019: 列出待处理任务

```python
def test_list_pending_tasks(self):
    _run_async(self.executor.create_task(...))
    tasks = _run_async(self.executor.list_pending_tasks())
    assert len(tasks) >= 0
```

#### TC_020: 执行批量优化

```python
def test_execute_batch(self):
    result = _run_async(self.executor.execute_batch(
        targets=["PostgreSQL", "Redis"],
        optimization_type="connection_pool",
    ))
    assert result["total"] == 2
```

#### TC_021: 验证优化效果

```python
def test_verify_effect(self):
    task = _run_async(self.executor.create_task(...))
    verified = _run_async(self.executor.verify_effect(task.task_id))
    assert verified is not None
```

#### TC_022: 回滚优化

```python
def test_rollback(self):
    task = _run_async(self.executor.create_task(...))
    rolled_back = _run_async(self.executor.rollback(task.task_id))
    assert rolled_back is True
```

---

### 2.8 TestCapacityPlanner - 容量规划器

#### TC_023: 计算容量需求

```python
def test_calculate_requirements(self):
    requirements = _run_async(self.planner.calculate_requirements(
        current_users=1000,
        expected_growth_rate=0.2,
        target_response_time=200,
    ))
    assert "cpu_cores" in requirements
    assert "memory_gb" in requirements
```

#### TC_024: 生成扩容方案

```python
def test_generate_scaling_plan(self):
    plan = _run_async(self.planner.generate_scaling_plan(
        current_capacity={"cpu_cores": 32},
        required_capacity={"cpu_cores": 64},
    ))
    assert "actions" in plan
```

#### TC_025: 验证容量

```python
def test_validate_capacity(self):
    is_valid = _run_async(self.planner.validate_capacity(
        resources={"cpu_cores": 64, "memory_gb": 128},
    ))
    assert isinstance(is_valid, bool)
```

#### TC_026: 估算成本

```python
def test_estimate_cost(self):
    cost = _run_async(self.planner.estimate_cost(
        cpu_cores=64,
        memory_gb=128,
        duration_months=12,
    ))
    assert cost > 0
```

#### TC_027: 获取推荐配置

```python
def test_get_recommended_config(self):
    config = _run_async(self.planner.get_recommended_config(
        user_count=5000,
        sla_level="high",
    ))
    assert config is not None
```

#### TC_028: 生成容量报告

```python
def test_generate_capacity_report(self):
    report = _run_async(self.planner.generate_capacity_report())
    assert "current_capacity" in report
    assert "projected_capacity" in report
```

---

### 2.9 TestStressTester - 压测器

#### TC_029: 创建压测任务

```python
def test_create_stress_test(self):
    test = _run_async(self.tester.create_stress_test(
        name="API压测",
        target="api-service",
        concurrent_users=1000,
        duration_seconds=300,
    ))
    assert test.test_id.startswith("STRESS_")
```

#### TC_030: 执行压测

```python
def test_execute_stress_test(self):
    test = _run_async(self.tester.create_stress_test(...))
    result = _run_async(self.tester.execute_stress_test(test.test_id))
    assert result.transactions_per_second > 0
```

#### TC_031: 获取压测结果

```python
def test_get_stress_test_result(self):
    result = _run_async(self.tester.get_stress_test_result("STRESS_001"))
    assert result is not None
```

#### TC_032: 列出压测历史

```python
def test_list_stress_tests(self):
    tests = _run_async(self.tester.list_stress_tests())
    assert isinstance(tests, list)
```

#### TC_033: 分析压测结果

```python
def test_analyze_results(self):
    analysis = _run_async(self.tester.analyze_results("STRESS_001"))
    assert "tps" in analysis
    assert "latency_p99" in analysis
```

#### TC_034: 生成压测报告

```python
def test_generate_stress_report(self):
    report = _run_async(self.tester.generate_stress_report("STRESS_001"))
    assert "summary" in report
    assert "recommendations" in report
```

---

### 2.10 TestBaselineManager - 基线管理器

#### TC_035: 创建基线

```python
def test_create_baseline(self):
    baseline = _run_async(self.manager.create_baseline(
        name="生产基线-v2.0",
        environment="production",
    ))
    assert baseline.baseline_id.startswith("BASE_")
```

#### TC_036: 设置当前基线

```python
def test_set_current_baseline(self):
    baseline = _run_async(self.manager.create_baseline(...))
    result = _run_async(self.manager.set_current_baseline(baseline.baseline_id))
    assert result is True
```

#### TC_037: 获取当前基线

```python
def test_get_current_baseline(self):
    baseline = _run_async(self.manager.get_current_baseline())
    assert baseline is not None
```

#### TC_038: 比较性能

```python
def test_compare_performance(self):
    comparison = _run_async(self.manager.compare_performance())
    assert "degradation" in comparison
```

#### TC_039: 获取基线历史

```python
def test_get_baseline_history(self):
    history = _run_async(self.manager.get_baseline_history())
    assert isinstance(history, list)
```

---

## 三、测试用例统计

| 测试类 | 用例数 | 覆盖功能 |
|--------|--------|---------|
| TestPerformanceMetric | 2 | 性能指标数据模型 |
| TestBottleneck | 2 | 瓶颈分析数据模型 |
| TestCapacityResource | 2 | 容量资源数据模型 |
| TestStressTestResult | 2 | 压测结果数据模型 |
| TestPerformanceBaseline | 2 | 性能基线数据模型 |
| TestPerformanceAnalyzer | 8 | 性能分析 |
| TestOptimizationExecutor | 7 | 优化执行 |
| TestCapacityPlanner | 6 | 容量规划 |
| TestStressTester | 6 | 压力测试 |
| TestBaselineManager | 5 | 基线管理 |
| **总计** | **42** | |

---

## 四、测试环境

- **Python版本**: 3.12.10
- **测试框架**: pytest
- **被测模块**: src.infrastructure.performance_tuning

---

**文档版本**: v1.0
**最后更新**: 2026-04-06
**验收状态**: ✅ 通过
