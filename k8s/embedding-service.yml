apiVersion: apps/v1
kind: Deployment
metadata:
  name: pms-embedding-service
  namespace: pms-system
  labels:
    app.kubernetes.io/name: pms
    app.kubernetes.io/component: embedding-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: pms
      app.kubernetes.io/component: embedding-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pms
        app.kubernetes.io/component: embedding-service
    spec:
      containers:
        - name: embedding-service
          image: ${REGISTRY}/pms-embedding-service:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "uvicorn", "src.apps.embedding_service:app", "--host", "0.0.0.0", "--port", "8104"]
          ports:
            - containerPort: 8104
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: pms-embedding-service
  namespace: pms-system
spec:
  selector:
    app.kubernetes.io/name: pms
    app.kubernetes.io/component: embedding-service
  ports:
    - name: http
      port: 8104
      targetPort: http
