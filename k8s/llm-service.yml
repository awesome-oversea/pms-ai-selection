apiVersion: apps/v1
kind: Deployment
metadata:
  name: pms-llm-service
  namespace: pms-system
  labels:
    app.kubernetes.io/name: pms
    app.kubernetes.io/component: llm-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: pms
      app.kubernetes.io/component: llm-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pms
        app.kubernetes.io/component: llm-service
    spec:
      containers:
        - name: llm-service
          image: ${REGISTRY}/pms-llm-service:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "uvicorn", "src.apps.llm_service:app", "--host", "0.0.0.0", "--port", "8102"]
          ports:
            - containerPort: 8102
              name: http
          env:
            - name: APP_ENVIRONMENT
              value: production
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: pms-llm-service
  namespace: pms-system
spec:
  selector:
    app.kubernetes.io/name: pms
    app.kubernetes.io/component: llm-service
  ports:
    - name: http
      port: 8102
      targetPort: http
