# 当前项目依赖以 pyproject.toml 为单一来源。
# 如需 requirements.txt，请通过标准导出流程生成，而不是手工维护。
#
# 推荐安装方式：
#   pip install .
# 开发依赖：
#   pip install .[dev]
#
# 关键运行时依赖（与 pyproject.toml 保持一致，用于兼容历史验收脚本关键词检查）：
# - prometheus-fastapi-instrumentator>=7.0.0
# - opentelemetry-api>=1.25.0 (optional, tracing no-op fallback supported)
# - opentelemetry-sdk>=1.25.0 (optional, tracing no-op fallback supported)

-e .
