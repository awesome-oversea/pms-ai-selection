# 下一步项目方案与计划

> 本文档由以下历史版本整合而来：
>
> - `20260419下一步项目方案.md`（主版本，Phase-N真实闭环收口阶段方案）
> - `20260419下一步项目计划.md`（主版本，6周执行计划）
> - `2026041902下一步项目方案.md`（1902版，已归档）
> - `2026041902下一步项目计划.md`（1902版，已归档）
> - `2026041901下一步项目方案.md`（1901版，已归档）
> - `2026041901下一步项目计划.md`（1901版，已归档）
>
> 整合日期：2026-04-16
> 本文档为当前唯一有效方案与计划文档，历史版本已移入 `_archive/` 目录

***

> 文档日期：2026-04-19
>
> 适用阶段：2026-04-20 至 2026-05-31（建议 6 周）
>
> 方案依据：
>
> - `D:\Project\fms\跨境电商AI选品系统—分层架构与数据流协作.md`
> - `D:\Project\fms\跨境电商AI选品系统PMS—架构与业务设计文档.md`
> - `D:\Project\fms\选品系统设计方案.md`
> - `D:\Project\fms\20260419现有项目下一步优化处理意见.md`

## 1. 方案定位

本方案不是重新定义终极架构，而是在既有设计方案基础上，给出当前项目的**下一阶段可执行落地方案**。

当前阶段的核心任务不是继续横向增加模块，而是把已经铺开的架构能力收口成一条**真实可运行、可验证、可交付**的业务主链路。

本阶段建议命名为：

**Phase-N：真实闭环收口阶段**

## 2. 当前阶段判断

结合现有代码、测试和架构文档，项目现状可以概括为：

1. 分层架构、业务闭环、Agent 编排、知识库、工作台、治理能力已经基本成型。
2. 大量能力处于 `mock / compatible / local-real / remote-service` 混合状态。
3. 真实外部联调、真实推理、真实数据回流、真实基础设施运行证据仍然不足。
4. 当前项目最缺的不是新模块，而是“真实黄金链路”与“真实运行证据”。

因此，下一步项目方案必须围绕以下主目标展开：

- 做透一条真实黄金链路
- 固化模式治理
- 收口真实依赖
- 补齐运行级测试证据
- 为后续 K8s / HA / 多模态 / GraphRAG 升级建立稳定基线

## 3. 本阶段目标

### 3.1 总体目标

在 6 周内，完成一条从“选品任务创建”到“执行反馈回流”的最小真实业务闭环，并形成稳定验收包。

### 3.2 业务目标

以“**户外储能电源**”作为标准样板场景，打通以下链路：

1. 创建选品任务
2. 触发 Agent 编排执行
3. 输出推荐结果与风险结论
4. 完成审批流
5. 采纳推荐并生成执行动作
6. 接入 OMS / CRM / FMS / BI 的最小真实回流
7. 完成再评分与报告导出
8. 在工作台、日志、指标、审计中可追踪

### 3.3 技术目标

- 默认把关键链路从 `mock` 收口到 `local-real` 或 `remote-service`
- 固化单节点基础设施闭环：Postgres、Redis、Qdrant、OpenSearch、Kafka、Kong
- 修复可信回归中的阻塞失败项
- 建立“结构验证 / 本地运行验证 / 真实依赖验证”三层测试体系

### 3.4 验收目标

本阶段结束时，至少满足以下标准：

- 黄金链路可以在本地或准生产环境重复执行
- 工作台可以真实展示主链路结果而不是仅展示静态蓝图
- 关键状态接口能够明确返回当前模式、fallback 和依赖健康度
- 可信回归通过率恢复到高可用基线，黄金链路无阻塞失败
- 输出完整验收证据：运行截图、接口日志、测试报告、状态快照

## 4. 方案原则

### 4.1 收口优先

不再以“页面数量”“模块数量”“接口数量”作为主要推进指标，改为以主链路闭环完整度作为核心指标。

### 4.2 真实优先

默认优先推进：

- `local-real`
- `remote-service`

非必要不继续扩展新的 `mock-only` 能力。

### 4.3 分层不推翻

保留现有分层架构：

- 用户接入层
- 网关层
- UI 层
- API 层
- Agent 编排层
- AI 中台
- LLM / 推理引擎层
- 数据处理层
- 数据存储层
- 内外部集成层
- 监控运维层
- 基础设施层

本阶段重点是把这些层之间的关键依赖收口，而不是重做架构。

### 4.4 证据优先

任何“完成”必须同时具备：

- 代码落点
- 运行证据
- 测试证据
- 状态可观测性

## 5. 本阶段范围

### 5.1 纳入范围

#### A. 业务主链路

- 选品任务创建
- Agent 编排执行
- 审批
- 采纳
- 执行反馈回流
- 再评分
- 报告导出

#### B. 关键外部与内部数据

- 外部数据：Amazon、Google Trends
- 内部系统：OMS、CRM、FMS、BI
- 数据流：Kafka / Outbox / CDC 兼容路径

#### C. AI 能力

- LangGraph-compatible 编排主路径
- LLM Gateway 真实主路径收口
- RAG 本地真实索引与检索
- 报告生成主路径

#### D. 前端工作台

- 选品工作台
- Agent 平台
- 报告中心
- 运营台中的关键状态页

#### E. 质量与可观测

- 审计日志
- 指标
- Trace ID / Request ID
- 可信回归
- Playwright smoke

### 5.2 暂不纳入范围

以下事项本阶段只保留设计与接口，不作为主要交付目标：

- 多 AZ Kubernetes 集群
- HPA / Istio / 完整 HA 演练
- 大规模 GPU 资源池
- 完整 LLaVA / Whisper 生产化
- GraphRAG 真图底座全面上线
- 移动端 APP 正式交付

## 6. 核心实施策略

### 6.1 业务策略：围绕黄金链路建立样板

选定一个标准任务样板：

- 品类：户外储能电源
- 目标市场：美国
- 数据输入：Amazon + Google Trends + 内部 OMS / CRM / FMS / BI 最小集
- 输出：推荐、风险、利润、审批、执行反馈、报告

这样做的目的不是限制业务，而是先建立一个可复制的标准样板，后续再扩到更多品类。

### 6.2 编排策略：当前阶段接受 compatible，下一阶段再切原生

基于现有代码，当前编排核心仍以 `LangGraphCompatibleRunner` 为主，且已经具备：

- 图结构描述
- 并行执行
- 断点 / 快照 / 恢复
- 风险与报告串联

因此本阶段策略是：

- **先把现有 compatible 路径做成正式运行基线**
- 原生 LangGraph runtime 迁移放到下一阶段作为专项

### 6.3 AI 策略：先主链路真实化，再扩高级能力

本阶段优先级：

1. LLM Gateway 主路径去 `mock`
2. RAG 检索和写入稳定
3. 报告生成稳定
4. 再考虑多模态、微调、GraphRAG 深化

### 6.4 数据流策略：先单节点跑通，再升级分布式

本阶段采用：

- Kafka 单节点
- Postgres 单节点
- Redis 单节点
- Qdrant / OpenSearch 单节点
- Outbox / CDC compatible 路径

先保证链路稳定，再进入 Flink、Iceberg、K8s、HA 的升级阶段。

### 6.5 前端策略：从蓝图验收到真实交互验收

前端本阶段不再以页面数量为主，而是要把以下真实交互做实：

- 登录与鉴权
- 创建任务
- 查看执行流
- 审批
- 反馈
- 报告下载
- 工作台状态刷新

## 7. 工作流分解

### 工作流 A：黄金链路与业务闭环

目标：

- 从任务创建到再评分形成单条真实闭环

交付物：

- 选品样板任务配置
- 审批链路稳定版本
- 采纳执行与回流脚本
- 再评分输出结构
- 报告生成与下载结果

验收：

- 单次任务可以跑通
- 重复执行结果结构一致
- 审批、采纳、反馈动作在 API 与前端都可验证

### 工作流 B：模式治理与运行口径收口

目标：

- 明确当前所有核心能力的运行模式和降级规则

交付物：

- 模式矩阵文档
- `.env` 模板分层
- 关键状态接口返回统一模式字段
- fallback 记录与最近真实调用状态

验收：

- `mock / compatible / local-real / remote-service` 口径统一
- 状态页能看出当前实际运行模式

### 工作流 C：真实依赖与基础设施基线

目标：

- 单节点基础设施一键启动、稳定可观测

交付物：

- 本地栈启动脚本
- 健康检查脚本
- Kong upstream 校验
- Kafka / OpenSearch / Qdrant / Redis / Postgres 状态页

验收：

- 一键拉起成功
- 服务健康可见
- 黄金链路不因基础依赖随机失败

### 工作流 D：外部数据与 ERP 最小真实联调

目标：

- 完成最小真实数据输入和执行反馈输出

交付物：

- Amazon 最小真实采集
- Google Trends 最小真实采集
- OMS / CRM / FMS / BI 最小回流
- 样本数据规范与路径约束

验收：

- 至少 2 个外部源和 4 个内部域可以提供真实或准真实输入
- 不再依赖路径随意、样本缺失的临时数据

### 工作流 E：前端与 BFF 真实交互收口

目标：

- 让工作台从“页面齐全”变成“交互可验”

交付物：

- 选品工作台真实链路联调
- Agent 平台状态联调
- 报告中心下载联调
- BFF 关键接口稳定版

验收：

- Playwright smoke 覆盖主链路
- 前端页面不再仅依赖字符串断言证明完成

### 工作流 F：质量与测试体系升级

目标：

- 让测试结果能真实反映系统可交付能力

交付物：

- 修复当前可信回归失败项
- 三层测试分组
- Playwright 联调 smoke
- real Postgres / Qdrant 专项回归

验收：

- 黄金链路回归通过
- 真实依赖相关测试具备稳定通过标准

## 8. 分阶段计划

### 阶段 1：基线修复与模式收口

时间：2026-04-20 至 2026-05-03

重点：

- 修复当前可信回归中的阻塞失败
- 固化模式矩阵
- 明确黄金链路样板任务
- 清理样本路径、配置路径和运行口径不一致问题

主要交付：

- 回归失败清零或压缩到非阻塞范围
- 模式矩阵 v1
- 黄金链路样板定义
- 依赖栈健康脚本

### 阶段 2：黄金链路打通

时间：2026-05-04 至 2026-05-17

重点：

- 打通选品 → 审批 → 采纳 → 回流 → 再评分 → 报告
- 收口 OMS / CRM / FMS / BI 最小回流
- 工作台和 BFF 联调

主要交付：

- 样板链路真实运行版本
- 前后端联调 smoke
- 回流数据结果结构固定

### 阶段 3：证据固化与准生产验收

时间：2026-05-18 至 2026-05-31

重点：

- 指标、审计、日志、状态页补齐
- 验收脚本和运行手册输出
- staging / UAT 默认关闭 mock

主要交付：

- 验收证据包
- 状态看板
- 运维手册
- 阶段复盘与下一阶段升级清单

## 9. 关键里程碑

| 里程碑        | 时间         | 标准                    |
| ---------- | ---------- | --------------------- |
| M1：基线稳定    | 2026-05-03 | 可信回归阻塞问题修复，模式矩阵明确     |
| M2：黄金链路跑通  | 2026-05-17 | 样板任务完成从创建到回流的真实闭环     |
| M3：准生产验收完成 | 2026-05-31 | 有完整证据包、运行手册、状态观测和回归结果 |

## 10. 本阶段交付物

### 文档类

- 下一步项目方案
- 模式矩阵文档
- 黄金链路验收说明
- 本地栈启动说明
- 阶段复盘文档

### 代码类

- 黄金链路相关服务收口代码
- 样本路径与配置治理修复
- 模式状态接口
- Playwright smoke 与真实依赖回归

### 运维类

- 单节点依赖栈脚本
- 健康检查脚本
- 状态快照导出
- 验收证据包

## 11. 验收标准

### 11.1 业务验收

- 可以创建样板任务并成功执行
- 可以完成审批、采纳、反馈、再评分
- 可以输出正式报告并下载

### 11.2 技术验收

- 关键依赖单节点可重复启动
- 关键状态接口清晰返回模式与健康度
- 审计、Trace、指标可以追踪一次完整任务

### 11.3 测试验收

- 可信回归恢复到高稳定状态
- 黄金链路 Playwright smoke 通过
- real Postgres / Qdrant 专项回归可稳定执行

## 12. 组织建议

本阶段建议按 6 条工作流分工，而不是按模块散点推进：

- 后端主链路负责人：黄金链路与服务拆分
- AI / 编排负责人：模式治理与 Agent 主路径
- 数据 / 集成负责人：外部数据与 ERP 回流
- 前端负责人：工作台与 BFF 联调
- 测试负责人：可信回归、Playwright、验收证据
- 运维负责人：单节点栈、状态观测、部署脚本

## 13. 风险与应对

| 风险            | 表现                             | 应对                       |
| ------------- | ------------------------------ | ------------------------ |
| 模式口径继续混乱      | “已完成”但实际仍是 mock                | 以模式矩阵和状态接口统一口径           |
| 真实依赖不稳定       | Postgres / Qdrant / Kafka 随机失败 | 先做单节点基线和健康脚本             |
| 服务过重导致改动风险高   | `SelectionTaskService` 持续膨胀    | 先拆应用服务、审批服务、反馈服务         |
| 前端验收失真        | 页面存在但交互不通                      | 强制加入 Playwright 联调 smoke |
| 过早推进 HA / K8s | 投入大但闭环仍未跑通                     | 本阶段明确暂缓，仅保留接口和脚本         |

## 14. 立即执行清单

建议先按以下顺序启动：

1. 修复本轮可信回归中的 `11` 个失败
2. 输出模式矩阵与环境模板
3. 固定样板场景为“户外储能电源”
4. 明确黄金链路接口清单
5. 收口 OMS / CRM / FMS / BI 最小回流
6. 拆分 `SelectionTaskService`
7. 增加 Playwright 主链路 smoke
8. 完成本地依赖栈健康检查脚本
9. 打通 Kong 与宿主应用主路由
10. 输出阶段验收证据目录结构

## 15. 结论

下一步项目方案的核心不是“继续建设更大的目标态”，而是：

**把现有架构能力变成一条真实、稳定、可重复验收的项目主通路。**

只有完成这一轮收口，后续再推进：

- K8s / HA
- 多模态生产化
- GraphRAG 真图底座
- 大规模 GPU 推理
- 多租户生产级扩展

才是有效投资，而不是提前扩面。

***

## 第二部分：执行计划

> 文档日期：2026-04-19
>
> 计划周期：2026-04-20 至 2026-05-31（6 周）
>
> 计划依据：
>
> - `D:\Project\fms\跨境电商AI选品系统—分层架构与数据流协作.md`
> - `D:\Project\fms\跨境电商AI选品系统PMS—架构与业务设计文档.md`
> - `D:\Project\fms\选品系统设计方案.md`
> - `D:\Project\fms\20260419现有项目下一步优化处理意见.md`
> - `D:\Project\fms\20260419下一步项目方案.md`

## 1. 计划目标

本阶段不再继续横向扩展模块数量，而是围绕一个真实业务样板，完成从“可演示”到“可重复交付”的收口。

计划总目标如下：

1. 以“户外储能电源”作为样板场景，打通真实黄金链路。
2. 将关键运行模式从 `mock / compatible` 收口到 `local-real / remote-service`。
3. 修复当前可信回归中的阻塞失败项，恢复高稳定基线。
4. 建立真实依赖、前后端联调、验收证据三位一体的交付方式。

## 2. 当前基线

当前项目已经具备较完整的架构与业务覆盖，但下一阶段的主问题不是“功能不够多”，而是“真实运行证据不够完整”。

当前计划以以下事实作为基线：

- 主链路、Agent 编排、审批、反馈、报告、租户、审计、工作台能力已有落点。
- 核心能力仍混合存在 `mock / compatible / local-real / remote-service` 多种模式。
- 可信回归执行结果为 `240` 项中 `229` 通过、`11` 失败。
- 当前失败主要集中在 OCR 模式契约、样本路径健壮性、real Postgres / real Qdrant 持久化稳定性。

## 3. 本阶段交付定义

本阶段以“真实闭环收口”为唯一主线，最终必须交付以下结果：

### 3.1 业务交付

- 可以创建“户外储能电源”选品任务
- 可以触发 Agent 执行并产出推荐、风险、利润结果
- 可以完成审批、采纳、执行反馈、再评分
- 可以导出正式报告并在工作台查看结果

### 3.2 技术交付

- 关键状态接口能返回当前模式、fallback、依赖健康度
- Postgres、Redis、Qdrant、OpenSearch、Kafka、Kong 单节点栈可重复启动
- `SelectionTaskService` 开始按职责拆分，降低后续改动风险
- 前后端联调链路可以稳定跑通

### 3.3 质量交付

- 当前 `11` 个阻塞失败项完成修复或压缩到非阻塞范围
- 建立结构层、本地运行层、真实依赖层三层验证体系
- 新增主链路 Playwright smoke
- 形成阶段验收证据包与运行手册

## 4. 计划范围

### 4.1 纳入范围

- 选品任务创建、执行、审批、采纳、反馈、再评分、报告导出
- Amazon、Google Trends 最小真实输入
- OMS、CRM、FMS、BI 最小真实回流
- LangGraph-compatible 主路径、LLM Gateway 主路径、RAG 检索与报告生成
- 前端工作台、BFF、状态页、报告中心
- 审计、指标、Trace、可信回归、Playwright smoke

### 4.2 暂不纳入范围

- 多 AZ Kubernetes 集群
- HPA、Istio、完整 HA 演练
- GraphRAG 真图底座全面上线
- LLaVA / Whisper 全量生产化
- 移动端 APP 正式交付

## 5. 六周执行计划

### 第 1 周：基线修复与样板冻结

时间：2026-04-20 至 2026-04-26

重点任务：

1. 修复可信回归中的 OCR 模式契约问题。
2. 修复本地样本路径与 `dummy/fms/finance-metrics` 相关健壮性问题。
3. 梳理 real Postgres / real Qdrant 失败用例的连接与持久化问题。
4. 冻结样板场景：`户外储能电源 / 美国市场`。
5. 输出模式矩阵 v1，明确 `mock / compatible / local-real / remote-service` 口径。

周交付：

- 回归失败分类清单与修复顺序
- 样板任务定义文档
- 模式矩阵 v1
- 环境变量模板 v1

周验收：

- OCR 与样本路径问题具备明确修复方案并完成首轮验证
- 样板任务字段、输入源、输出结构冻结
- 所有核心模块具备统一模式口径说明

### 第 2 周：服务收口与依赖基线

时间：2026-04-27 至 2026-05-03

重点任务：

1. 启动 `SelectionTaskService` 拆分，先拆应用编排、审批、反馈闭环三类职责。
2. 完成单节点依赖栈启动脚本与健康检查脚本。
3. 补齐 Kong upstream、主应用、关键依赖状态页。
4. 将关键状态接口统一返回模式、fallback、最近真实调用结果。

周交付：

- `SelectionTaskService` 拆分方案与首批代码落点
- 单节点依赖栈启动脚本
- 健康检查脚本
- 状态接口统一响应结构

周验收：

- 单节点依赖可重复拉起
- 状态页可见主链路依赖健康度
- 可信回归阻塞问题数量明显下降

### 第 3 周：外部输入与内部回流打通

时间：2026-05-04 至 2026-05-10

重点任务：

1. 打通 Amazon 最小真实采集链路。
2. 打通 Google Trends 最小真实采集链路。
3. 接入 OMS / CRM / FMS / BI 最小真实回流。
4. 固定回流事件结构、失败重试口径和样本路径规范。

周交付：

- 外部输入适配结果
- 内部回流接口清单
- 回流事件结构与路径规范
- 样板任务端到端输入输出结构文档

周验收：

- 至少 2 个外部输入源可用于样板任务
- 至少 4 个内部域具备最小回流能力
- 不再依赖临时样本路径和随意文件结构

### 第 4 周：黄金链路联调

时间：2026-05-11 至 2026-05-17

重点任务：

1. 打通“创建任务 → Agent 执行 → 审批 → 采纳 → 回流 → 再评分 → 报告”的真实链路。
2. 完成 BFF 与前端工作台联调。
3. 固化报告中心下载、状态刷新、执行过程查看等关键交互。
4. 输出黄金链路接口清单与验收动作清单。

周交付：

- 黄金链路真实运行版本
- 前后端联调结果
- 报告中心下载联调结果
- 黄金链路验收动作清单

周验收：

- 样板任务可以真实跑通一次完整流程
- API 与前端都可验证审批、采纳、反馈动作
- 报告下载与结果查看流程稳定

### 第 5 周：测试体系升级与证据补齐

时间：2026-05-18 至 2026-05-24

重点任务：

1. 建立结构层、本地运行层、真实依赖层三层测试分组。
2. 增加主链路 Playwright smoke。
3. 固化 real Postgres / real Qdrant 专项回归。
4. 补齐审计、Trace、指标、状态快照导出。

周交付：

- 三层测试分组说明
- Playwright 主链路 smoke
- real persistence 专项回归集
- 审计、Trace、指标、状态快照样例

周验收：

- 主链路 smoke 可重复通过
- 真实依赖回归具备稳定执行标准
- 一次完整任务具备可追踪证据

### 第 6 周：准生产验收与阶段收口

时间：2026-05-25 至 2026-05-31

重点任务：

1. 在 staging / UAT 默认关闭 `mock`。
2. 输出运行手册、验收手册、问题回退手册。
3. 整理阶段验收证据包。
4. 形成下一阶段升级清单。

周交付：

- staging / UAT 非 mock 运行结果
- 运行手册
- 验收证据包
- 阶段复盘与下阶段清单

周验收：

- 黄金链路可在准生产环境重复执行
- 验收证据完整可追溯
- 下阶段工作具备清晰边界和优先级

## 6. 工作流分工建议

建议按工作流而不是按模块散点推进：

| 工作流      | 负责人角色        | 重点职责                  |
| -------- | ------------ | --------------------- |
| A. 黄金链路  | 后端主链路负责人     | 任务、审批、采纳、反馈、再评分       |
| B. 模式治理  | AI / 平台负责人   | 模式矩阵、状态接口、fallback 口径 |
| C. 依赖基线  | 运维 / 平台负责人   | 单节点栈、健康检查、状态观测        |
| D. 数据与集成 | 数据 / 集成负责人   | 外部输入、内部回流、事件结构        |
| E. 前端联调  | 前端 / BFF 负责人 | 工作台、报告中心、交互链路         |
| F. 质量验收  | 测试负责人        | 回归、Playwright、证据包     |

## 7. 关键里程碑

| 里程碑        | 时间         | 判断标准                    |
| ---------- | ---------- | ----------------------- |
| M1：基线稳定    | 2026-05-03 | 可信回归阻塞问题下降，模式矩阵与依赖基线完成  |
| M2：黄金链路跑通  | 2026-05-17 | 样板任务完成从创建到回流的真实闭环       |
| M3：准生产验收完成 | 2026-05-31 | 有完整证据包、运行手册、状态观测和稳定回归结果 |

## 8. 本周立即启动清单

建议按以下顺序直接开工：

1. 修复可信回归中的 `11` 个失败项。
2. 输出模式矩阵与环境模板。
3. 冻结样板场景与黄金链路接口清单。
4. 建立 real Postgres / real Qdrant 稳定性问题清单。
5. 启动 `SelectionTaskService` 职责拆分。
6. 补齐单节点依赖栈与健康检查脚本。
7. 规划 Playwright 主链路 smoke。

## 9. 风险与控制措施

| 风险       | 表现                               | 控制措施                     |
| -------- | -------------------------------- | ------------------------ |
| 模式口径混乱   | 看似完成，实际仍运行在 mock                 | 模式矩阵、状态接口、环境模板同步收口       |
| 真实依赖不稳定  | Postgres / Qdrant / Kafka 随机失败   | 先做单节点基线和专项回归             |
| 服务持续膨胀   | `SelectionTaskService` 成为后续改动阻塞点 | 按职责拆分，优先拆核心闭环能力          |
| 前端验收失真   | 页面存在但交互不通                        | 强制纳入 Playwright 联调 smoke |
| 过早扩展基础设施 | K8s / HA 投入增加但主链路未闭环             | 本阶段坚持单节点基线优先             |

## 10. 计划结论

下一步项目计划的核心不是“继续扩面”，而是“围绕真实黄金链路完成阶段收口”。

只要本阶段完成以下三件事，项目就能从“能力铺设阶段”进入“可交付基线阶段”：

1. 跑通真实链路
2. 修复关键阻塞
3. 固化运行证据

完成这一步后，再进入 K8s / HA、多模态、GraphRAG、生产级扩展，投入才是有效的。
