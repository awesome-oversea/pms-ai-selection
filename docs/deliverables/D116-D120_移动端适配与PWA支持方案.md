# 移动端适配与PWA支持方案

> **项目名称**: 跨境电商AI选品系统（PMS增强版）
> **文档类型**: 技术设计文档
> **子任务**: D116-D120 移动端适配与PWA支持
> **文档版本**: v1.0

---

## 目录

- [1. 概述](#1-概述)
- [2. 响应式设计](#2-响应式设计)
- [3. 移动端适配](#3-移动端适配)
- [4. PWA实现](#4-pwa实现)
- [5. 离线功能](#5-离线功能)
- [6. 性能优化](#6-性能优化)

---

## 1. 概述

### 1.1 设计目标

为AI选品系统提供完整的移动端支持和PWA能力，实现跨平台一致的用户体验。

### 1.2 支持平台

| 平台 | 支持方式 | 说明 |
|------|---------|------|
| iOS Safari | 响应式Web | iOS 14+ |
| Android Chrome | PWA | Android 8+ |
| 微信内置浏览器 | 响应式Web | 微信8.0+ |
| 桌面浏览器 | 响应式Web | Chrome/Safari/Firefox |

---

## 2. 响应式设计

### 2.1 断点设计

```css
:root {
  --breakpoint-xs: 0;
  --breakpoint-sm: 576px;
  --breakpoint-md: 768px;
  --breakpoint-lg: 992px;
  --breakpoint-xl: 1200px;
  --breakpoint-xxl: 1400px;
}

@media (max-width: 575.98px) {
  .container { padding: 0 16px; }
  .grid { grid-template-columns: 1fr; }
}

@media (min-width: 576px) and (max-width: 767.98px) {
  .container { max-width: 540px; }
  .grid { grid-template-columns: repeat(2, 1fr); }
}

@media (min-width: 768px) and (max-width: 991.98px) {
  .container { max-width: 720px; }
  .grid { grid-template-columns: repeat(3, 1fr); }
}

@media (min-width: 992px) and (max-width: 1199.98px) {
  .container { max-width: 960px; }
  .grid { grid-template-columns: repeat(4, 1fr); }
}

@media (min-width: 1200px) {
  .container { max-width: 1140px; }
  .grid { grid-template-columns: repeat(6, 1fr); }
}
```

### 2.2 布局系统

```css
.flex-layout {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.flex-layout .header {
  flex-shrink: 0;
}

.flex-layout .main {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.flex-layout .footer {
  flex-shrink: 0;
}

.grid-layout {
  display: grid;
  grid-template-columns: 240px 1fr;
  grid-template-rows: auto 1fr auto;
  grid-template-areas:
    "header header"
    "sidebar main"
    "footer footer";
  min-height: 100vh;
}

@media (max-width: 767.98px) {
  .grid-layout {
    grid-template-columns: 1fr;
    grid-template-areas:
      "header"
      "main"
      "footer";
  }
  
  .grid-layout .sidebar {
    position: fixed;
    left: -240px;
    transition: left 0.3s ease;
  }
  
  .grid-layout .sidebar.open {
    left: 0;
  }
}
```

### 2.3 组件适配

```css
.card {
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  padding: 16px;
  margin-bottom: 16px;
}

@media (max-width: 575.98px) {
  .card {
    border-radius: 0;
    margin-left: -16px;
    margin-right: -16px;
    margin-bottom: 0;
    border-bottom: 1px solid #eee;
  }
}

.button {
  padding: 12px 24px;
  font-size: 14px;
  border-radius: 4px;
}

@media (max-width: 575.98px) {
  .button {
    width: 100%;
    padding: 14px 24px;
    font-size: 16px;
  }
}

.input {
  padding: 12px 16px;
  font-size: 14px;
  border-radius: 4px;
}

@media (max-width: 575.98px) {
  .input {
    font-size: 16px;
  }
}
```

---

## 3. 移动端适配

### 3.1 触摸交互

```javascript
class TouchHandler {
  constructor(element) {
    this.element = element;
    this.startX = 0;
    this.startY = 0;
    this.threshold = 50;
    
    this.bindEvents();
  }
  
  bindEvents() {
    this.element.addEventListener('touchstart', this.handleTouchStart.bind(this));
    this.element.addEventListener('touchmove', this.handleTouchMove.bind(this));
    this.element.addEventListener('touchend', this.handleTouchEnd.bind(this));
  }
  
  handleTouchStart(event) {
    this.startX = event.touches[0].clientX;
    this.startY = event.touches[0].clientY;
  }
  
  handleTouchMove(event) {
    if (!this.startX || !this.startY) return;
    
    const currentX = event.touches[0].clientX;
    const currentY = event.touches[0].clientY;
    
    const diffX = this.startX - currentX;
    const diffY = this.startY - currentY;
    
    if (Math.abs(diffX) > Math.abs(diffY)) {
      if (diffX > this.threshold) {
        this.onSwipeLeft();
      } else if (diffX < -this.threshold) {
        this.onSwipeRight();
      }
    }
  }
  
  handleTouchEnd() {
    this.startX = 0;
    this.startY = 0;
  }
  
  onSwipeLeft() {
    console.log('Swiped left');
  }
  
  onSwipeRight() {
    console.log('Swiped right');
  }
}
```

### 3.2 移动端导航

```html
<nav class="mobile-nav">
  <div class="nav-header">
    <button class="menu-toggle" aria-label="Toggle menu">
      <span class="hamburger"></span>
    </button>
    <a class="logo" href="/">FMS</a>
    <button class="search-toggle" aria-label="Search">
      <svg><!-- search icon --></svg>
    </button>
  </div>
  
  <div class="nav-drawer">
    <div class="drawer-header">
      <span class="user-name">用户名</span>
      <button class="close-drawer" aria-label="Close menu">×</button>
    </div>
    
    <ul class="nav-list">
      <li><a href="/dashboard">仪表盘</a></li>
      <li><a href="/products">商品管理</a></li>
      <li><a href="/selection">选品分析</a></li>
      <li><a href="/reports">报告中心</a></li>
      <li><a href="/settings">设置</a></li>
    </ul>
  </div>
</nav>
```

```css
.mobile-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: #fff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.nav-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
}

.nav-drawer {
  position: fixed;
  top: 0;
  left: -280px;
  width: 280px;
  height: 100vh;
  background: #fff;
  transition: left 0.3s ease;
  z-index: 1001;
}

.nav-drawer.open {
  left: 0;
}

.drawer-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease;
  z-index: 1000;
}

.drawer-overlay.open {
  opacity: 1;
  visibility: visible;
}
```

### 3.3 移动端表格

```html
<div class="responsive-table">
  <table class="data-table">
    <thead>
      <tr>
        <th>商品名称</th>
        <th>价格</th>
        <th>销量</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td data-label="商品名称">户外便携电源</td>
        <td data-label="价格">¥299.00</td>
        <td data-label="销量">1,234</td>
        <td data-label="操作">
          <button class="btn-icon">查看</button>
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

```css
@media (max-width: 767.98px) {
  .responsive-table table {
    border-collapse: collapse;
  }
  
  .responsive-table thead {
    display: none;
  }
  
  .responsive-table tbody tr {
    display: block;
    margin-bottom: 16px;
    border: 1px solid #eee;
    border-radius: 8px;
  }
  
  .responsive-table td {
    display: flex;
    justify-content: space-between;
    padding: 12px 16px;
    border-bottom: 1px solid #eee;
  }
  
  .responsive-table td:last-child {
    border-bottom: none;
  }
  
  .responsive-table td::before {
    content: attr(data-label);
    font-weight: 600;
  }
}
```

---

## 4. PWA实现

### 4.1 Manifest配置

```json
{
  "name": "FMS AI选品系统",
  "short_name": "FMS",
  "description": "跨境电商AI智能选品系统",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait-primary",
  "background_color": "#ffffff",
  "theme_color": "#1890ff",
  "icons": [
    {
      "src": "/icons/icon-72x72.png",
      "sizes": "72x72",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-96x96.png",
      "sizes": "96x96",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-128x128.png",
      "sizes": "128x128",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-144x144.png",
      "sizes": "144x144",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-152x152.png",
      "sizes": "152x152",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-384x384.png",
      "sizes": "384x384",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ],
  "screenshots": [
    {
      "src": "/screenshots/home.png",
      "sizes": "1280x720",
      "type": "image/png",
      "label": "首页"
    }
  ],
  "shortcuts": [
    {
      "name": "选品分析",
      "short_name": "选品",
      "description": "创建新的选品分析任务",
      "url": "/selection/create",
      "icons": [{ "src": "/icons/selection.png", "sizes": "96x96" }]
    },
    {
      "name": "报告中心",
      "short_name": "报告",
      "description": "查看分析报告",
      "url": "/reports",
      "icons": [{ "src": "/icons/report.png", "sizes": "96x96" }]
    }
  ]
}
```

### 4.2 Service Worker

```javascript
const CACHE_NAME = 'fms-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/styles/main.css',
  '/scripts/main.js',
  '/icons/icon-192x192.png',
  '/icons/icon-512x512.png'
];

const API_CACHE = 'pms-api-v1';
const API_PATTERNS = [
  /\/api\/v1\/products/,
  /\/api\/v1\/categories/
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll(STATIC_ASSETS);
    })
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((cacheNames) => {
      return Promise.all(
        cacheNames
          .filter((name) => name !== CACHE_NAME && name !== API_CACHE)
          .map((name) => caches.delete(name))
      );
    })
  );
});

self.addEventListener('fetch', (event) => {
  const { request } = event;
  
  if (isApiRequest(request)) {
    event.respondWith(handleApiRequest(request));
  } else {
    event.respondWith(handleStaticRequest(request));
  }
});

function isApiRequest(request) {
  return API_PATTERNS.some(pattern => pattern.test(request.url));
}

async function handleApiRequest(request) {
  const cache = await caches.open(API_CACHE);
  
  try {
    const response = await fetch(request);
    
    if (response.ok) {
      cache.put(request, response.clone());
    }
    
    return response;
  } catch (error) {
    const cached = await cache.match(request);
    
    if (cached) {
      return cached;
    }
    
    return new Response(JSON.stringify({ error: 'Offline' }), {
      status: 503,
      headers: { 'Content-Type': 'application/json' }
    });
  }
}

async function handleStaticRequest(request) {
  const cache = await caches.open(CACHE_NAME);
  const cached = await cache.match(request);
  
  if (cached) {
    return cached;
  }
  
  try {
    const response = await fetch(request);
    cache.put(request, response.clone());
    return response;
  } catch (error) {
    return caches.match('/offline.html');
  }
}
```

### 4.3 安装提示

```javascript
class PWAInstaller {
  constructor() {
    this.deferredPrompt = null;
    this.installButton = null;
    
    this.init();
  }
  
  init() {
    window.addEventListener('beforeinstallprompt', (event) => {
      event.preventDefault();
      this.deferredPrompt = event;
      this.showInstallButton();
    });
    
    window.addEventListener('appinstalled', () => {
      this.deferredPrompt = null;
      this.hideInstallButton();
      this.trackInstall();
    });
  }
  
  showInstallButton() {
    this.installButton = document.createElement('button');
    this.installButton.className = 'pwa-install-btn';
    this.installButton.textContent = '安装应用';
    this.installButton.addEventListener('click', () => this.promptInstall());
    
    document.body.appendChild(this.installButton);
  }
  
  hideInstallButton() {
    if (this.installButton) {
      this.installButton.remove();
      this.installButton = null;
    }
  }
  
  async promptInstall() {
    if (!this.deferredPrompt) return;
    
    this.deferredPrompt.prompt();
    
    const { outcome } = await this.deferredPrompt.userChoice;
    
    if (outcome === 'accepted') {
      console.log('User accepted the install prompt');
    }
    
    this.deferredPrompt = null;
  }
  
  trackInstall() {
    if (typeof gtag !== 'undefined') {
      gtag('event', 'pwa_install', {
        event_category: 'PWA',
        event_label: 'Install'
      });
    }
  }
}
```

---

## 5. 离线功能

### 5.1 离线数据存储

```javascript
class OfflineStorage {
  constructor() {
    this.dbName = 'fms-offline';
    this.dbVersion = 1;
    this.db = null;
  }
  
  async init() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(this.dbName, this.dbVersion);
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => {
        this.db = request.result;
        resolve();
      };
      
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        
        if (!db.objectStoreNames.contains('products')) {
          db.createObjectStore('products', { keyPath: 'product_id' });
        }
        
        if (!db.objectStoreNames.contains('tasks')) {
          db.createObjectStore('tasks', { keyPath: 'task_id' });
        }
        
        if (!db.objectStoreNames.contains('pending')) {
          const store = db.createObjectStore('pending', { keyPath: 'id', autoIncrement: true });
          store.createIndex('type', 'type');
          store.createIndex('timestamp', 'timestamp');
        }
      };
    });
  }
  
  async saveProducts(products) {
    const tx = this.db.transaction('products', 'readwrite');
    const store = tx.objectStore('products');
    
    for (const product of products) {
      await store.put(product);
    }
  }
  
  async getProducts() {
    const tx = this.db.transaction('products', 'readonly');
    const store = tx.objectStore('products');
    
    return new Promise((resolve, reject) => {
      const request = store.getAll();
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }
  
  async addPendingAction(action) {
    const tx = this.db.transaction('pending', 'readwrite');
    const store = tx.objectStore('pending');
    
    return store.add({
      ...action,
      timestamp: Date.now()
    });
  }
  
  async getPendingActions() {
    const tx = this.db.transaction('pending', 'readonly');
    const store = tx.objectStore('pending');
    
    return new Promise((resolve, reject) => {
      const request = store.getAll();
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }
  
  async clearPendingAction(id) {
    const tx = this.db.transaction('pending', 'readwrite');
    const store = tx.objectStore('pending');
    
    return store.delete(id);
  }
}
```

### 5.2 离线同步

```javascript
class OfflineSync {
  constructor(storage, api) {
    this.storage = storage;
    this.api = api;
    this.syncing = false;
  }
  
  async sync() {
    if (this.syncing) return;
    this.syncing = true;
    
    try {
      const pending = await this.storage.getPendingActions();
      
      for (const action of pending) {
        try {
          await this.executeAction(action);
          await this.storage.clearPendingAction(action.id);
        } catch (error) {
          console.error('Failed to sync action:', action, error);
        }
      }
    } finally {
      this.syncing = false;
    }
  }
  
  async executeAction(action) {
    switch (action.type) {
      case 'create_task':
        return await this.api.createTask(action.data);
      case 'update_task':
        return await this.api.updateTask(action.id, action.data);
      case 'delete_task':
        return await this.api.deleteTask(action.id);
      default:
        throw new Error(`Unknown action type: ${action.type}`);
    }
  }
}

navigator.serviceWorker.ready.then((registration) => {
  if ('sync' in registration) {
    registration.sync.register('offline-sync');
  }
});

self.addEventListener('sync', (event) => {
  if (event.tag === 'offline-sync') {
    event.waitUntil(syncOfflineData());
  }
});
```

---

## 6. 性能优化

### 6.1 图片优化

```html
<picture>
  <source 
    media="(max-width: 575px)"
    srcset="image-small.webp 1x, image-small@2x.webp 2x"
    type="image/webp"
  >
  <source 
    media="(min-width: 576px)"
    srcset="image-large.webp 1x, image-large@2x.webp 2x"
    type="image/webp"
  >
  <img 
    src="image-fallback.jpg" 
    alt="Product image"
    loading="lazy"
    decoding="async"
  >
</picture>
```

### 6.2 代码分割

```javascript
const routes = {
  '/': () => import('./pages/Home.js'),
  '/products': () => import('./pages/Products.js'),
  '/selection': () => import('./pages/Selection.js'),
  '/reports': () => import('./pages/Reports.js'),
  '/settings': () => import('./pages/Settings.js')
};

async function loadRoute(path) {
  const loader = routes[path];
  
  if (loader) {
    const module = await loader();
    return module.default;
  }
  
  return import('./pages/NotFound.js').then(m => m.default);
}
```

### 6.3 预加载策略

```html
<head>
  <link rel="preload" href="/styles/main.css" as="style">
  <link rel="preload" href="/scripts/main.js" as="script">
  <link rel="preconnect" href="https://api.fms.example.com">
  <link rel="dns-prefetch" href="https://cdn.fms.example.com">
</head>
```

---

## 附录

### A. 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0 | 2026-04-06 | AI助手 | 初始版本 |

---

**文档状态**: ✅ 已完成
