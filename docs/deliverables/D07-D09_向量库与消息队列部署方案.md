# 向量库与消息队列部署方案

> **项目名称**: 跨境电商AI选品系统（PMS增强版）
> **文档类型**: 技术设计文档
> **子任务**: D7-D9 向量库与消息队列
> **文档版本**: v1.0
>
> **更新说明（2026-04-19）**: 当前本地默认文本推理方案已切换为 `WSL + Ollama + Qwen2.5-1.5B (GGUF量化)`。本文中的 vLLM 相关章节保留为历史高配 / 扩展路线，不再作为本地默认执行方案。

---

## 目录

- [1. 概述](#1-概述)
- [2. Qdrant向量数据库](#2-qdrant向量数据库)
- [3. Kafka消息队列](#3-kafka消息队列)
- [4. vLLM推理服务](#4-vllm推理服务)
- [5. 数据库Schema](#5-数据库schema)

---

## 1. 概述

### 1.1 部署目标

完成向量数据库、消息队列和LLM推理服务的部署，为AI选品系统提供核心能力支撑。

### 1.2 组件规划

| 组件 | 数量 | 规格 | 用途 |
|------|------|------|------|
| Qdrant | 3 | 4核8G | 向量存储与检索 |
| Kafka | 3 | 4核8G | 消息队列 |
| vLLM | 1 | 8核32G + A10 | LLM推理 |

---

## 2. Qdrant向量数据库

### 2.1 集群架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Qdrant Raft集群                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Raft共识层                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ Leader   │ │ Follower │ │ Follower │               │   │
│  │  │ Node-1   │ │ Node-2   │ │ Node-3   │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    存储层                                │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ HNSW索引 │ │ Payload  │ │ Snapshot │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 StatefulSet配置

```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: qdrant
  namespace: pms-data
spec:
  serviceName: qdrant-headless
  replicas: 3
  selector:
    matchLabels:
      app: qdrant
  template:
    metadata:
      labels:
        app: qdrant
    spec:
      containers:
        - name: qdrant
          image: qdrant/qdrant:v1.7.0
          ports:
            - containerPort: 6333
              name: http
            - containerPort: 6334
              name: grpc
          env:
            - name: QDRANT__CLUSTER__ENABLED
              value: "true"
            - name: QDRANT__CLUSTER__P2P__PORT
              value: "6335"
          volumeMounts:
            - name: qdrant-storage
              mountPath: /qdrant/storage
          livenessProbe:
            httpGet:
              path: /health
              port: 6333
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 6333
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: qdrant-storage
          persistentVolumeClaim:
            claimName: qdrant-pvc
```

### 2.3 Collection创建

```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, HnswConfig

client = QdrantClient(host="qdrant", port=6333)

client.create_collection(
    collection_name="product_knowledge",
    vectors_config=VectorParams(
        size=1024,
        distance=Distance.COSINE,
        hnsw_config=HnswConfig(
            m=16,
            ef_construct=100
        )
    )
)

client.create_collection(
    collection_name="market_insights",
    vectors_config=VectorParams(
        size=1024,
        distance=Distance.COSINE
    )
)
```

### 2.4 集群初始化

```bash
# 初始化Raft集群
curl -X POST "http://qdrant-0.qdrant-headless:6333/cluster" \
  -H "Content-Type: application/json" \
  -d '{
    "uri": "http://qdrant-0.qdrant-headless:6333"
  }'

# 添加节点
curl -X POST "http://qdrant-0.qdrant-headless:6333/cluster/peer" \
  -H "Content-Type: application/json" \
  -d '{"uri": "http://qdrant-1.qdrant-headless:6333"}'

curl -X POST "http://qdrant-0.qdrant-headless:6333/cluster/peer" \
  -H "Content-Type: application/json" \
  -d '{"uri": "http://qdrant-2.qdrant-headless:6333"}'
```

---

## 3. Kafka消息队列

### 3.1 集群架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Kafka集群架构                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Producer层                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ 数据采集  │ │ Agent事件 │ │ 系统日志  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Broker集群                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ Broker-1 │ │ Broker-2 │ │ Broker-3 │               │   │
│  │  │ Leader   │ │ Follower │ │ Follower │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Consumer层                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ ETL处理  │ │ 实时分析  │ │ 数据存储  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Kafka部署配置

```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: pms-data
spec:
  serviceName: kafka-headless
  replicas: 3
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: confluentinc/cp-kafka:7.5.0
          ports:
            - containerPort: 9092
              name: kafka
            - containerPort: 9093
              name: kafka-internal
          env:
            - name: KAFKA_BROKER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: KAFKA_ZOOKEEPER_CONNECT
              value: "zookeeper:2181"
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: "PLAINTEXT:PLAINTEXT,INTERNAL:PLAINTEXT"
            - name: KAFKA_ADVERTISED_LISTENERS
              value: "PLAINTEXT://$(POD_NAME).kafka-headless:9092,INTERNAL://$(POD_IP):9093"
            - name: KAFKA_INTER_BROKER_LISTENER_NAME
              value: "INTERNAL"
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "3"
            - name: KAFKA_TRANSACTION_STATE_LOG_MIN_ISR
              value: "2"
            - name: KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR
              value: "3"
            - name: KAFKA_LOG_RETENTION_HOURS
              value: "168"
            - name: KAFKA_LOG_SEGMENT_BYTES
              value: "1073741824"
          volumeMounts:
            - name: kafka-data
              mountPath: /var/lib/kafka/data
      volumes:
        - name: kafka-data
          persistentVolumeClaim:
            claimName: kafka-pvc
```

### 3.3 Topic创建

```bash
# 创建核心Topic
kafka-topics --create \
  --bootstrap-server kafka-0.kafka-headless:9092 \
  --topic amazon-data \
  --partitions 12 \
  --replication-factor 3 \
  --config retention.ms=604800000

kafka-topics --create \
  --bootstrap-server kafka-0.kafka-headless:9092 \
  --topic tiktok-data \
  --partitions 6 \
  --replication-factor 3

kafka-topics --create \
  --bootstrap-server kafka-0.kafka-headless:9092 \
  --topic agent-events \
  --partitions 12 \
  --replication-factor 3

# 验证Topic
kafka-topics --list --bootstrap-server kafka-0.kafka-headless:9092
```

---

## 4. vLLM推理服务

### 4.1 服务架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    vLLM推理服务                                  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    API层                                 │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ OpenAI   │ │ 自定义API │ │ WebSocket │               │   │
│  │  │ 兼容API  │ │          │ │          │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    推理引擎                              │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ PagedAtt │ │ Scheduler │ │ KV Cache │               │   │
│  │  │ ention   │ │          │ │ Manager  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    模型层                                │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │           Qwen2.5-72B-Instruct                    │  │   │
│  │  │           (TP=4, PP=2)                            │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 vLLM部署配置

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
  namespace: pms-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm
  template:
    metadata:
      labels:
        app: vllm
    spec:
      nodeSelector:
        node-role.kubernetes.io/worker-gpu: ""
      containers:
        - name: vllm
          image: vllm/vllm-openai:v0.3.0
          ports:
            - containerPort: 8000
          resources:
            limits:
              nvidia.com/gpu: 4
          env:
            - name: MODEL_NAME
              value: "Qwen/Qwen2.5-72B-Instruct"
          command:
            - python
            - -m
            - vllm.entrypoints.openai.api_server
          args:
            - --model
            - /models/Qwen2.5-72B-Instruct
            - --tensor-parallel-size
            - "4"
            - --pipeline-parallel-size
            - "2"
            - --max-model-len
            - "8192"
            - --gpu-memory-utilization
            - "0.9"
            - --enable-prefix-caching
          volumeMounts:
            - name: model-storage
              mountPath: /models
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 300
            periodSeconds: 30
      volumes:
        - name: model-storage
          persistentVolumeClaim:
            claimName: model-pvc
```

### 4.3 性能优化配置

```python
vllm_config = {
    "model": "Qwen/Qwen2.5-72B-Instruct",
    "tensor_parallel_size": 4,
    "pipeline_parallel_size": 2,
    "max_model_len": 8192,
    "gpu_memory_utilization": 0.9,
    "enable_prefix_caching": True,
    "enforce_eager": False,
    "max_num_batched_tokens": 32768,
    "max_num_seqs": 256,
    "swap_space": 4
}
```

### 4.4 性能基准测试

```bash
# 启动vLLM后测试
python benchmark_serving.py \
  --backend vllm \
  --model Qwen/Qwen2.5-72B-Instruct \
  --dataset-name sharegpt \
  --num-prompts 1000 \
  --request-rate 10

# 预期结果
# - P50延迟: <1s
# - P99延迟: <3s
# - 吞吐量: >50 tokens/s
```

---

## 5. 数据库Schema

### 5.1 核心表结构

```sql
-- 商品表
CREATE TABLE products (
    product_id VARCHAR(36) PRIMARY KEY,
    asin VARCHAR(20) UNIQUE NOT NULL,
    title VARCHAR(500) NOT NULL,
    description TEXT,
    category VARCHAR(100),
    brand VARCHAR(100),
    price DECIMAL(10, 2),
    rating DECIMAL(3, 2),
    review_count INTEGER,
    sales_rank INTEGER,
    image_url VARCHAR(500),
    source VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_products_asin ON products(asin);
CREATE INDEX idx_products_category ON products(category);
CREATE INDEX idx_products_sales_rank ON products(sales_rank);

-- 竞品表
CREATE TABLE competitors (
    competitor_id VARCHAR(36) PRIMARY KEY,
    product_id VARCHAR(36) REFERENCES products(product_id),
    competitor_asin VARCHAR(20),
    competitor_title VARCHAR(500),
    price_difference DECIMAL(10, 2),
    rating_difference DECIMAL(3, 2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 选品任务表
CREATE TABLE selection_tasks (
    task_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    query TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',
    result JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP
);

CREATE INDEX idx_selection_tasks_user ON selection_tasks(user_id);
CREATE INDEX idx_selection_tasks_status ON selection_tasks(status);

-- Agent运行记录表
CREATE TABLE agent_runs (
    run_id VARCHAR(36) PRIMARY KEY,
    task_id VARCHAR(36) REFERENCES selection_tasks(task_id),
    agent_name VARCHAR(50) NOT NULL,
    status VARCHAR(20) DEFAULT 'running',
    input JSONB,
    output JSONB,
    started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP
);

-- 知识库表
CREATE TABLE knowledge_base (
    doc_id VARCHAR(36) PRIMARY KEY,
    title VARCHAR(500) NOT NULL,
    content TEXT NOT NULL,
    doc_type VARCHAR(50),
    source VARCHAR(100),
    embedding_id VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_knowledge_base_type ON knowledge_base(doc_type);

-- 报告表
CREATE TABLE reports (
    report_id VARCHAR(36) PRIMARY KEY,
    task_id VARCHAR(36) REFERENCES selection_tasks(task_id),
    report_type VARCHAR(50) NOT NULL,
    title VARCHAR(500),
    content JSONB,
    file_url VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.2 种子数据

```sql
-- 插入测试商品
INSERT INTO products (product_id, asin, title, category, price, rating, review_count)
VALUES 
    (uuid_generate_v4(), 'B0XXXXX001', '户外便携储能电源 500W', '电子产品', 299.00, 4.5, 1234),
    (uuid_generate_v4(), 'B0XXXXX002', '太阳能充电板 100W', '电子产品', 159.00, 4.3, 567),
    (uuid_generate_v4(), 'B0XXXXX003', 'LED露营灯 可充电', '户外用品', 29.99, 4.7, 2345);
```

---

## 附录

### A. 验收检查清单

```markdown
## D7-D9 验收检查清单

### Qdrant
- [ ] 3节点Raft集群健康
- [ ] Collection创建成功
- [ ] 向量插入/检索正常
- [ ] API响应正常

### Kafka
- [ ] 3 Broker集群运行
- [ ] Topic创建成功
- [ ] 消息收发正常
- [ ] 日志保留配置生效

### vLLM
- [ ] 模型加载成功
- [ ] OpenAI兼容API可用
- [ ] P99延迟<3s
- [ ] GPU利用率正常

### 数据库
- [ ] Schema创建完成
- [ ] 索引创建成功
- [ ] 种子数据插入
```

### B. 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0 | 2026-04-06 | AI助手 | 初始版本 |

---

**文档状态**: ✅ 已完成
