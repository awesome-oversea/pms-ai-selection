# 并行执行与前端方案

> **项目名称**: 跨境电商AI选品系统（PMS增强版）
> **文档类型**: 技术设计文档
> **子任务**: D24-D26 并行执行与前端
> **文档版本**: v1.0

---

## 目录

- [1. 概述](#1-概述)
- [2. Ray并行执行](#2-ray并行执行)
- [3. Next.js前端初始化](#3-nextjs前端初始化)
- [4. Vue3后台框架](#4-vue3后台框架)
- [5. Human-in-the-loop](#5-human-in-the-loop)

---

## 1. 概述

### 1.1 设计目标

实现Agent并行执行能力，搭建前端项目框架，支持人机协作流程。

### 1.2 组件规划

| 组件 | 技术 | 用途 |
|------|------|------|
| Ray | 分布式计算 | Agent并行执行 |
| Next.js 14 | React框架 | 前端应用 |
| Vue3 + ElementUI | 后台框架 | 管理后台 |
| WebSocket | 实时通信 | 人机交互 |

---

## 2. Ray并行执行

### 2.1 Ray架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Ray集群架构                                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Head Node                             │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ GCS      │ │ Dashboard│ │ API Server│               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Worker Nodes                          │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │   │
│  │  │ Worker-1 │ │ Worker-2 │ │ Worker-3 │ │ Worker-4 │   │   │
│  │  │ (Actor)  │ │ (Actor)  │ │ (Actor)  │ │ (Actor)  │   │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 Ray集群部署

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ray-head
  namespace: pms-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ray
      role: head
  template:
    metadata:
      labels:
        app: ray
        role: head
    spec:
      containers:
        - name: ray-head
          image: rayproject/ray:2.9.0
          ports:
            - containerPort: 6379
              name: gcs
            - containerPort: 8265
              name: dashboard
            - containerPort: 10001
              name: client
          command: ["ray", "start", "--head", "--port=6379", "--dashboard-host=0.0.0.0"]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ray-worker
  namespace: pms-ai
spec:
  replicas: 4
  selector:
    matchLabels:
      app: ray
      role: worker
  template:
    metadata:
      labels:
        app: ray
        role: worker
    spec:
      containers:
        - name: ray-worker
          image: rayproject/ray:2.9.0
          command: ["ray", "start", "--address=ray-head:6379"]
```

### 2.3 Actor定义

```python
import ray
from typing import Dict, Any

@ray.remote
class DataCollectionActor:
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.agent = None
    
    async def initialize(self):
        from agents import DataCollectionAgent
        self.agent = DataCollectionAgent(self.config)
    
    async def collect(self, query: str, sources: list) -> Dict[str, Any]:
        return await self.agent.collect(query, sources)

@ray.remote
class MarketAnalysisActor:
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.agent = None
    
    async def initialize(self):
        from agents import MarketInsightAgent
        self.agent = MarketInsightAgent(self.config)
    
    async def analyze(self, market_data: Dict) -> Dict[str, Any]:
        return await self.agent.analyze(market_data)

@ray.remote
class ProductPlanningActor:
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.agent = None
    
    async def initialize(self):
        from agents import ProductPlannerAgent
        self.agent = ProductPlannerAgent(self.config)
    
    async def plan(self, analysis_result: Dict) -> Dict[str, Any]:
        return await self.agent.plan(analysis_result)

@ray.remote
class CommercialAnalysisActor:
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.agent = None
    
    async def initialize(self):
        from agents import CommercialAgent
        self.agent = CommercialAgent(self.config)
    
    async def analyze(self, product_plan: Dict) -> Dict[str, Any]:
        return await self.agent.analyze(product_plan)
```

### 2.4 并行调度器

```python
class ParallelAgentScheduler:
    def __init__(self, ray_address: str = "auto"):
        ray.init(address=ray_address)
        
        self.data_collector = DataCollectionActor.remote({"llm": "vllm"})
        self.market_analyst = MarketAnalysisActor.remote({"llm": "vllm"})
        self.product_planner = ProductPlanningActor.remote({"llm": "vllm"})
        self.commercial_analyst = CommercialAnalysisActor.remote({"llm": "vllm"})
    
    async def initialize(self):
        await asyncio.gather(
            self.data_collector.initialize.remote(),
            self.market_analyst.initialize.remote(),
            self.product_planner.initialize.remote(),
            self.commercial_analyst.initialize.remote()
        )
    
    async def run_parallel_collection(self, query: str) -> Dict[str, Any]:
        sources = ["amazon", "tiktok", "google_trends", "1688"]
        
        tasks = [
            self.data_collector.collect.remote(query, [source])
            for source in sources
        ]
        
        results = await asyncio.gather(*[
            asyncio.to_thread(ray.get, task)
            for task in tasks
        ])
        
        merged = {}
        for result in results:
            merged.update(result)
        
        return merged
    
    async def run_pipeline(self, query: str) -> Dict[str, Any]:
        market_data = await self.run_parallel_collection(query)
        
        analysis_result = await asyncio.to_thread(
            ray.get,
            self.market_analyst.analyze.remote(market_data)
        )
        
        product_plan = await asyncio.to_thread(
            ray.get,
            self.product_planner.plan.remote(analysis_result)
        )
        
        commercial_result = await asyncio.to_thread(
            ray.get,
            self.commercial_analyst.analyze.remote(product_plan)
        )
        
        return {
            "market_data": market_data,
            "analysis": analysis_result,
            "product_plan": product_plan,
            "commercial": commercial_result
        }
```

---

## 3. Next.js前端初始化

### 3.1 项目结构

```
fms-frontend/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── selection/
│   │   ├── page.tsx
│   │   └── [id]/
│   │       └── page.tsx
│   ├── products/
│   │   └── page.tsx
│   ├── reports/
│   │   └── page.tsx
│   └── settings/
│       └── page.tsx
├── components/
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   └── Input.tsx
│   ├── selection/
│   │   ├── TaskForm.tsx
│   │   ├── TaskList.tsx
│   │   └── ResultDisplay.tsx
│   └── layout/
│       ├── Header.tsx
│       ├── Sidebar.tsx
│       └── Footer.tsx
├── lib/
│   ├── api.ts
│   ├── auth.ts
│   └── utils.ts
├── styles/
│   └── globals.css
├── public/
│   └── ...
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── next.config.js
```

### 3.2 配置文件

```json
{
  "name": "fms-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@tanstack/react-query": "^5.0.0",
    "axios": "^1.6.0",
    "echarts": "^5.4.0",
    "echarts-for-react": "^3.0.0",
    "zustand": "^4.4.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^3.3.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "14.0.0"
  }
}
```

### 3.3 核心组件

```tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { useState } from 'react'
import { Header } from '@/components/layout/Header'
import { Sidebar } from '@/components/layout/Sidebar'

const queryClient = new QueryClient()

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh-CN">
      <body>
        <QueryClientProvider client={queryClient}>
          <div className="flex h-screen">
            <Sidebar />
            <div className="flex-1 flex flex-col">
              <Header />
              <main className="flex-1 overflow-auto p-6">
                {children}
              </main>
            </div>
          </div>
        </QueryClientProvider>
      </body>
    </html>
  )
}
```

```tsx
'use client'

import { useQuery } from '@tanstack/react-query'
import { TaskList } from '@/components/selection/TaskList'
import { TaskForm } from '@/components/selection/TaskForm'

export default function SelectionPage() {
  const { data: tasks, isLoading } = useQuery({
    queryKey: ['selection-tasks'],
    queryFn: async () => {
      const res = await fetch('/api/v1/selection/tasks')
      return res.json()
    }
  })

  return (
    <div className="space-y-6">
      <div className="flex justify-between items-center">
        <h1 className="text-2xl font-bold">选品分析</h1>
        <TaskForm />
      </div>
      
      {isLoading ? (
        <div>加载中...</div>
      ) : (
        <TaskList tasks={tasks} />
      )}
    </div>
  )
}
```

---

## 4. Vue3后台框架

### 4.1 项目结构

```
fms-admin/
├── src/
│   ├── layouts/
│   │   └── MainLayout.vue
│   ├── views/
│   │   ├── Dashboard.vue
│   │   ├── Products.vue
│   │   ├── Selection.vue
│   │   ├── Agents.vue
│   │   └── Settings.vue
│   ├── components/
│   │   ├── common/
│   │   │   ├── SearchBar.vue
│   │   │   └── DataTable.vue
│   │   └── business/
│   │       ├── AgentStatus.vue
│   │       └── TaskProgress.vue
│   ├── router/
│   │   └── index.ts
│   ├── stores/
│   │   ├── user.ts
│   │   └── app.ts
│   ├── api/
│   │   ├── request.ts
│   │   ├── selection.ts
│   │   └── agent.ts
│   ├── styles/
│   │   └── main.scss
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
└── tsconfig.json
```

### 4.2 核心配置

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3001,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

### 4.3 布局组件

```vue
<template>
  <el-container class="main-layout">
    <el-aside width="220px" class="sidebar">
      <div class="logo">
        <img src="@/assets/logo.png" alt="FMS" />
        <span>FMS Admin</span>
      </div>
      <el-menu
        :default-active="activeMenu"
        router
        class="sidebar-menu"
      >
        <el-menu-item index="/dashboard">
          <el-icon><HomeFilled /></el-icon>
          <span>仪表盘</span>
        </el-menu-item>
        <el-menu-item index="/selection">
          <el-icon><Search /></el-icon>
          <span>选品分析</span>
        </el-menu-item>
        <el-menu-item index="/products">
          <el-icon><Goods /></el-icon>
          <span>商品管理</span>
        </el-menu-item>
        <el-menu-item index="/agents">
          <el-icon><Monitor /></el-icon>
          <span>Agent监控</span>
        </el-menu-item>
        <el-menu-item index="/reports">
          <el-icon><Document /></el-icon>
          <span>报告中心</span>
        </el-menu-item>
        <el-menu-item index="/settings">
          <el-icon><Setting /></el-icon>
          <span>系统设置</span>
        </el-menu-item>
      </el-menu>
    </el-aside>
    
    <el-container>
      <el-header class="header">
        <div class="header-left">
          <el-breadcrumb separator="/">
            <el-breadcrumb-item :to="{ path: '/' }">首页</el-breadcrumb-item>
            <el-breadcrumb-item>{{ currentPage }}</el-breadcrumb-item>
          </el-breadcrumb>
        </div>
        <div class="header-right">
          <el-dropdown>
            <span class="user-info">
              <el-avatar :size="32" />
              <span>{{ username }}</span>
            </span>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item>个人设置</el-dropdown-item>
                <el-dropdown-item divided @click="logout">退出登录</el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>
      
      <el-main class="main-content">
        <router-view />
      </el-main>
    </el-container>
  </el-container>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()

const activeMenu = computed(() => route.path)
const currentPage = computed(() => route.meta.title || '')
const username = computed(() => userStore.username)

const logout = () => {
  userStore.logout()
  router.push('/login')
}
</script>
```

---

## 5. Human-in-the-loop

### 5.1 审批流程设计

```
┌─────────────────────────────────────────────────────────────────┐
│                    Human-in-the-loop流程                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Agent执行 ──→ 等待审批 ──→ 人工决策 ──→ 继续执行              │
│                    │              │                             │
│                    │              ├── 通过 ──→ 继续下一步       │
│                    │              ├── 拒绝 ──→ 返回修改         │
│                    │              └── 反馈 ──→ 重新分析         │
│                    │                                            │
│                    ▼                                            │
│               WebSocket通知                                     │
│                    │                                            │
│                    ▼                                            │
│               前端审批界面                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 WebSocket服务

```python
from fastapi import WebSocket, WebSocketDisconnect
from typing import Dict, Set
import asyncio

class ConnectionManager:
    def __init__(self):
        self.active_connections: Dict[str, Set[WebSocket]] = {}
    
    async def connect(self, websocket: WebSocket, user_id: str):
        await websocket.accept()
        if user_id not in self.active_connections:
            self.active_connections[user_id] = set()
        self.active_connections[user_id].add(websocket)
    
    def disconnect(self, websocket: WebSocket, user_id: str):
        if user_id in self.active_connections:
            self.active_connections[user_id].discard(websocket)
    
    async def send_to_user(self, user_id: str, message: dict):
        if user_id in self.active_connections:
            for connection in self.active_connections[user_id]:
                await connection.send_json(message)
    
    async def broadcast(self, message: dict):
        for connections in self.active_connections.values():
            for connection in connections:
                await connection.send_json(message)

manager = ConnectionManager()

@app.websocket("/ws/{user_id}")
async def websocket_endpoint(websocket: WebSocket, user_id: str):
    await manager.connect(websocket, user_id)
    try:
        while True:
            data = await websocket.receive_json()
            await handle_websocket_message(user_id, data)
    except WebSocketDisconnect:
        manager.disconnect(websocket, user_id)

async def handle_websocket_message(user_id: str, data: dict):
    message_type = data.get("type")
    
    if message_type == "approval_response":
        task_id = data.get("task_id")
        approved = data.get("approved")
        feedback = data.get("feedback")
        
        await process_approval_response(task_id, approved, feedback)
```

### 5.3 审批通知

```python
class ApprovalNotifier:
    def __init__(self, ws_manager: ConnectionManager):
        self.ws_manager = ws_manager
    
    async def request_approval(
        self,
        user_id: str,
        task_id: str,
        approval_type: str,
        content: dict
    ):
        notification = {
            "type": "approval_request",
            "task_id": task_id,
            "approval_type": approval_type,
            "content": content,
            "timestamp": datetime.now().isoformat()
        }
        
        await self.ws_manager.send_to_user(user_id, notification)
        
        await self.store_pending_approval(task_id, user_id, approval_type)
    
    async def store_pending_approval(self, task_id: str, user_id: str, approval_type: str):
        pass
```

### 5.4 前端审批组件

```vue
<template>
  <el-dialog
    v-model="visible"
    title="审批确认"
    width="600px"
    :close-on-click-modal="false"
  >
    <div class="approval-content">
      <div class="summary">
        <h4>分析摘要</h4>
        <p>{{ approvalData.summary }}</p>
      </div>
      
      <div class="details">
        <h4>详细信息</h4>
        <el-descriptions :column="2" border>
          <el-descriptions-item label="市场规模">
            {{ approvalData.marketSize }}
          </el-descriptions-item>
          <el-descriptions-item label="竞争程度">
            {{ approvalData.competition }}
          </el-descriptions-item>
          <el-descriptions-item label="预估ROI">
            {{ approvalData.roi }}%
          </el-descriptions-item>
          <el-descriptions-item label="风险等级">
            <el-tag :type="riskTagType">{{ approvalData.riskLevel }}</el-tag>
          </el-descriptions-item>
        </el-descriptions>
      </div>
      
      <div class="feedback">
        <h4>反馈意见</h4>
        <el-input
          v-model="feedback"
          type="textarea"
          :rows="3"
          placeholder="请输入您的反馈意见（可选）"
        />
      </div>
    </div>
    
    <template #footer>
      <el-button @click="handleReject">拒绝</el-button>
      <el-button type="warning" @click="handleFeedback">反馈修改</el-button>
      <el-button type="primary" @click="handleApprove">通过</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'

const props = defineProps<{
  visible: boolean
  approvalData: any
  taskId: string
}>()

const emit = defineEmits(['approve', 'reject', 'feedback', 'close'])

const feedback = ref('')

const riskTagType = computed(() => {
  const level = props.approvalData?.riskLevel
  if (level === '低') return 'success'
  if (level === '中') return 'warning'
  return 'danger'
})

const handleApprove = () => {
  emit('approve', {
    taskId: props.taskId,
    feedback: feedback.value
  })
}

const handleReject = () => {
  emit('reject', {
    taskId: props.taskId,
    feedback: feedback.value
  })
}

const handleFeedback = () => {
  emit('feedback', {
    taskId: props.taskId,
    feedback: feedback.value
  })
}
</script>
```

---

## 附录

### A. 验收检查清单

```markdown
## D24-D26 验收检查清单

### Ray并行执行
- [ ] Ray集群运行正常
- [ ] Actor定义正确
- [ ] 并行调度测试通过
- [ ] 结果聚合正确

### Next.js前端
- [ ] 项目初始化完成
- [ ] App Router配置
- [ ] TailwindCSS配置
- [ ] 基础组件开发

### Vue3后台
- [ ] 项目初始化完成
- [ ] ElementUI集成
- [ ] 路由配置
- [ ] 布局组件

### Human-in-loop
- [ ] WebSocket连接正常
- [ ] 审批通知发送
- [ ] 审批界面可用
- [ ] 流程跑通
```

### B. 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0 | 2026-04-06 | AI助手 | 初始版本 |

---

**文档状态**: ✅ 已完成
