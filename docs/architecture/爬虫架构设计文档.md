# 爬虫架构设计文档

> **项目名称**: 跨境电商AI选品系统（PMS增强版）
> **文档类型**: 数据采集架构设计文档
> **子任务**: D10-D11 - Amazon数据采集爬虫
> **文档版本**: v1.0

---

## 1. 概述

数据采集爬虫是系统获取外部电商平台数据的核心模块，负责采集Amazon BSR排名、商品详情和用户评论等数据，通过Kafka消息队列传递给下游ETL处理。

| 项目 | 规格 |
|------|------|
| 目标平台 | Amazon (BSR排名 + 商品详情 + 用户评论) |
| 开发语言 | Python (aiohttp + BeautifulSoup) |
| 消息输出 | Kafka (amazon-data / amazon-reviews topic) |
| 反爬策略 | IP代理池轮换 + User-Agent轮换 + 请求限流 |

---

## 2. 爬虫架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    数据采集爬虫架构                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    调度层                                │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ 任务队列  │ │ 代理管理  │ │ 限流控制  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    采集层                                │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │Amazon BSR│ │ 评论采集  │ │ 商品详情  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    输出层                                │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │  │ Kafka    │ │ 数据库   │ │ 文件存储  │               │   │
│  │  └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**架构说明**:
- **调度层**: 管理爬虫任务队列、IP代理池轮换、请求频率限流
- **采集层**: BSR排名爬虫(品类维度)、评论爬虫(ASIN维度)、商品详情爬虫
- **输出层**: 采集数据通过Kafka Producer发送到消息队列，下游ETL消费处理

---

## 3. Amazon BSR爬虫

### 3.1 核心实现

```python
class AmazonBSRCrawler:
    def __init__(self, proxy_pool: List[str], kafka_producer):
        self.proxy_pool = proxy_pool
        self.kafka_producer = kafka_producer
        self.base_url = "https://www.amazon.com/Best-Sellers/zgbs"

    async def crawl_category(self, category: str) -> List[Dict]:
        url = f"{self.base_url}/{category}"
        proxy = random.choice(self.proxy_pool)
        async with aiohttp.ClientSession() as session:
            async with session.get(url, headers=self.headers, proxy=proxy) as response:
                html = await response.text()
        products = self._parse_bsr_page(html, category)
        for product in products:
            await self.kafka_producer.send("amazon-data", product)
        return products
```

### 3.2 数据Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| asin | string | Amazon商品唯一标识 |
| title | string | 商品标题 |
| price | float | 价格(USD) |
| rating | float | 评分(1-5) |
| review_count | int | 评论数量 |
| rank | int | BSR排名 |
| category | string | 品类 |
| source | string | 来源标识 "amazon_bsr" |
| timestamp | string | 采集时间(ISO8601) |

### 3.3 代码文件

完整实现代码: [src/crawlers/amazon.py](../../src/crawlers/amazon.py)

---

## 4. Amazon评论爬虫

### 4.1 核心实现

```python
class AmazonReviewCrawler:
    def __init__(self, proxy_pool: List[str], kafka_producer):
        self.proxy_pool = proxy_pool
        self.kafka_producer = kafka_producer

    async def crawl_reviews(self, asin: str, max_pages: int = 10) -> List[Dict]:
        reviews = []
        for page in range(1, max_pages + 1):
            await asyncio.sleep(random.uniform(1, 3))  # 请求间隔
            # ... 获取并解析评论页面
            page_reviews = self._parse_reviews(html, asin)
            if not page_reviews:
                break
            reviews.extend(page_reviews)
            for review in page_reviews:
                await self.kafka_producer.send("amazon-reviews", review)
        return reviews
```

### 4.2 评论数据Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| review_id | string | 评论唯一ID |
| asin | string | 商品ASIN |
| rating | float | 评分(1-5) |
| title | string | 评论标题 |
| content | string | 评论内容 |
| author | string | 评论者 |
| date | string | 评论日期 |
| verified | bool | 是否已验证购买 |
| helpful_votes | int | 有帮助票数 |

---

## 5. 验收检查清单

| 检查项 | 预期结果 | 状态 |
|--------|---------|------|
| BSR爬虫框架代码 | 结构清晰，支持多品类/多地区扩展 | ☐ |
| 评论爬虫代码 | ASIN级评论抓取，支持分页 | ☐ |
| JSON Schema定义 | asin/title/price/rank/category/sales/date | ☐ |
| Kafka Producer集成 | 数据发送到amazon-data topic | ☐ |
| 解析准确性>95% | 评分/日期/内容完整性 | ☐ |
| 数据清洗规则 | 异常值剔除(空评分/超长内容/乱码) | ☐ |

---

**文档状态**: ✅ 已完成
